---
milestone: v1.0
planned: 2026-05-03T02:55:00+07:00
source_audit: .planning/v1.0-MILESTONE-AUDIT.md
status: complete
scope: milestone-gap-closure
blockers: []
---

# v1.0 Milestone Gap Closure Plan

## Purpose

Close the milestone audit blockers without changing parser semantics, artifact shape, worker contracts, replay discovery ownership, `server-2` persistence/API behavior, canonical identity, or `web` UI behavior.

This plan is intentionally narrow. The audit found no broken CLI, parser-core, schema, worker, S3 artifact, or RabbitMQ result flow. The remaining blockers are release-gate/evidence gaps:

- `scripts/coverage-gate.sh` fails because `coverage/allowlist.toml` includes `crates/parser-harness/src/benchmark_report.rs`, but that source file lacks an inline `coverage-exclusion:` marker.
- Phase 05 has summaries, UAT, and validation evidence but no `.planning/phases/05-cli-golden-parity-benchmarks-and-coverage-gates/05-VERIFICATION.md`.
- Phase 07 has summaries, human UAT, and validation evidence but no `.planning/phases/07-parallel-and-container-hardening/07-VERIFICATION.md`.

## Scope

In scope:

- Repair the strict coverage allowlist marker mismatch.
- Re-run the focused coverage gate needed to prove `TEST-07`.
- Create Phase 05 and Phase 07 verification reports from existing plan summaries, validation/UAT artifacts, and fresh gate evidence.
- Re-run the milestone audit after the verification files exist.
- Commit the resulting source/planning changes with a clean git tree.

Out of scope:

- Parser artifact contract changes.
- RabbitMQ/S3 message or artifact-key changes.
- Reopening Phase 5.2 benchmark acceptance decisions.
- Live Timeweb S3 validation, because that remains deployer-run operational evidence requiring external credentials.
- Any `server-2`, `replays-fetcher`, or `web` implementation changes.

## Execution Result

Executed on 2026-05-03. The original missing-verification blockers were closed, the original `benchmark_report.rs` marker mismatch was repaired, and the broader stale allowlist problem for current production files was refreshed. Fresh strict coverage now passes with `production_files=41`, `allowlisted_locations=2194`, and `uncovered_locations=0`. The milestone gap closure is complete for `TEST-07`.

## Action Items

[x] Add a `coverage-exclusion:` marker to `crates/parser-harness/src/benchmark_report.rs` or remove the stale `benchmark_report.rs` allowlist entry if fresh coverage proves it is no longer needed.

[x] Run `scripts/coverage-gate.sh --check` and record the exact result. Result: passed with `coverage smoke check passed; summary: .planning/generated/phase-05/coverage/check-summary.json`.

[x] Strict postprocessor follow-up: refreshed exact-line allowlist entries and inline `coverage-exclusion:` markers for current production files. Result: `scripts/coverage-gate.sh` passed with `production_files=41`, `allowlisted_locations=2194`, and `uncovered_locations=0`.

[x] Generate `.planning/phases/05-cli-golden-parity-benchmarks-and-coverage-gates/05-VERIFICATION.md` with status `passed-with-accepted-gaps` or equivalent GSD-compatible wording. It must map `CLI-01..CLI-04`, `TEST-01..TEST-05`, and `TEST-07..TEST-12` to evidence from Phase 05 summaries, `05-UAT.md`, `05-VALIDATION.md`, and the fresh coverage gate. It must explicitly note that benchmark/parity gaps were superseded by Phase 5.1/5.2 and accepted on 2026-05-02, not silently treated as Phase 05 standalone success.

[x] Generate `.planning/phases/07-parallel-and-container-hardening/07-VERIFICATION.md` with status `passed-with-deployer-evidence-pending` or equivalent GSD-compatible wording. It must map `WORK-08` and `WORK-09` to Phase 07 summaries, `07-HUMAN-UAT.md`, `07-VALIDATION.md`, worker tests, smoke evidence, and docs. It must mark live Timeweb validation as deployer-run operational evidence, not a parser code blocker.

[x] Run the focused Phase 07 verification commands that are affordable locally: `cargo test -p parser-worker -p parser-cli`, `scripts/worker-smoke.sh` if Docker is available, `scripts/fault-report-gate.sh`, and `git diff --check`. Docker was available; all listed commands passed.

[x] Re-run the milestone audit workflow or manually refresh `.planning/v1.0-MILESTONE-AUDIT.md` using the same three-source cross-reference: `REQUIREMENTS.md`, phase `*-VERIFICATION.md`, and summary frontmatter. Result: no orphaned Phase 05 or Phase 07 requirements; direct TEST-07 blocker is closed by fresh strict coverage evidence.

[x] Update `.planning/STATE.md` only if the gap-closure state changes materially, then commit all intended source and planning artifacts atomically.

## Verification Gate

The gap closure is complete only when all of the following are true:

- `scripts/coverage-gate.sh --check` passes.
- `05-VERIFICATION.md` exists and covers every Phase 05 requirement assigned in `REQUIREMENTS.md`.
- `07-VERIFICATION.md` exists and covers `WORK-08` and `WORK-09`.
- The milestone audit no longer reports orphaned Phase 05 or Phase 07 requirements.
- The repository ends with `git status --short` clean after committing intended results.

## Risk Notes

- Do not regenerate large benchmark output just to replace accepted Phase 5.2 evidence. The current milestone audit warns that the generated benchmark report is smoke-only, but project state already records product-owner benchmark acceptance on 2026-05-02.
- Do not treat Timeweb live validation as required parser work unless credentials are supplied. Local MinIO and worker smoke evidence cover parser-owned behavior; provider-specific validation belongs to deployment.
- Do not broaden this into a parser contract revision. The milestone audit blockers are evidence/gate failures, not a discovered downstream contract break.
