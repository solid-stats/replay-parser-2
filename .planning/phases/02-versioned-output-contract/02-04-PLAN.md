---
phase: 02-versioned-output-contract
plan: 04
type: execute
wave: 5
depends_on:
  - 02-03
files_modified:
  - crates/parser-contract/Cargo.toml
  - crates/parser-contract/src/failure.rs
  - crates/parser-contract/src/schema.rs
  - crates/parser-contract/src/artifact.rs
  - crates/parser-contract/src/lib.rs
  - crates/parser-contract/examples/export_schema.rs
  - crates/parser-contract/examples/parse_failure.v1.json
  - crates/parser-contract/examples/parse_artifact_success.v1.json
  - crates/parser-contract/tests/failure_contract.rs
  - crates/parser-contract/tests/schema_contract.rs
  - schemas/parse-artifact-v1.schema.json
  - README.md
autonomous: true
requirements:
  - OUT-01
  - OUT-02
  - OUT-03
  - OUT-04
  - OUT-05
  - OUT-06
  - OUT-07
requirements_addressed: [OUT-01, OUT-02, OUT-03, OUT-04, OUT-05, OUT-06, OUT-07]
must_haves:
  truths:
    - "`ParseFailure` contains job/replay/file identifiers, stage, error code, message, retryability, and source cause."
    - "JSON Schema is generated from Rust contract types and committed as `schemas/parse-artifact-v1.schema.json`."
    - "Committed success and failure examples validate against the generated schema and deserialize into `ParseArtifact`."
    - "README reflects that the initial Rust contract crate and schema now exist after execution."
    - "D-01: Final success and failure examples both deserialize into the unified `ParseArtifact` envelope."
    - "D-15: Parser-level `skipped` and `failed` outcomes remain distinct; legacy aggregate skip reasons stay compatibility diagnostics unless a later phase promotes them."
    - "D-16: `ParseFailure` includes retryability enum values `retryable`, `not_retryable`, and `unknown`, plus stage, error code, message, and source cause."
    - "D-17: `schemas/parse-artifact-v1.schema.json` is generated from Rust contract types and validated against examples."
    - "D-18: README documents parser-owned contract artifacts while preserving `server-2` and `web` ownership boundaries."
    - "D-19: `ErrorCode` validates stable namespaced families `io.*`, `json.*`, `schema.*`, `unsupported.*`, and `internal.*`."
  artifacts:
    - path: "crates/parser-contract/src/failure.rs"
      provides: "Structured failure contract"
      contains: "ParseFailure"
    - path: "crates/parser-contract/src/schema.rs"
      provides: "Schema generation helper"
      contains: "parse_artifact_schema"
    - path: "schemas/parse-artifact-v1.schema.json"
      provides: "Committed machine-readable schema"
      contains: "ParseArtifact"
  key_links:
    - from: "schemas/parse-artifact-v1.schema.json"
      to: "crates/parser-contract/src/artifact.rs"
      via: "generated from Rust types"
      pattern: "ParseArtifact"
---

<objective>
Implement structured parse failures, generated JSON Schema, committed examples, final contract validation, and README handoff.

Purpose: complete Phase 2 with a machine-checkable parser artifact and failure contract that downstream phases and `server-2` can rely on.
Output: failure/schema modules, schema export example, committed schema, success/failure example validation tests, and README update.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-versioned-output-contract/02-CONTEXT.md
@.planning/phases/02-versioned-output-contract/02-RESEARCH.md
@.planning/phases/02-versioned-output-contract/02-VALIDATION.md
@.planning/phases/02-versioned-output-contract/02-PATTERNS.md
@gsd-briefs/replay-parser-2.md
@gsd-briefs/server-2.md
@README.md
@AGENTS.md
</context>

<decision_coverage>
- D-01: Final success and failure examples both deserialize into the unified `ParseArtifact` envelope.
- D-15: Parser-level `skipped` and `failed` outcomes remain distinct; legacy aggregate skip reasons stay compatibility diagnostics unless a later phase promotes them.
- D-16: `ParseFailure` includes retryability enum values `retryable`, `not_retryable`, and `unknown`, plus stage, error code, message, and source cause.
- D-17: `schemas/parse-artifact-v1.schema.json` is generated from Rust contract types and validated against examples.
- D-18: README documents parser-owned contract artifacts while preserving `server-2` and `web` ownership boundaries.
- D-19: `ErrorCode` validates stable namespaced families `io.*`, `json.*`, `schema.*`, `unsupported.*`, and `internal.*`.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Implement structured ParseFailure and retryability</name>
  <files>crates/parser-contract/src/failure.rs, crates/parser-contract/src/artifact.rs, crates/parser-contract/tests/failure_contract.rs</files>
  <read_first>
    - `crates/parser-contract/src/artifact.rs`
    - `crates/parser-contract/src/source_ref.rs`
    - `.planning/phases/02-versioned-output-contract/02-CONTEXT.md`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/src/1 - replays/workers/types.ts`
  </read_first>
  <action>
    Implement:

    ```rust
    pub enum ParseStage {
        Input,
        Checksum,
        JsonDecode,
        Schema,
        Normalize,
        Aggregate,
        Output,
        Internal,
    }

    pub enum Retryability {
        Retryable,
        NotRetryable,
        Unknown,
    }

    pub struct ErrorCode(String);

    pub struct ParseFailure {
        pub job_id: Option<String>,
        pub replay_id: Option<String>,
        pub source_file: Option<String>,
        pub checksum: Option<SourceChecksum>,
        pub stage: ParseStage,
        pub error_code: ErrorCode,
        pub message: String,
        pub retryability: Retryability,
        pub source_cause: Option<String>,
        pub source_refs: Vec<SourceRef>,
    }
    ```

    Implement `ErrorCode::new(value)` validation:
    - must start with one of `io.`, `json.`, `schema.`, `unsupported.`, or `internal.`
    - after the family prefix, only lowercase ASCII letters, digits, dots, hyphens, and underscores are allowed
    - cannot end with a dot

    Connect `ParseArtifact.failure` to this concrete `ParseFailure` type if a placeholder exists.

    Add tests:
    - `ParseFailure should serialize structured retryability and stage when json decode fails`
    - `ErrorCode should reject unknown family when value is not namespaced`
    - `Failed artifact should carry status failed and failure object together`
  </action>
  <verify>
    <automated>cargo test -p parser-contract failure_contract && rg -n "ParseFailure|Retryability|ParseStage|ErrorCode|json_decode|not_retryable" crates/parser-contract/src/failure.rs crates/parser-contract/tests/failure_contract.rs && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `ParseFailure` contains `job_id`, `replay_id`, `source_file`, `checksum`, `stage`, `error_code`, `message`, `retryability`, `source_cause`, and `source_refs`.
    - `Retryability` serializes as `retryable`, `not_retryable`, and `unknown`.
    - `ErrorCode::new("json.decode")` succeeds.
    - `ErrorCode::new("decode")` fails.
    - `cargo test -p parser-contract failure_contract` exits 0.
  </acceptance_criteria>
  <done>OUT-07 structured failure semantics are implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Generate JSON Schema from Rust contract types</name>
  <files>crates/parser-contract/src/schema.rs, crates/parser-contract/examples/export_schema.rs, schemas/parse-artifact-v1.schema.json, crates/parser-contract/tests/schema_contract.rs</files>
  <read_first>
    - `crates/parser-contract/src/artifact.rs`
    - `crates/parser-contract/src/failure.rs`
    - `crates/parser-contract/src/schema.rs` if it exists
    - `.planning/phases/02-versioned-output-contract/02-RESEARCH.md`
  </read_first>
  <action>
    Implement `crates/parser-contract/src/schema.rs`:

    ```rust
    pub fn parse_artifact_schema() -> schemars::schema::RootSchema {
        schemars::schema_for!(crate::artifact::ParseArtifact)
    }
    ```

    Adjust the return type if the installed `schemars` version exposes a different public schema type, but keep the public function name `parse_artifact_schema`.

    Create `crates/parser-contract/examples/export_schema.rs` that prints pretty JSON for `parse_artifact_schema()` to stdout.

    Generate and commit `schemas/parse-artifact-v1.schema.json` with:

    ```bash
    mkdir -p schemas
    cargo run -p parser-contract --example export_schema > schemas/parse-artifact-v1.schema.json
    ```

    Add `schema_contract` tests that assert:
    - `schemas/parse-artifact-v1.schema.json` exists.
    - The schema text contains `ParseArtifact`.
    - The schema text contains `contract_version`, `parser`, `status`, `diagnostics`, `replay`, `entities`, `events`, `aggregates`, and `failure`.
    - The schema was generated from the Rust helper during test setup or matches a freshly generated schema string exactly.
  </action>
  <verify>
    <automated>cargo run -p parser-contract --example export_schema > /tmp/parse-artifact-v1.schema.json && cmp /tmp/parse-artifact-v1.schema.json schemas/parse-artifact-v1.schema.json && cargo test -p parser-contract schema_contract && rg -n "contract_version|ParseArtifact|failure|aggregates" schemas/parse-artifact-v1.schema.json && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `parse_artifact_schema` exists in `crates/parser-contract/src/schema.rs`.
    - `schemas/parse-artifact-v1.schema.json` is generated by the example command, not handwritten.
    - Fresh schema output is byte-identical to the committed schema file.
    - `cargo test -p parser-contract schema_contract` exits 0.
  </acceptance_criteria>
  <done>OUT-06 has a committed machine-readable schema generated from Rust types.</done>
</task>

<task type="auto">
  <name>Task 3: Validate committed success and failure examples against schema</name>
  <files>crates/parser-contract/Cargo.toml, crates/parser-contract/examples/parse_artifact_success.v1.json, crates/parser-contract/examples/parse_failure.v1.json, crates/parser-contract/tests/schema_contract.rs</files>
  <read_first>
    - `crates/parser-contract/examples/parse_artifact_success.v1.json`
    - `crates/parser-contract/src/failure.rs`
    - `schemas/parse-artifact-v1.schema.json`
  </read_first>
  <action>
    Add a dev-dependency suitable for JSON Schema validation from Rust, such as `jsonschema`, to `crates/parser-contract/Cargo.toml`.

    Update `parse_artifact_success.v1.json` so it includes concrete `replay`, `entities`, `events`, and `aggregates` values from Plans 02 and 03, while staying within Phase 2 skeleton scope. It must include:
    - one `ReplayMetadata` object with `mission_name`, `world_name`, `mission_author`, `players_count`, `capture_delay`, and `end_frame`
    - one observed entity with `source_entity_id`, `nickname`, missing `steam_id` as unknown, `side`, `group`, and `role`
    - one normalized event with `source_refs` and `rule_id`
    - one aggregate contribution with `source_refs` and `rule_id`

    Create `crates/parser-contract/examples/parse_failure.v1.json` as a full `ParseArtifact` with `status` `failed`, `failure.error_code` `json.decode`, `failure.stage` `json_decode`, `failure.retryability` `not_retryable`, and source file/checksum identifiers.

    Add tests that:
    - deserialize both examples into `ParseArtifact`
    - validate both examples against `schemas/parse-artifact-v1.schema.json`
    - assert the failure example contains `job_id`, `replay_id`, `source_file`, `stage`, `error_code`, `message`, `retryability`, and `source_cause`
  </action>
  <verify>
    <automated>cargo test -p parser-contract schema_contract && jq -e '.status == "success" and (.entities | length) == 1 and (.events | length) == 1 and (.aggregates.contributions | length) == 1' crates/parser-contract/examples/parse_artifact_success.v1.json && jq -e '.status == "failed" and .failure.error_code == "json.decode" and .failure.retryability == "not_retryable"' crates/parser-contract/examples/parse_failure.v1.json && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Success example has one replay metadata object, one entity, one event, and one aggregate contribution.
    - Failure example has `status` equal to `failed`.
    - Failure example has `failure.error_code` equal to `json.decode`.
    - Both examples validate against the committed schema in tests.
  </acceptance_criteria>
  <done>Developers can validate current success and failure artifact examples.</done>
</task>

<task type="auto">
  <name>Task 4: Update README and run final contract validation</name>
  <files>README.md</files>
  <read_first>
    - `README.md`
    - `.planning/PROJECT.md`
    - `.planning/ROADMAP.md`
    - `.planning/phases/02-versioned-output-contract/02-CONTEXT.md`
    - `.planning/phases/02-versioned-output-contract/02-RESEARCH.md`
    - `schemas/parse-artifact-v1.schema.json`
  </read_first>
  <action>
    Update `README.md` after the contract crate is implemented:
    - Change current status from "Phase 1" to "Phase 2 contract crate implemented" or equivalent accurate wording.
    - Replace "does not yet contain a runnable Rust workspace" with a statement that the initial Rust workspace contains `crates/parser-contract`.
    - Add the schema command:

      ```bash
      cargo run -p parser-contract --example export_schema > schemas/parse-artifact-v1.schema.json
      ```

    - Add the validation command:

      ```bash
      cargo test -p parser-contract
      ```

    - Keep the AI agents plus GSD workflow requirement and cross-application ownership boundaries intact.

    Then run final validation:

    ```bash
    cargo fmt --all -- --check
    cargo clippy --workspace --all-targets -- -D warnings
    cargo test --workspace
    git diff --check
    ```
  </action>
  <verify>
    <automated>rg -n "crates/parser-contract|cargo test -p parser-contract|export_schema|AI agents using the GSD workflow|server-2|web" README.md && cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - README names `crates/parser-contract`.
    - README contains `cargo test -p parser-contract`.
    - README contains `export_schema`.
    - README still says project development is performed only by AI agents using the GSD workflow.
    - Full validation command exits 0.
  </acceptance_criteria>
  <done>Phase 2 implementation is documented and fully validated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| failure artifact -> server retry logic | Retryability and stage must be explicit to avoid unsafe retries or dropped jobs. |
| Rust types -> committed schema | Schema must be generated from code to avoid contract drift. |
| README -> future agents/developers | README must not claim no Rust workspace after Phase 2 creates one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Repudiation | parse failures | mitigate | Structured `ParseFailure` includes identifiers, stage, error code, retryability, and source cause. |
| T-02-04-02 | Tampering | schema file | mitigate | Regenerate schema from Rust and compare fresh output to committed schema. |
| T-02-04-03 | Denial of Service | retry policy | mitigate | Use explicit `Retryability` enum and namespaced error-code families. |
| T-02-04-04 | Tampering | README status | mitigate | Update README and verify it names contract crate and validation commands. |
</threat_model>

<verification>
Run:

```bash
cargo test -p parser-contract failure_contract
cargo run -p parser-contract --example export_schema > /tmp/parse-artifact-v1.schema.json
cmp /tmp/parse-artifact-v1.schema.json schemas/parse-artifact-v1.schema.json
cargo test -p parser-contract schema_contract
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
rg -n "crates/parser-contract|cargo test -p parser-contract|export_schema" README.md
git diff --check
```
</verification>

<success_criteria>
Plan 04 is complete when failures are structured, examples validate against the generated schema, README reflects the new contract crate, and the workspace passes format, clippy, and tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-versioned-output-contract/02-04-SUMMARY.md`.
</output>
