---
phase: 01-legacy-baseline-and-corpus
plan: 02
type: execute
wave: 2
depends_on:
  - 01-00
files_modified:
  - .planning/phases/01-legacy-baseline-and-corpus/corpus-manifest.md
  - .planning/phases/01-legacy-baseline-and-corpus/fixture-index.json
  - .planning/PROJECT.md
  - gsd-briefs/replay-parser-2.md
autonomous: true
requirements:
  - DOC-01
  - LEG-03
  - WF-01
  - WF-02
must_haves:
  truths:
    - "Developer can inspect current full-history corpus counts for raw replays, replay list metadata, existing results, and yearly reference outputs."
    - "Corpus profile evidence includes top-level keys, event/entity shape summaries, largest files, malformed files, and game-type distribution."
    - "Fixture selection follows the current full corpus profile instead of stale 3,938-file assumptions."
  artifacts:
    - path: ".planning/phases/01-legacy-baseline-and-corpus/corpus-manifest.md"
      provides: "Committed full-history corpus manifest and profile summary"
      contains: "23,473"
    - path: ".planning/phases/01-legacy-baseline-and-corpus/fixture-index.json"
      provides: "Small deterministic fixture index derived from corpus profile"
      contains: "source_file"
    - path: ".planning/PROJECT.md"
      provides: "Project context corrected to current corpus facts"
      contains: "23,473"
  key_links:
    - from: ".planning/phases/01-legacy-baseline-and-corpus/corpus-manifest.md"
      to: ".planning/generated/phase-01/corpus-profiles/"
      via: "profile artifact path"
      pattern: "corpus-profiles/"
    - from: ".planning/phases/01-legacy-baseline-and-corpus/fixture-index.json"
      to: ".planning/phases/01-legacy-baseline-and-corpus/corpus-manifest.md"
      via: "profile-derived fixture reasons"
      pattern: "reason"
---

<objective>
Document the current full-history `~/sg_stats` corpus and create a compact fixture index grounded in actual profile evidence.

Purpose: satisfy LEG-03 and correct stale corpus-count language before later parser contract, fixture, parity, and benchmark work.
Output: `corpus-manifest.md`, `fixture-index.json`, and current corpus facts in `.planning/PROJECT.md` and `gsd-briefs/replay-parser-2.md`.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-legacy-baseline-and-corpus/01-CONTEXT.md
@.planning/phases/01-legacy-baseline-and-corpus/01-RESEARCH.md
@.planning/phases/01-legacy-baseline-and-corpus/01-VALIDATION.md
@.planning/phases/01-legacy-baseline-and-corpus/01-PATTERNS.md
@gsd-briefs/replay-parser-2.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate current corpus profile evidence</name>
  <files>.planning/phases/01-legacy-baseline-and-corpus/corpus-manifest.md, .planning/phases/01-legacy-baseline-and-corpus/fixture-index.json</files>
  <read_first>
    - `.planning/phases/01-legacy-baseline-and-corpus/01-CONTEXT.md`
    - `.planning/phases/01-legacy-baseline-and-corpus/01-RESEARCH.md`
    - `.planning/phases/01-legacy-baseline-and-corpus/01-PATTERNS.md`
    - `~/sg_stats/lists/replaysList.json`
    - `~/sg_stats/raw_replays`
    - `~/sg_stats/results`
    - `~/sg_stats/year_results`
  </read_first>
  <action>
    Create an ignored generated profile under `.planning/generated/phase-01/corpus-profiles/<UTC-profile-id>/corpus-profile.json`.

    The generated JSON must contain these fields with freshly measured values:
    - `raw_replays.count` from `find "$HOME/sg_stats/raw_replays" -maxdepth 1 -type f -name '*.json' | wc -l`; expected current value is `23473`.
    - `replays_list.replays_count` and `replays_list.parsed_replays_count`; expected current value is `23456` for both when present.
    - `replays_list.prepared_at`; expected current value is `2026-04-25T04:42:54.889Z`.
    - `results.file_count`; expected current value is `88485`.
    - `year_results.file_count`; expected current value is `14`.
    - `raw_not_listed_count`; expected current value is `17`.
    - `listed_missing_raw_count`; expected current value is `0`.
    - `largest_files`, at least the top 10 by bytes.
    - `top_level_keys`, with counts across parsed valid raw replay JSON files.
    - `event_shape_summaries`, with representative event keys/types and counts or samples.
    - `entity_shape_summaries`, with representative entity keys/types and counts or samples.
    - `malformed_files`, even if the array is empty.
    - `game_type_distribution` using replay-list mission prefixes `sg`, `mace`, `sm`, `sgs`, and `other`.

    Use null-delimited file handling for filesystem walks. Catch JSON parse errors and record them in `malformed_files`; do not let one malformed replay abort the whole profile. Keep the full generated profile ignored; commit only summary facts in `corpus-manifest.md` and compact fixture entries in `fixture-index.json`.
  </action>
  <verify>
    <automated>PROFILE=$(find .planning/generated/phase-01/corpus-profiles -name corpus-profile.json | sort | tail -1) && test -n "$PROFILE" && jq -e '.raw_replays.count == 23473 and .replays_list.prepared_at == "2026-04-25T04:42:54.889Z" and .results.file_count == 88485 and .year_results.file_count == 14 and .raw_not_listed_count == 17 and .listed_missing_raw_count == 0 and (.largest_files | length) >= 10 and has("top_level_keys") and has("event_shape_summaries") and has("entity_shape_summaries") and has("malformed_files") and has("game_type_distribution")' "$PROFILE" && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Generated `corpus-profile.json` reports `raw_replays.count` as `23473`.
    - Generated `corpus-profile.json` reports replay-list `prepared_at` as `2026-04-25T04:42:54.889Z`.
    - Generated `corpus-profile.json` reports `raw_not_listed_count` as `17` and `listed_missing_raw_count` as `0`.
    - Generated profile includes top-level keys, event shape summaries, entity shape summaries, largest files, malformed files, and game-type distribution.
  </acceptance_criteria>
  <done>Full-history corpus evidence exists locally under the ignored generated profile path.</done>
</task>

<task type="auto">
  <name>Task 2: Write corpus manifest and fixture index</name>
  <files>.planning/phases/01-legacy-baseline-and-corpus/corpus-manifest.md, .planning/phases/01-legacy-baseline-and-corpus/fixture-index.json, .planning/PROJECT.md, gsd-briefs/replay-parser-2.md</files>
  <read_first>
    - `.planning/phases/01-legacy-baseline-and-corpus/corpus-manifest.md` if it exists
    - `.planning/phases/01-legacy-baseline-and-corpus/fixture-index.json` if it exists
    - `.planning/PROJECT.md`
    - `gsd-briefs/replay-parser-2.md`
    - latest generated `.planning/generated/phase-01/corpus-profiles/*/corpus-profile.json`
  </read_first>
  <action>
    Write `.planning/phases/01-legacy-baseline-and-corpus/corpus-manifest.md` with these exact top-level sections:
    - `## Summary`
    - `## Raw Replay Corpus`
    - `## Replay List Metadata`
    - `## Raw/List Discrepancies`
    - `## Existing Results`
    - `## Yearly Reference Outputs`
    - `## Schema And Shape Profile`
    - `## Largest Files`
    - `## Malformed Files`
    - `## Game-Type Distribution`
    - `## Generated Artifacts`
    - `## Fixture Selection Rationale`

    Include the concrete facts `23,473 raw replay JSON files`, `23,456 replay-list rows`, `preparedAt 2026-04-25T04:42:54.889Z`, `88,485 result files`, `14 year_results files`, `17 raw basenames not present in replaysList.json`, and `0 listed filenames missing from raw_replays`.

    Create `.planning/phases/01-legacy-baseline-and-corpus/fixture-index.json` as a compact sorted JSON array. Each entry must include `source_file`, `reason`, `game_type_from_replays_list`, `old_parser_skip_expected`, and `cross_app_relevance`. Include entries for largest-file coverage, at least one `sg`, `mace`, `sm`, `sgs` or excluded-prefix case if present, `other`, raw-not-listed coverage, and malformed coverage if `malformed_files` is non-empty.

    Update stale corpus-count language in `.planning/PROJECT.md` and `gsd-briefs/replay-parser-2.md`: replace any current `around 3,938` raw replay assumption with full-history language naming `23,473` raw replay JSON files and `23,456` replay-list rows prepared at `2026-04-25T04:42:54.889Z`.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-legacy-baseline-and-corpus/corpus-manifest.md && test -f .planning/phases/01-legacy-baseline-and-corpus/fixture-index.json && rg -n "23,473 raw replay JSON files|23,456 replay-list rows|2026-04-25T04:42:54\\.889Z|88,485 result files|14 year_results files|17 raw basenames|0 listed filenames" .planning/phases/01-legacy-baseline-and-corpus/corpus-manifest.md && jq -e 'type == "array" and length >= 5 and all(.[]; has("source_file") and has("reason") and has("game_type_from_replays_list") and has("old_parser_skip_expected") and has("cross_app_relevance"))' .planning/phases/01-legacy-baseline-and-corpus/fixture-index.json && rg -n "23,473|23,456|2026-04-25T04:42:54\\.889Z" .planning/PROJECT.md gsd-briefs/replay-parser-2.md && ! rg -n "3,938" .planning/PROJECT.md gsd-briefs/replay-parser-2.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `corpus-manifest.md` contains the full-history counts and raw/list discrepancy facts listed in the action.
    - `fixture-index.json` is valid JSON and every entry has the required five keys.
    - `.planning/PROJECT.md` and `gsd-briefs/replay-parser-2.md` no longer contain the stale string `3,938`.
    - The manifest points to the generated profile path under `.planning/generated/phase-01/corpus-profiles/`.
  </acceptance_criteria>
  <done>Current corpus facts and fixture rationale are committed compactly, with bulky profile evidence kept ignored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw corpus -> profiler | Historical replay JSON may be malformed, huge, or shape-shifted. |
| generated profile -> committed manifest | Full profile output must remain local/ignored while summaries are committed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Denial of Service | corpus profiler | mitigate | Catch JSON parse errors, record malformed files, and continue profiling instead of aborting. |
| T-01-02-02 | Repudiation | corpus counts | mitigate | Record exact commands, generated profile path, and concrete full-history counts in `corpus-manifest.md`. |
| T-01-02-03 | Tampering | stale documentation | mitigate | Replace stale `3,938` corpus language in `.planning/PROJECT.md` and `gsd-briefs/replay-parser-2.md`. |
| T-01-02-04 | Information Disclosure | generated corpus profile | mitigate | Commit only compact summaries and `fixture-index.json`; keep full profiles ignored under `.planning/generated/phase-01/`. |
</threat_model>

<verification>
Run:

```bash
test -f .planning/phases/01-legacy-baseline-and-corpus/corpus-manifest.md
test -f .planning/phases/01-legacy-baseline-and-corpus/fixture-index.json
rg -n "23,473|23,456|88,485|17 raw basenames|2026-04-25T04:42:54\\.889Z" .planning/phases/01-legacy-baseline-and-corpus/corpus-manifest.md
jq -e 'type == "array" and length >= 5' .planning/phases/01-legacy-baseline-and-corpus/fixture-index.json
git diff --check
```
</verification>

<success_criteria>
LEG-03 is satisfied when the manifest and fixture index reflect the current full-history corpus, not prior stale counts, and generated profile evidence remains outside git.
</success_criteria>

<output>
After completion, create `.planning/phases/01-legacy-baseline-and-corpus/01-02-SUMMARY.md`.
</output>
