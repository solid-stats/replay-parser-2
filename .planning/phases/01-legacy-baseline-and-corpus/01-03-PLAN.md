---
phase: 01-legacy-baseline-and-corpus
plan: 03
type: execute
wave: 2
depends_on:
  - 01-00
files_modified:
  - .planning/phases/01-legacy-baseline-and-corpus/legacy-rules-output-surfaces.md
autonomous: true
requirements:
  - LEG-04
  - INT-01
  - INT-02
  - INT-04
must_haves:
  truths:
    - "Developer can inspect old parser game-type filters, skip rules, exclusions, and config inputs."
    - "Developer can distinguish observed identity from legacy compatibility identity before Phase 2 contract work."
    - "Developer can inventory ordinary legacy output surfaces while keeping annual nomination outputs v2-deferred."
  artifacts:
    - path: ".planning/phases/01-legacy-baseline-and-corpus/legacy-rules-output-surfaces.md"
      provides: "Legacy filters, skip rules, config inputs, identity compatibility, and output surface inventory"
      contains: "mace_min_players"
  key_links:
    - from: ".planning/phases/01-legacy-baseline-and-corpus/legacy-rules-output-surfaces.md"
      to: "/home/afgan0r/Projects/SolidGames/replays-parser/src/1 - replays/getReplays.ts"
      via: "game-type filter source reference"
      pattern: "startsWith"
    - from: ".planning/phases/01-legacy-baseline-and-corpus/legacy-rules-output-surfaces.md"
      to: "server-2/web boundaries"
      via: "identity and output ownership notes"
      pattern: "observed identity"
---

<objective>
Document legacy parser selection rules, config inputs, identity compatibility behavior, and output surfaces for the parity harness.

Purpose: satisfy LEG-04 while keeping game-type filtering and old identity compatibility out of the parser core contract.
Output: `legacy-rules-output-surfaces.md`.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-legacy-baseline-and-corpus/01-CONTEXT.md
@.planning/phases/01-legacy-baseline-and-corpus/01-RESEARCH.md
@.planning/phases/01-legacy-baseline-and-corpus/01-PATTERNS.md
@gsd-briefs/replay-parser-2.md
@gsd-briefs/server-2.md
@gsd-briefs/web.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory filters, skip rules, config inputs, and identity rules</name>
  <files>.planning/phases/01-legacy-baseline-and-corpus/legacy-rules-output-surfaces.md</files>
  <read_first>
    - `.planning/phases/01-legacy-baseline-and-corpus/01-CONTEXT.md`
    - `.planning/phases/01-legacy-baseline-and-corpus/01-RESEARCH.md`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/src/index.ts`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/src/1 - replays/getReplays.ts`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/src/1 - replays/workers/parseReplayWorker.ts`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/src/2 - parseReplayInfo/getEntities.ts`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/src/2 - parseReplayInfo/combineSamePlayersInfo.ts`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/config/excludeReplays.json`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/config/includeReplays.json`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/config/excludePlayers.json`
    - `~/sg_stats/config/nameChanges.csv` if it exists
  </read_first>
  <action>
    Create `.planning/phases/01-legacy-baseline-and-corpus/legacy-rules-output-surfaces.md` with frontmatter `phase: 01`, `artifact: legacy-rules-output-surfaces`, and `status: draft`.

    Add sections `## Game-Type Selection`, `## Skip Rules`, `## Config Inputs`, `## Identity Compatibility Rules`, and `## Harness Ownership Boundary`.

    Document these concrete legacy facts with source file references:
    - Game types are selected by `mission_name.startsWith(gameType)` in `getReplays.ts`.
    - `sgs` missions are excluded by `!replay.mission_name.startsWith('sgs')`.
    - `sm` replay selection is filtered to dates after `2023-01-01` by month in `src/index.ts`.
    - Empty parsed replay results are skipped with reason `empty_replay`.
    - `mace` parsed replay results with fewer than `10` players are skipped with reason `mace_min_players`.
    - Config inputs include `config/excludeReplays.json`, `config/includeReplays.json`, `config/excludePlayers.json`, and `~/sg_stats/config/nameChanges.csv`.
    - Observed identity belongs to the parser contract later; old name changes, same-name combining, and compatibility naming belong to a named parity/aggregate compatibility layer per D-10 and D-11.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-legacy-baseline-and-corpus/legacy-rules-output-surfaces.md && rg -n "mission_name\\.startsWith|startsWith\\('sgs'\\)|2023-01-01|empty_replay|mace_min_players|excludeReplays\\.json|includeReplays\\.json|excludePlayers\\.json|nameChanges\\.csv|observed identity|compatibility layer" .planning/phases/01-legacy-baseline-and-corpus/legacy-rules-output-surfaces.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The document names all three old parser config JSON files and `nameChanges.csv`.
    - The document includes exact skip reasons `empty_replay` and `mace_min_players`.
    - The document states game-type filters belong to the parity harness, not parser core contract.
    - The document states observed identity and compatibility identity are separate.
  </acceptance_criteria>
  <done>Legacy selection, skip, config, and identity compatibility rules are documented with source references and ownership boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Inventory ordinary and v2-deferred output surfaces</name>
  <files>.planning/phases/01-legacy-baseline-and-corpus/legacy-rules-output-surfaces.md</files>
  <read_first>
    - `.planning/phases/01-legacy-baseline-and-corpus/legacy-rules-output-surfaces.md`
    - `.planning/REQUIREMENTS.md`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/src/4 - output/index.ts`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/src/4 - output`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/src/!yearStatistics`
    - `~/sg_stats/results`
    - `~/sg_stats/year_results`
  </read_first>
  <action>
    Extend `legacy-rules-output-surfaces.md` with sections `## Ordinary Output Surfaces`, `## Ordinary Comparable Fields`, `## Annual Nomination Reference Only`, and `## Phase 2 And Phase 5 Handoff`.

    Document ordinary output surfaces including `statistics.json`, `squad_statistics.json`, `squad_full_rotation_statistics.json`, `rotations_info.json`, `stats.zip`, per-player weapon/week/other-player outputs, and rotation folders when present in old source or `~/sg_stats/results`.

    Document ordinary comparable fields including `kills`, `killsFromVehicle`, `vehicleKills`, `teamkills`, `deaths`, `kdRatio`, `killsFromVehicleCoef`, `score`, `totalPlayedGames`, `week`, `startDate`, and `endDate`.

    Document `src/!yearStatistics` and `~/sg_stats/year_results` only as v2-deferred annual nomination references per D-13 and FUT-06. Include the exact sentence: `Annual/yearly nomination outputs are historical references only for v1 and must not be folded into ordinary player, squad, rotation, weekly, bounty, or parser contract support in Phase 1.`
  </action>
  <verify>
    <automated>rg -n "statistics\\.json|squad_statistics\\.json|squad_full_rotation_statistics\\.json|rotations_info\\.json|stats\\.zip|killsFromVehicle|vehicleKills|teamkills|kdRatio|killsFromVehicleCoef|totalPlayedGames|src/!yearStatistics|year_results|Annual/yearly nomination outputs are historical references only" .planning/phases/01-legacy-baseline-and-corpus/legacy-rules-output-surfaces.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The document lists ordinary output filenames and comparable ordinary stat fields.
    - The document names `src/!yearStatistics` and `~/sg_stats/year_results` as v2-deferred references only.
    - The exact annual/yearly sentence from the action appears in the file.
    - No task implements Rust parser code, server API code, web UI, or yearly nomination product support.
  </acceptance_criteria>
  <done>Legacy output surfaces are inventoried with ordinary v1 stats separated from v2-deferred annual nomination references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy parser rules -> new parser contract | Old filter/name rules must not accidentally become normalized parser-core contract semantics. |
| ordinary outputs -> yearly outputs | Annual nomination outputs are historical reference only and must not enter v1 ordinary stats scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | parser contract boundary | mitigate | Label game-type filters and skip rules as parity harness concerns per D-10. |
| T-01-03-02 | Spoofing | identity compatibility | mitigate | Separate observed identity from compatibility identity and cite `server-2` canonical identity ownership. |
| T-01-03-03 | Repudiation | output surface inventory | mitigate | Cite old source files and existing results paths for every listed surface. |
| T-01-03-04 | Tampering | yearly nomination scope | mitigate | Include exact v2-deferred annual/yearly statement and do not plan product support. |
</threat_model>

<verification>
Run:

```bash
test -f .planning/phases/01-legacy-baseline-and-corpus/legacy-rules-output-surfaces.md
rg -n "empty_replay|mace_min_players|excludeReplays\\.json|nameChanges\\.csv|statistics\\.json|src/!yearStatistics|v2-deferred" .planning/phases/01-legacy-baseline-and-corpus/legacy-rules-output-surfaces.md
git diff --check
```
</verification>

<success_criteria>
LEG-04 is satisfied when a developer can inspect one committed document for old filters, skip rules, config inputs, identity compatibility behavior, ordinary outputs, and yearly v2-deferred references.
</success_criteria>

<output>
After completion, create `.planning/phases/01-legacy-baseline-and-corpus/01-03-SUMMARY.md`.
</output>
