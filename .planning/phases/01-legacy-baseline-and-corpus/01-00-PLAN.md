---
phase: 01-legacy-baseline-and-corpus
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md
autonomous: false
requirements:
  - WF-01
  - WF-02
  - LEG-01
  - LEG-02
must_haves:
  truths:
    - "Generated Phase 1 logs, profiles, hashes, and regenerated outputs cannot be accidentally committed."
    - "The canonical old-parser source command `pnpm run parse` is preflighted before any full baseline run."
    - "If the source command still fails, `parse:dist` is not promoted to canonical baseline without an explicit user decision."
  artifacts:
    - path: ".gitignore"
      provides: "Ignore rule for `.planning/generated/`"
      contains: ".planning/generated/"
    - path: ".planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md"
      provides: "Plan 00 source-command gate and fallback decision record"
      contains: "pnpm run parse"
  key_links:
    - from: ".gitignore"
      to: ".planning/generated/phase-01/"
      via: "ignore rule"
      pattern: "^\\.planning/generated/$"
    - from: ".planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md"
      to: "/home/afgan0r/Projects/SolidGames/replays-parser/package.json"
      via: "canonical script record"
      pattern: "parse.*tsx src/start.ts"
---

<objective>
Prepare Phase 1 for safe baseline evidence generation and block on the current canonical source-command failure before any full old-parser run.

Purpose: prevent destructive or misleading baseline work by ignoring heavy local artifacts, recording the exact `pnpm run parse` preflight, and requiring a user decision before any fallback baseline.
Output: `.gitignore`, `.planning/generated/phase-01/` local directories, and the Plan 00 gate section in `baseline-command-runtime.md`.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-legacy-baseline-and-corpus/01-CONTEXT.md
@.planning/phases/01-legacy-baseline-and-corpus/01-RESEARCH.md
@.planning/phases/01-legacy-baseline-and-corpus/01-VALIDATION.md
@.planning/phases/01-legacy-baseline-and-corpus/01-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add generated artifact hygiene</name>
  <files>.gitignore, .planning/generated/phase-01/</files>
  <read_first>
    - `AGENTS.md`
    - `.planning/phases/01-legacy-baseline-and-corpus/01-CONTEXT.md`
    - `.planning/phases/01-legacy-baseline-and-corpus/01-VALIDATION.md`
    - `.gitignore` if it already exists
  </read_first>
  <action>
    Create `.gitignore` if it does not exist. Add exactly one ignore rule for generated GSD artifacts:

    ```gitignore
    .planning/generated/
    ```

    Then create local ignored directories `mkdir -p .planning/generated/phase-01/baseline-runs .planning/generated/phase-01/corpus-profiles .planning/generated/phase-01/backups`.
    Do not add `.gitkeep` or any tracked file under `.planning/generated/`; that directory is for bulky logs, full hashes, regenerated results, backups, and profiles per D-09.
  </action>
  <verify>
    <automated>rg -n "^\\.planning/generated/$" .gitignore && git check-ignore -q .planning/generated/phase-01/baseline-runs && git check-ignore -q .planning/generated/phase-01/corpus-profiles && git check-ignore -q .planning/generated/phase-01/backups && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains the exact line `.planning/generated/`.
    - `.planning/generated/phase-01/baseline-runs`, `.planning/generated/phase-01/corpus-profiles`, and `.planning/generated/phase-01/backups` exist locally.
    - `git status --short -- .planning/generated` prints no tracked or untracked generated files.
  </acceptance_criteria>
  <done>Generated Phase 1 artifacts are ignored before any baseline logs, hashes, or profiles are created.</done>
</task>

<task type="auto">
  <name>Task 2: Record the canonical source-command preflight gate</name>
  <files>.planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md</files>
  <read_first>
    - `.planning/phases/01-legacy-baseline-and-corpus/01-CONTEXT.md`
    - `.planning/phases/01-legacy-baseline-and-corpus/01-RESEARCH.md`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/package.json`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/.nvmrc`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/src/start.ts`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/src/0 - utils/runtimeConfig.ts`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/src/0 - utils/paths.ts`
  </read_first>
  <action>
    Create or update `.planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md` with frontmatter `phase: 01`, `artifact: baseline-command-runtime`, and `status: wave-0-gate`.

    Add a section named `## Plan 00 Source Command Gate` that records, per D-01 and D-02:
    - Legacy repo path: `/home/afgan0r/Projects/SolidGames/replays-parser`
    - Legacy commit: capture with `git -C /home/afgan0r/Projects/SolidGames/replays-parser rev-parse HEAD`
    - Canonical command: `pnpm run parse`
    - Script mapping: `parse` -> `tsx src/start.ts`
    - Runtime target: `.nvmrc` `v18.14.0`
    - Local pnpm version from `pnpm --version`
    - Lockfile hash from `sha256sum /home/afgan0r/Projects/SolidGames/replays-parser/pnpm-lock.yaml`
    - Preflight log path under `.planning/generated/phase-01/baseline-runs/<UTC-run-id>/source-preflight.log`

    Run this exact preflight and append `Source command status: PASS` or `Source command status: FAIL` with the first actionable error lines:

    ```bash
    cd /home/afgan0r/Projects/SolidGames/replays-parser
    source "$HOME/.nvm/nvm.sh"
    nvm use --silent v18.14.0
    pnpm run parse -- --help > "/home/afgan0r/Projects/SolidGames/replay-parser-2/.planning/generated/phase-01/baseline-runs/<UTC-run-id>/source-preflight.log" 2>&1
    ```

    Also run `pnpm run parse:dist -- --help` only as a secondary diagnostic. Record it under a clearly labeled `Secondary diagnostic only` subsection with the exact sentence: `parse:dist is not the canonical Phase 1 baseline unless the user explicitly approves a fallback override.`
  </action>
  <verify>
    <automated>test -f .planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md && rg -n "D-01|pnpm run parse|tsx src/start.ts|Source command status: (PASS|FAIL)|parse:dist is not the canonical Phase 1 baseline" .planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `baseline-command-runtime.md` names `pnpm run parse` as canonical and `tsx src/start.ts` as its script target.
    - The file records either `Source command status: PASS` or `Source command status: FAIL`.
    - If `parse:dist` appears, the file also contains `parse:dist is not the canonical Phase 1 baseline unless the user explicitly approves a fallback override.`
    - No task runs a full old-parser baseline in this plan.
  </acceptance_criteria>
  <done>The source-command gate is documented with reproducible command, runtime, lockfile, log path, and pass/fail status.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Resolve source-command blocker before baseline execution</name>
  <files>.planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md</files>
  <read_first>
    - `.planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md`
    - `.planning/phases/01-legacy-baseline-and-corpus/01-CONTEXT.md`
    - `.planning/phases/01-legacy-baseline-and-corpus/01-VALIDATION.md`
  </read_first>
  <action>
    If `baseline-command-runtime.md` contains `Source command status: PASS`, append `Gate decision: source command unblocked; continue with canonical baseline.` and complete this checkpoint.

    If it contains `Source command status: FAIL`, pause and ask the user to choose one option:
    1. `repair-source-command` - authorize a separate compatibility repair for old parser source execution before Phase 1 baseline runs.
    2. `approve-parse-dist-fallback` - explicitly approve `pnpm run parse:dist` as a fallback baseline despite D-01; record that this is a user override.
    3. `pause` - stop Phase 1 execution until the old parser source command is fixed outside this plan.

    Do not run a full baseline while the gate is unresolved. Do not treat `parse:dist` as canonical unless the user selects `approve-parse-dist-fallback`.
  </action>
  <verify>
    <automated>rg -n "Gate decision:|User fallback override:|Source command status: PASS" .planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md</automated>
  </verify>
  <acceptance_criteria>
    - The file contains `Gate decision:` when `pnpm run parse` passes.
    - If fallback is approved, the file contains `User fallback override: approve-parse-dist-fallback` and explains that D-01 was overridden by explicit user decision.
    - If the user chooses `pause`, execution stops and no downstream baseline plan is run.
  </acceptance_criteria>
  <done>The downstream baseline plan has a recorded source-command pass or explicit user override to rely on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> generated artifacts | Ignored local evidence must not become committed source-controlled content. |
| planner/executor -> legacy parser command | A failing source command must not be silently replaced with another command. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-00-01 | Tampering | `.planning/generated/phase-01/` | mitigate | Add `.planning/generated/` to `.gitignore` before generating logs, hashes, profiles, or regenerated results. |
| T-01-00-02 | Repudiation | `baseline-command-runtime.md` | mitigate | Record command, runtime, lockfile hash, log path, and PASS/FAIL status for `pnpm run parse -- --help`. |
| T-01-00-03 | Tampering | old-parser baseline selection | mitigate | Blocking checkpoint if source command fails; `parse:dist` requires explicit user override and cannot be silently used. |
| T-01-00-04 | Information Disclosure | generated logs | mitigate | Store full logs under ignored `.planning/generated/phase-01/` and commit only sanitized summary lines. |
</threat_model>

<verification>
Run:

```bash
rg -n "^\\.planning/generated/$" .gitignore
test -f .planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md
rg -n "Source command status: (PASS|FAIL)" .planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md
git diff --check
```
</verification>

<success_criteria>
Plan 00 is complete when generated artifacts are ignored, the canonical source-command preflight is recorded, and any failed preflight has a blocking user decision before full baseline execution.
</success_criteria>

<output>
After completion, create `.planning/phases/01-legacy-baseline-and-corpus/01-00-SUMMARY.md`.
</output>
