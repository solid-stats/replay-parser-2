---
phase: 01-legacy-baseline-and-corpus
plan: 01
type: execute
wave: 2
depends_on:
  - 01-00
files_modified:
  - .planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md
autonomous: true
requirements:
  - LEG-01
  - LEG-02
  - LEG-05
  - WF-01
  - WF-02
must_haves:
  truths:
    - "Developer can reproduce the old parser baseline command without mutating real `~/sg_stats/results` or `~/sg_stats/year_results`."
    - "Developer can see command, commit, runtime versions, env inputs, worker count, logs, output locations, and output hashes used for parity."
    - "Two worker-count profiles are captured: deterministic `WORKER_COUNT=1` and legacy/default or user-approved fallback equivalent."
    - "Current `~/sg_stats/results` evidence is compared against regenerated old-parser output evidence and any differences are categorized per D-08."
  artifacts:
    - path: ".planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md"
      provides: "Reproducible old-parser baseline dossier"
      contains: "WORKER_COUNT=1"
    - path: ".planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md"
      provides: "Current-results versus regenerated-results comparison summary"
      contains: "Current Results Comparison"
  key_links:
    - from: ".planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md"
      to: ".planning/generated/phase-01/baseline-runs/"
      via: "recorded generated run paths"
      pattern: "baseline-runs/"
    - from: ".planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md"
      to: "~/sg_stats/results"
      via: "non-destructive real-results statement"
      pattern: "real ~/sg_stats/results"
    - from: ".planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md"
      to: ".planning/generated/phase-01/baseline-runs/"
      via: "D-08 current-results comparison evidence"
      pattern: "current-vs-regenerated-results"
---

<objective>
Produce the committed baseline command/runtime dossier and local isolated run evidence for the legacy parser baseline.

Purpose: satisfy LEG-01 and LEG-02 with reproducible command/runtime facts while preserving the real historical result directories.
Output: updated `baseline-command-runtime.md` and ignored generated baseline evidence under `.planning/generated/phase-01/baseline-runs/`.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-legacy-baseline-and-corpus/01-CONTEXT.md
@.planning/phases/01-legacy-baseline-and-corpus/01-RESEARCH.md
@.planning/phases/01-legacy-baseline-and-corpus/01-VALIDATION.md
@.planning/phases/01-legacy-baseline-and-corpus/01-PATTERNS.md
@.planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run isolated old-parser baseline profiles</name>
  <files>.planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md</files>
  <read_first>
    - `.planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md`
    - `.planning/phases/01-legacy-baseline-and-corpus/01-CONTEXT.md`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/package.json`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/src/0 - utils/paths.ts`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/src/4 - output/index.ts`
    - `~/sg_stats/lists/replaysList.json`
    - `~/sg_stats/config/nameChanges.csv` if it exists
  </read_first>
  <action>
    First verify the Plan 00 gate. Continue only if `baseline-command-runtime.md` contains either `Gate decision: source command unblocked` or `User fallback override: approve-parse-dist-fallback`. If neither string exists, stop and report that Plan 01 depends on the unresolved Plan 00 source-command gate.

    Capture a real-results safety digest before any run:

    ```bash
    find "$HOME/sg_stats/results" -type f -print0 | sort -z | xargs -0 sha256sum | sha256sum
    find "$HOME/sg_stats/year_results" -type f -print0 | sort -z | xargs -0 sha256sum | sha256sum
    ```

    For each profile, create an isolated fake HOME under `.planning/generated/phase-01/baseline-runs/<UTC-run-id>-wc1` and `.planning/generated/phase-01/baseline-runs/<UTC-run-id>-default`. In each run root:
    - `mkdir -p "$RUN_HOME/sg_stats"`
    - symlink real `~/sg_stats/raw_replays` to `$RUN_HOME/sg_stats/raw_replays`
    - symlink real `~/sg_stats/lists` to `$RUN_HOME/sg_stats/lists`
    - copy `~/sg_stats/config` to `$RUN_HOME/sg_stats/config` if it exists
    - do not symlink or copy real `~/sg_stats/results` or `~/sg_stats/year_results`

    Run the canonical source command for both profiles:

    ```bash
    cd /home/afgan0r/Projects/SolidGames/replays-parser
    source "$HOME/.nvm/nvm.sh"
    nvm use --silent v18.14.0
    HOME="$RUN_HOME" WORKER_COUNT=1 pnpm run parse > "$RUN_ROOT/baseline.log" 2>&1
    HOME="$RUN_HOME_DEFAULT" env -u WORKER_COUNT pnpm run parse > "$RUN_ROOT_DEFAULT/baseline.log" 2>&1
    ```

    If and only if Plan 00 recorded `User fallback override: approve-parse-dist-fallback`, use `pnpm run parse:dist` for the profile commands and label every run as `fallback command: pnpm run parse:dist, user-approved override`. After each successful run, write generated `results.sha256`, `results-file-count.txt`, and `results-size.txt` under the run root.

    Capture the real-results safety digest again after the runs and record whether the before/after digest matched.
  </action>
  <verify>
    <automated>rg -n "Gate decision: source command unblocked|User fallback override: approve-parse-dist-fallback" .planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md && rg -n "WORKER_COUNT=1|default worker|fake HOME|results.sha256|real ~/sg_stats/results.*unchanged" .planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `baseline-command-runtime.md` records a generated run path for a `WORKER_COUNT=1` profile.
    - `baseline-command-runtime.md` records a generated run path for a default or production-like worker profile.
    - The run setup explicitly says real `~/sg_stats/results` and `~/sg_stats/year_results` were not symlinked into fake HOME.
    - The file records before/after safety digests for real `~/sg_stats/results` and `~/sg_stats/year_results` and states whether they matched.
  </acceptance_criteria>
  <done>Two isolated baseline profiles are run or blocked with explicit gate status, and real historical outputs are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Compare current and regenerated old-parser result evidence</name>
  <files>.planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md</files>
  <read_first>
    - `.planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md`
    - `.planning/phases/01-legacy-baseline-and-corpus/01-CONTEXT.md`
    - `.planning/phases/01-legacy-baseline-and-corpus/01-RESEARCH.md`
    - latest generated `.planning/generated/phase-01/baseline-runs/*/results.sha256` files
    - `~/sg_stats/results`
  </read_first>
  <action>
    After Task 1 produces regenerated old-parser outputs, compare current `~/sg_stats/results` evidence against each regenerated output evidence per D-08. Create an ignored generated report at `.planning/generated/phase-01/baseline-runs/<UTC-comparison-id>/current-vs-regenerated-results.json`.

    The generated comparison report must include:
    - `current_results.path` as `~/sg_stats/results`
    - `current_results.file_count`, `current_results.size_bytes`, and an aggregate SHA-256 digest from a sorted null-delimited file list
    - `regenerated_results[]` entries for each successful isolated run, including run path, command label, worker profile, file count, size bytes, and aggregate SHA-256 digest
    - `comparison[]` entries for each regenerated run with `match: true|false`, `category`, `impact.parser_artifact`, `impact.server_2_recalculation`, `impact.ui_visible_public_stats`, and `notes`

    Use exactly these category values when classifying differences: `compatible`, `intentional change`, `old bug preserved`, `old bug fixed`, `new bug`, `insufficient data`, and `human review`. If aggregate digests and file counts match, use `compatible`. If no successful regenerated output exists because the source-command gate blocked the baseline, record one `insufficient data` comparison with the unresolved gate reason. If digests differ and there is no already documented reason in `baseline-command-runtime.md`, classify as `human review`; do not invent a preserve/fix decision for suspected legacy bugs.

    Append a committed compact section named `## Current Results Comparison` to `baseline-command-runtime.md`. The section must name D-08, the generated comparison report path, the current-results digest, each regenerated-results digest, the category for each comparison, and whether parser artifact, `server-2` recalculation, or UI-visible public stats impact was identified. Keep full file-level diff/hash output in the ignored generated report.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md && rg -n "## Current Results Comparison|D-08|current ~/sg_stats/results|regenerated old-parser output|current-vs-regenerated-results|compatible|intentional change|old bug preserved|old bug fixed|new bug|insufficient data|human review|server-2 recalculation|UI-visible public stats" .planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `baseline-command-runtime.md` contains `## Current Results Comparison`.
    - The comparison section names D-08 and the ignored generated `current-vs-regenerated-results.json` path.
    - Every regenerated result comparison is categorized with one of the seven allowed mismatch categories.
    - If baseline generation was blocked, the section records `insufficient data` with the gate reason instead of claiming parity.
    - Full file-level comparison evidence remains under `.planning/generated/phase-01/` and is not committed.
  </acceptance_criteria>
  <done>Current historical results and regenerated old-parser outputs are treated as dual evidence, with matches or categorized differences recorded for downstream parity work.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize command/runtime manifest details</name>
  <files>.planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md</files>
  <read_first>
    - `.planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md`
    - `.planning/phases/01-legacy-baseline-and-corpus/01-VALIDATION.md`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/package.json`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/.nvmrc`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/src/0 - utils/runtimeConfig.ts`
  </read_first>
  <action>
    Update `baseline-command-runtime.md` so it has these exact top-level sections:
    - `## Summary`
    - `## Canonical Command`
    - `## Runtime Versions`
    - `## Environment Inputs`
    - `## Worker Count Profiles`
    - `## Non-Destructive Isolation`
    - `## Logs And Generated Artifacts`
    - `## Output Hash Summary`
    - `## Current Results Comparison`
    - `## Reproduction Commands`
    - `## Open Baseline Issues`

    Include concrete values for old parser commit, `node --version`, `.nvmrc` value `v18.14.0`, `pnpm --version`, package manager value `pnpm@10.33.0`, `pnpm-lock.yaml` SHA-256, command used, worker count used, generated log paths, generated result paths, and output hash digest paths. Keep full logs and full hash lists under `.planning/generated/phase-01/baseline-runs/`; do not paste bulky generated output into the committed markdown.
  </action>
  <verify>
    <automated>rg -n "## Canonical Command|## Runtime Versions|## Environment Inputs|## Worker Count Profiles|## Non-Destructive Isolation|## Logs And Generated Artifacts|## Output Hash Summary|## Current Results Comparison|pnpm@10\\.33\\.0|v18\\.14\\.0|sha256" .planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - The manifest includes the exact package manager string `pnpm@10.33.0`.
    - The manifest includes the exact Node target string `v18.14.0`.
    - The manifest records both `WORKER_COUNT=1` and a default/legacy worker profile.
    - The manifest preserves the D-08 current-results comparison section.
    - The manifest records generated artifact paths instead of committing full logs or regenerated results.
  </acceptance_criteria>
  <done>Developer can inspect and reproduce the old parser command/runtime baseline without reading generated bulk artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| old parser -> fake HOME | Legacy parser writes under `os.homedir()/sg_stats`; fake HOME must isolate generated outputs. |
| generated baseline -> committed docs | Full logs and result hashes are local artifacts; committed docs contain compact sanitized evidence only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | `~/sg_stats/results` | mitigate | Run full baseline only with fake HOME and record real-results before/after digests. |
| T-01-01-02 | Repudiation | baseline command manifest | mitigate | Record commit, runtime versions, lockfile hash, command, env, worker count, logs, output locations, and hash summaries. |
| T-01-01-03 | Information Disclosure | old parser logs/env | mitigate | Commit variable names and sanitized summaries only; keep full logs under ignored generated path. |
| T-01-01-04 | Spoofing | fallback baseline command | mitigate | Use `parse:dist` only when Plan 00 recorded explicit user override. |
| T-01-01-05 | Repudiation | current/regenerated result evidence | mitigate | Compare current `~/sg_stats/results` against regenerated old-parser outputs per D-08 and categorize every match or difference. |
</threat_model>

<verification>
Run:

```bash
test -f .planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md
rg -n "pnpm run parse|WORKER_COUNT=1|fake HOME|results.sha256|Current Results Comparison|current-vs-regenerated-results|pnpm@10\\.33\\.0|v18\\.14\\.0" .planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md
git diff --check
```
</verification>

<success_criteria>
LEG-01, LEG-02, and the D-08 baseline comparison are satisfied when the committed dossier gives a reproducible old-parser baseline command, generated run evidence proves isolation, and current results are compared against regenerated old-parser outputs with categorized differences.
</success_criteria>

<output>
After completion, create `.planning/phases/01-legacy-baseline-and-corpus/01-01-SUMMARY.md`.
</output>
