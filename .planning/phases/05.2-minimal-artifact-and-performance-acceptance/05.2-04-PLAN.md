---
phase: 05.2-minimal-artifact-and-performance-acceptance
plan: 04
type: execute
wave: 5
depends_on:
  - 05.2-02
  - 05.2-03
files_modified:
  - crates/parser-harness/src/comparison.rs
  - crates/parser-harness/src/summary_report.rs
  - crates/parser-harness/tests/comparison_report.rs
  - crates/parser-cli/tests/compare_command.rs
  - scripts/benchmark-phase5.sh
autonomous: true
requirements:
  - TEST-14
  - AGG-12
  - OUT-11
  - OUT-12
requirements_addressed: [TEST-14, AGG-12, OUT-11, OUT-12]
must_haves:
  truths:
    - "D-21: old-vs-new parity compares a derived legacy view from minimal tables, not the minimal JSON shape itself."
    - "D-09: comparison surfaces do not include GitHub issue #13 vehicle_score output."
    - "D-03: derived legacy view may compute kdRatio, score, and killsFromVehicleCoef for parity only; those fields do not return to default artifact."
    - "TEST-14: reports remain summary-first and human-reviewable by default with machine-readable detailed evidence separated."
---

<objective>
Update old-vs-new comparison so Phase 5.2 parity is derived from minimal tables, removes vehicle-score surfaces, and keeps Markdown summary as the default human review output.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-CONTEXT.md
@.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-RESEARCH.md
@crates/parser-harness/src/comparison.rs
@crates/parser-harness/src/summary_report.rs
@crates/parser-harness/tests/comparison_report.rs
@crates/parser-cli/tests/compare_command.rs
@AGENTS.md
</context>

<decision_coverage>
- D-21, D-09, D-03.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Add derived legacy view from minimal artifact tables</name>
  <files>crates/parser-harness/src/comparison.rs, crates/parser-harness/tests/comparison_report.rs</files>
  <read_first>
    - `crates/parser-harness/src/comparison.rs`
    - `crates/parser-harness/tests/comparison_report.rs`
    - `crates/parser-contract/src/minimal.rs`
    - `crates/parser-core/tests/aggregate_projection.rs`
  </read_first>
  <action>
    Add comparison-side conversion from v3 minimal artifacts to legacy-equivalent views. The conversion must not require default `source_refs` or aggregate contributions.

    Add helper logic that reads:

    - `players[]`
    - `player_stats[]`
    - `kills[]`
    - `destroyed_vehicles[]`

    and builds these comparison-only surfaces:

    - `legacy.player_game_results`: one row per `player_stats[]`, including `compatibility_key`, `observed_entity_ids`, `observed_name`, `side`, `kills`, `killsFromVehicle`, `vehicleKills`, `teamkills`, `isDead`, `isDeadByTeamkill`, `deaths.total`, `deaths.byTeamkills`, `kdRatio`, `killsFromVehicleCoef`, `score`, and `totalPlayedGames: 1`.
    - `legacy.relationships`: killed/killers rows from enemy kills and teamkilled/teamkillers rows from teamkills.
    - `bounty.inputs`: rows derived from `kills[]` where `bounty_eligible` is true, carrying killer/victim player refs, side strings, weapon, and attacker vehicle context only.

    Keep derived `kdRatio`, `killsFromVehicleCoef`, and `score` inside comparison-only output. Do not add them to default parser artifacts.
  </action>
  <verify>
    <automated>cargo test -p parser-harness comparison_report && rg -n "legacy\\.player_game_results|legacy\\.relationships|bounty\\.inputs|players|player_stats|destroyed_vehicles" crates/parser-harness/src/comparison.rs crates/parser-harness/tests/comparison_report.rs && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `crates/parser-harness/src/comparison.rs` contains `legacy.player_game_results`.
    - `crates/parser-harness/src/comparison.rs` contains `player_stats`.
    - `crates/parser-harness/tests/comparison_report.rs` asserts `killsFromVehicle`.
    - `crates/parser-harness/tests/comparison_report.rs` asserts `legacy.relationships`.
    - `cargo test -p parser-harness comparison_report` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Replace selected comparison surfaces and remove vehicle-score parity</name>
  <files>crates/parser-harness/src/comparison.rs, crates/parser-harness/src/summary_report.rs, crates/parser-harness/tests/comparison_report.rs, crates/parser-cli/tests/compare_command.rs</files>
  <read_first>
    - `crates/parser-harness/src/comparison.rs`
    - `crates/parser-harness/src/summary_report.rs`
    - `crates/parser-harness/tests/comparison_report.rs`
    - `crates/parser-cli/tests/compare_command.rs`
  </read_first>
  <action>
    Replace selected comparison surfaces with:

    - `status`
    - `replay`
    - `legacy.player_game_results`
    - `legacy.relationships`
    - `bounty.inputs`

    Remove selected surfaces:

    - `participants`
    - `facts.combat`
    - `facts.aggregate_contributions`
    - `summaries.projections.legacy.player_game_results`
    - `summaries.projections.legacy.relationships`
    - `summaries.projections.bounty.inputs`
    - `summaries.projections.vehicle_score.inputs`

    Update Markdown summary tests so default `replay-parser-2 compare` output remains Markdown, optional `--detail-output` remains structured JSON, and top diffs/counts are present when derived legacy surfaces differ.
  </action>
  <verify>
    <automated>cargo test -p parser-harness comparison_report && cargo test -p parser-cli compare_command && rg -n "legacy\\.player_game_results|legacy\\.relationships|detail-output|Markdown" crates/parser-harness/src crates/parser-harness/tests crates/parser-cli/tests/compare_command.rs && ! rg -n "vehicle_score|facts\\.combat|aggregate_contributions|participants" crates/parser-harness/src/comparison.rs crates/parser-harness/tests/comparison_report.rs crates/parser-cli/tests/compare_command.rs && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `crates/parser-harness/src/comparison.rs` does not contain `vehicle_score`.
    - `crates/parser-harness/src/comparison.rs` does not contain `facts.combat`.
    - `crates/parser-harness/src/comparison.rs` does not contain `participants`.
    - `crates/parser-cli/tests/compare_command.rs` asserts Markdown output by default.
    - `crates/parser-cli/tests/compare_command.rs` asserts `--detail-output` writes JSON detail.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update benchmark old-selected adapter to emit minimal-shape comparison inputs</name>
  <files>scripts/benchmark-phase5.sh</files>
  <read_first>
    - `scripts/benchmark-phase5.sh`
    - `crates/parser-harness/src/comparison.rs`
  </read_first>
  <action>
    Update the generated old-selected adapter inside `scripts/benchmark-phase5.sh` so its saved old artifact uses the comparison-compatible legacy surfaces expected by Task 1:

    - `status`
    - `replay`
    - `legacy.player_game_results`
    - `legacy.relationships`

    Remove old adapter fields `participants`, `facts`, `summaries`, and `vehicle_score.inputs`. Keep the new parser side as a real v3 minimal parser artifact; `compare` is responsible for deriving the same legacy view from minimal tables.
  </action>
  <verify>
    <automated>bash -n scripts/benchmark-phase5.sh && rg -n "legacy\\.player_game_results|legacy\\.relationships" scripts/benchmark-phase5.sh && ! rg -n "vehicle_score|facts|summaries|participants" scripts/benchmark-phase5.sh && cargo test -p parser-harness comparison_report && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/benchmark-phase5.sh` contains `legacy.player_game_results`.
    - `scripts/benchmark-phase5.sh` contains `legacy.relationships`.
    - `scripts/benchmark-phase5.sh` does not contain `vehicle_score`.
    - `bash -n scripts/benchmark-phase5.sh` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Old parser artifact to comparison harness | Old saved output is untrusted JSON and must not redefine parser contract. |
| Minimal parser artifact to parity report | Comparison must not mistake shape differences for semantic parity failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-04-01 | Tampering | Parity conversion | mitigate | Tests assert derived legacy counters and relationships from minimal rows. |
| T-05.2-04-02 | Repudiation | Vehicle score removal | mitigate | Grep gates reject vehicle-score comparison surfaces. |
| T-05.2-04-03 | Information Disclosure | Comparison defaults | mitigate | Markdown remains summary-first; JSON detail is explicit. |
</threat_model>

<verification>
- `cargo test -p parser-harness comparison_report`
- `cargo test -p parser-cli compare_command`
- `bash -n scripts/benchmark-phase5.sh`
- `! rg -n "vehicle_score|facts\\.combat|aggregate_contributions|participants" crates/parser-harness/src/comparison.rs crates/parser-harness/tests/comparison_report.rs crates/parser-cli/tests/compare_command.rs`
- `git diff --check`
</verification>

<success_criteria>
- Comparison parity uses derived legacy views from minimal tables.
- Vehicle-score surfaces are removed from v1 comparison.
- Markdown remains the default human-readable comparison output with JSON detail opt-in.
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-04-SUMMARY.md`.
</output>
