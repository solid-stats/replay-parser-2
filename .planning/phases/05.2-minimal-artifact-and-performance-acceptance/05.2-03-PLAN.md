---
phase: 05.2-minimal-artifact-and-performance-acceptance
plan: 03
type: execute
wave: 4
depends_on:
  - 05.2-01
  - 05.2-02
files_modified:
  - crates/parser-cli/src/main.rs
  - crates/parser-cli/tests/parse_command.rs
  - crates/parser-cli/tests/schema_command.rs
  - crates/parser-contract/tests/schema_contract.rs
  - README.md
autonomous: true
requirements:
  - OUT-09
  - OUT-10
  - OUT-11
  - OUT-12
  - AGG-12
requirements_addressed: [OUT-09, OUT-10, OUT-11, OUT-12, AGG-12]
must_haves:
  truths:
    - "D-08: default artifact JSON is minified for ordinary parse output; human-readable pretty output is explicit."
    - "D-11: detailed investigation output is written only behind an explicit debug flag such as --debug-artifact <path>."
    - "D-12: debug sidecar writes full normalized detail."
    - "D-13: debug sidecar is internal tooling, deterministic and tested, but not the server-facing public contract."
    - "D-14: default path does not build or write debug detail unless requested."
    - "D-09: CLI/schema/docs expose no v1 vehicle_score output."
---

<objective>
Update the CLI and public command documentation for v3 minimal default output, explicit pretty formatting, explicit debug sidecar output, and v3 schema paths.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-CONTEXT.md
@.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-RESEARCH.md
@crates/parser-cli/src/main.rs
@crates/parser-cli/tests/parse_command.rs
@crates/parser-cli/tests/schema_command.rs
@README.md
@AGENTS.md
</context>

<decision_coverage>
- D-08, D-11, D-12, D-13, D-14, D-09.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Make parse output minified by default with explicit pretty mode</name>
  <files>crates/parser-cli/src/main.rs, crates/parser-cli/tests/parse_command.rs</files>
  <read_first>
    - `crates/parser-cli/src/main.rs`
    - `crates/parser-cli/tests/parse_command.rs`
    - `crates/parser-core/src/lib.rs`
  </read_first>
  <action>
    Update `Commands::Parse` to add:

    - `#[arg(long)] pretty: bool`
    - `#[arg(long)] debug_artifact: Option<PathBuf>`

    Change default parse output serialization from `serde_json::to_vec_pretty(&artifact)` to `serde_json::to_vec(&artifact)`. Keep exactly one trailing newline. When `--pretty` is present, use `serde_json::to_vec_pretty(&artifact)` and one trailing newline.

    Add CLI tests proving:

    - default output contains no newline before the final trailing newline by asserting `artifact_text.trim_end().lines().count() == 1`;
    - `--pretty` output has more than one line;
    - default JSON contains `players`, `player_stats`, `kills`, and `destroyed_vehicles`;
    - default JSON does not contain `participants`, `facts`, `summaries`, `source_refs`, `rule_id`, `frame`, `event_index`, `json_path`, or `vehicle_score`.
  </action>
  <verify>
    <automated>cargo test -p parser-cli parse_command && rg -n "pretty|to_vec\\(&artifact\\)|players|player_stats|destroyed_vehicles" crates/parser-cli/src/main.rs crates/parser-cli/tests/parse_command.rs && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `crates/parser-cli/src/main.rs` contains `pretty: bool`.
    - `crates/parser-cli/src/main.rs` contains `serde_json::to_vec(&artifact)`.
    - `crates/parser-cli/tests/parse_command.rs` contains `lines().count() == 1`.
    - `crates/parser-cli/tests/parse_command.rs` contains `--pretty`.
    - `cargo test -p parser-cli parse_command` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add explicit debug sidecar CLI output</name>
  <files>crates/parser-cli/src/main.rs, crates/parser-cli/tests/parse_command.rs</files>
  <read_first>
    - `crates/parser-cli/src/main.rs`
    - `crates/parser-core/src/debug_artifact.rs`
    - `crates/parser-core/src/lib.rs`
    - `crates/parser-cli/tests/parse_command.rs`
  </read_first>
  <action>
    Use the `--debug-artifact <path>` option from Task 1 to call `parser_core::parse_replay_debug` only when the option is supplied. Write the debug artifact as pretty deterministic JSON with one trailing newline to the requested path. The ordinary `--output` file remains the v3 minimal artifact.

    Add CLI tests proving:

    - `parse input --output artifact.json --debug-artifact debug.json` writes both files and exits success;
    - `artifact.json` does not contain `source_refs`;
    - `debug.json` contains `source_refs`, `rule_id`, `frame`, and `event_index`;
    - a default parse without `--debug-artifact` does not create a debug file.
  </action>
  <verify>
    <automated>cargo test -p parser-cli parse_command && rg -n "debug-artifact|parse_replay_debug|source_refs|event_index" crates/parser-cli/src/main.rs crates/parser-cli/tests/parse_command.rs && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `crates/parser-cli/src/main.rs` contains `debug_artifact: Option<PathBuf>`.
    - `crates/parser-cli/src/main.rs` contains `parse_replay_debug`.
    - `crates/parser-cli/tests/parse_command.rs` contains `--debug-artifact`.
    - `crates/parser-cli/tests/parse_command.rs` asserts debug JSON contains `source_refs`.
    - `cargo test -p parser-cli parse_command` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update schema command tests and README for v3 minimal output</name>
  <files>crates/parser-cli/tests/schema_command.rs, README.md</files>
  <read_first>
    - `crates/parser-cli/tests/schema_command.rs`
    - `crates/parser-contract/tests/schema_contract.rs`
    - `README.md`
    - `schemas/parse-artifact-v3.schema.json`
  </read_first>
  <action>
    Update schema command tests to compare against `schemas/parse-artifact-v3.schema.json` and assert schema output contains `MinimalPlayerRow`, `MinimalPlayerStatsRow`, `MinimalKillRow`, `MinimalDestroyedVehicleRow`, `players`, `player_stats`, `kills`, and `destroyed_vehicles`.

    Update README command/status sections to state:

    - Contract schema path is `schemas/parse-artifact-v3.schema.json`.
    - Example artifacts are `parse_artifact_success.v3.json` and `parse_failure.v3.json`.
    - `replay-parser-2 parse <input> --output <path>` writes minified minimal JSON by default.
    - `--pretty` writes human-readable minimal JSON.
    - `--debug-artifact <path>` writes internal full normalized detail for investigation.
    - v1 has no issue #13 vehicle score output; ordinary vehicle kill/destruction context remains.
  </action>
  <verify>
    <automated>cargo test -p parser-cli schema_command && rg -n "parse-artifact-v3|parse_artifact_success\\.v3|--debug-artifact|--pretty|minified minimal|no issue #13 vehicle score" README.md crates/parser-cli/tests/schema_command.rs && ! rg -n "parse-artifact-v2|parse_artifact_success\\.v2|vehicle_score" crates/parser-cli/tests/schema_command.rs README.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `schemas/parse-artifact-v3.schema.json`.
    - `README.md` contains `--debug-artifact`.
    - `README.md` contains `--pretty`.
    - `README.md` contains `minified minimal JSON`.
    - `crates/parser-cli/tests/schema_command.rs` contains `MinimalDestroyedVehicleRow`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI filesystem writes | User-provided output paths receive either default artifact or debug sidecar. |
| Debug flag | Full normalized evidence is sensitive/internal and must be opt-in. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-03-01 | Information Disclosure | CLI default output | mitigate | Tests assert default output lacks debug-only keys and sidecar is explicit. |
| T-05.2-03-02 | Repudiation | Output format | mitigate | Tests distinguish minified default and explicit pretty output. |
| T-05.2-03-03 | Tampering | Schema path | mitigate | Schema command tests compare to committed v3 schema. |
</threat_model>

<verification>
- `cargo test -p parser-cli parse_command schema_command`
- `rg -n "parse-artifact-v3|--debug-artifact|--pretty|minified minimal" README.md crates/parser-cli/src/main.rs crates/parser-cli/tests`
- `! rg -n "parse-artifact-v2|parse_artifact_success\\.v2|vehicle_score" crates/parser-cli/tests/schema_command.rs README.md`
- `git diff --check`
</verification>

<success_criteria>
- CLI default parse output is minified minimal v3 JSON.
- Pretty output and debug sidecar output are explicit.
- README and schema command tests reference v3 contract artifacts only.
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-03-SUMMARY.md`.
</output>
