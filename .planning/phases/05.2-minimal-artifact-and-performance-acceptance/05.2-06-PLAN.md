---
phase: 05.2-minimal-artifact-and-performance-acceptance
plan: 06
type: execute
wave: 7
depends_on:
  - 05.2-05
files_modified:
  - README.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - crates/parser-harness/src/fault_report.rs
  - crates/parser-harness/tests/fault_report_gate.rs
  - scripts/fault-report-gate.sh
autonomous: true
requirements:
  - OUT-09
  - OUT-10
  - OUT-11
  - OUT-12
  - PARS-12
  - AGG-12
  - TEST-06
  - TEST-13
  - TEST-14
  - TEST-15
requirements_addressed: [OUT-09, OUT-10, OUT-11, OUT-12, PARS-12, AGG-12, TEST-06, TEST-13, TEST-14, TEST-15]
must_haves:
  truths:
    - "D-01 through D-22: final documentation and gates reflect all Phase 5.2 locked decisions."
    - "Phase 6 remains blocked unless benchmark report records selected x3 pass, all-raw x10 pass, zero-failure pass, ratio size-gate pass, max artifact bytes <= 100000, no oversized artifacts, and compatibility acceptance."
    - "README.md remains current and states project development uses only AI agents plus GSD workflow."
---

<objective>
Run final Phase 5.2 quality gates, update documentation/planning handoff, remove stale vehicle-score fault targets, and clearly mark whether Phase 6 is unblocked or still blocked by benchmark acceptance.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-CONTEXT.md
@.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-RESEARCH.md
@README.md
@crates/parser-harness/src/fault_report.rs
@scripts/fault-report-gate.sh
@AGENTS.md
</context>

<decision_coverage>
- D-01 through D-22.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Retarget fault report gate away from removed vehicle score</name>
  <files>crates/parser-harness/src/fault_report.rs, crates/parser-harness/tests/fault_report_gate.rs, scripts/fault-report-gate.sh</files>
  <read_first>
    - `crates/parser-harness/src/fault_report.rs`
    - `crates/parser-harness/tests/fault_report_gate.rs`
    - `scripts/fault-report-gate.sh`
    - `.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-RESEARCH.md`
  </read_first>
  <action>
    Remove `parser-core::vehicle_score` from required fault targets and deterministic fallback cases.

    Required high-risk fault targets must become:

    - `parser-core::events`
    - `parser-core::aggregates`
    - `parser-core::minimal_artifact`

    Add or update deterministic fallback cases that target:

    - enemy/teamkill/suicide/null/unknown classification;
    - `killsFromVehicle` and `vehicleKills` counter preservation;
    - absence of debug-only keys from default artifacts;
    - debug sidecar source refs/rule IDs when explicitly requested.
  </action>
  <verify>
    <automated>cargo test -p parser-harness fault_report_gate && scripts/fault-report-gate.sh && rg -n "parser-core::minimal_artifact|killsFromVehicle|vehicleKills|debug sidecar" crates/parser-harness/src crates/parser-harness/tests scripts/fault-report-gate.sh && ! rg -n "vehicle_score|VehicleScore|vehicle-score" crates/parser-harness/src/fault_report.rs crates/parser-harness/tests/fault_report_gate.rs scripts/fault-report-gate.sh && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `crates/parser-harness/src/fault_report.rs` contains `parser-core::minimal_artifact`.
    - `crates/parser-harness/src/fault_report.rs` does not contain `vehicle_score`.
    - `scripts/fault-report-gate.sh` does not contain `vehicle-score`.
    - `scripts/fault-report-gate.sh` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run final quality and boundary gates</name>
  <files>README.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - `README.md`
    - `.planning/ROADMAP.md`
    - `.planning/STATE.md`
    - `.planning/generated/phase-05/benchmarks/benchmark-report.json`
  </read_first>
  <action>
    Run the final Phase 5.2 gates:

    - `cargo fmt --all -- --check`
    - `cargo clippy --workspace --all-targets -- -D warnings`
    - `cargo test --workspace`
    - `cargo doc --workspace --no-deps`
    - `scripts/coverage-gate.sh --check`
    - `scripts/fault-report-gate.sh`
    - `scripts/benchmark-phase5.sh --ci`
    - `cargo run -p parser-harness --bin benchmark-report-check -- --report .planning/generated/phase-05/benchmarks/benchmark-report.json`
    - `git diff --check`

    Add boundary grep checks:

    - no active production/test v1 code references `vehicle_score`, `VehicleScore`, or `vehicle-score`;
    - default artifact source/tests do not reference `artifact.participants`, `artifact.facts`, or `artifact.summaries`;
    - CLI/default artifact tests assert absence of `source_refs`, `rule_id`, `frame`, `event_index`, and `json_path`.

    If a command fails, fix code/tests/docs before proceeding. If the only failure is full performance acceptance unavailable in CI smoke mode or artifact-size acceptance above 100 KB, record it as a Phase 6 blocker in README/ROADMAP/STATE rather than marking Phase 5.2 accepted.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace && cargo doc --workspace --no-deps && scripts/coverage-gate.sh --check && scripts/fault-report-gate.sh && scripts/benchmark-phase5.sh --ci && cargo run -p parser-harness --bin benchmark-report-check -- --report .planning/generated/phase-05/benchmarks/benchmark-report.json && ! rg -n "vehicle_score|VehicleScore|vehicle-score" crates/parser-contract/src crates/parser-contract/tests crates/parser-core/src crates/parser-core/tests crates/parser-harness/src crates/parser-harness/tests crates/parser-cli/src crates/parser-cli/tests schemas README.md && ! rg -n "artifact\\.participants|artifact\\.facts|artifact\\.summaries" crates/parser-core/src crates/parser-core/tests crates/parser-cli/tests && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0.
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0.
    - `cargo test --workspace` exits 0.
    - `scripts/coverage-gate.sh --check` exits 0.
    - `scripts/fault-report-gate.sh` exits 0.
    - `cargo run -p parser-harness --bin benchmark-report-check -- --report .planning/generated/phase-05/benchmarks/benchmark-report.json` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update README, ROADMAP, REQUIREMENTS, and STATE handoff</name>
  <files>README.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - `README.md`
    - `.planning/ROADMAP.md`
    - `.planning/REQUIREMENTS.md`
    - `.planning/STATE.md`
    - `.planning/generated/phase-05/benchmarks/benchmark-report.json`
    - `.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-SERVER-COMPATIBILITY.md`
  </read_first>
  <action>
    Update handoff docs based on final gate evidence.

    If benchmark report records selected x3 pass, all-raw x10 pass, zero-failure pass, ratio size-gate pass, `max_artifact_bytes <= 100000`, `oversized_artifact_count == 0`, and compatibility acceptance:

    - mark Phase 5.2 execution complete;
    - mark OUT-09, OUT-10, OUT-11, OUT-12, PARS-12, AGG-12, TEST-06, TEST-13, TEST-14, and TEST-15 as complete/validated where appropriate;
    - update STATE current focus to Phase 6 ready to plan/execute depending on roadmap state.

    If any acceptance gate is missing, unknown, failed, or any successful default artifact exceeds 100 KB (100,000 bytes):

    - keep Phase 6 blocked;
    - record exact benchmark fields and failure/skip/size/parity/speed status in README, ROADMAP, and STATE;
    - do not mark TEST-06, TEST-13, or TEST-15 complete.

    In all cases, README must state that development uses only AI agents plus GSD workflow and that v1 has no GitHub issue #13 vehicle score output.
  </action>
  <verify>
    <automated>rg -n "Phase 5\\.2|minimal flat|players\\[\\]|player_stats\\[\\]|destroyed_vehicles\\[\\]|debug sidecar|AI agents plus GSD|issue #13|Phase 6" README.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `AI agents plus GSD`.
    - `README.md` contains `players[]`.
    - `README.md` contains `debug sidecar`.
    - `.planning/STATE.md` contains `Phase 05.2`.
    - `.planning/ROADMAP.md` contains `05.2-06-PLAN.md`.
    - `.planning/REQUIREMENTS.md` contains `GitHub issue #13 vehicle score`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Quality gates to phase handoff | A green test suite alone is insufficient if performance acceptance failed or was not run. |
| Documentation to future Phase 6 execution | Incorrect handoff could let worker integration proceed on unaccepted parser output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-06-01 | Repudiation | Final acceptance status | mitigate | README/ROADMAP/STATE record exact benchmark pass/fail/unknown fields. |
| T-05.2-06-02 | Tampering | Fault targets | mitigate | Fault report no longer targets removed vehicle-score code and instead targets minimal artifact behavior. |
| T-05.2-06-03 | Denial of Service | Phase 6 readiness | mitigate | Phase 6 remains blocked unless all Phase 5.2 gates, including the hard 100 KB max default artifact gate, pass or user explicitly accepts gaps. |
</threat_model>

<verification>
- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `cargo doc --workspace --no-deps`
- `scripts/coverage-gate.sh --check`
- `scripts/fault-report-gate.sh`
- `scripts/benchmark-phase5.sh --ci`
- `cargo run -p parser-harness --bin benchmark-report-check -- --report .planning/generated/phase-05/benchmarks/benchmark-report.json`
- `git diff --check`
</verification>

<success_criteria>
- Final code quality, coverage, fault, benchmark report, hard 100 KB max artifact, and boundary gates pass, or any remaining benchmark acceptance gap is explicitly recorded as a Phase 6 blocker.
- README, ROADMAP, REQUIREMENTS, and STATE reflect the actual Phase 5.2 outcome.
- Active v1 code has no vehicle-score implementation or default-artifact references to removed Phase 5.1 compact surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-06-SUMMARY.md`.
</output>
