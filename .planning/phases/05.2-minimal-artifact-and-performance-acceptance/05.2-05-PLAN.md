---
phase: 05.2-minimal-artifact-and-performance-acceptance
plan: 05
type: execute
wave: 6
depends_on:
  - 05.2-03
  - 05.2-04
files_modified:
  - crates/parser-harness/src/benchmark_report.rs
  - crates/parser-harness/src/bin/benchmark-report-check.rs
  - crates/parser-harness/tests/benchmark_report.rs
  - crates/parser-harness/benches/parser_pipeline.rs
  - scripts/benchmark-phase5.sh
  - README.md
autonomous: false
requirements:
  - TEST-06
  - TEST-13
  - TEST-15
  - PARS-12
requirements_addressed: [TEST-06, TEST-13, TEST-15, PARS-12]
user_setup:
  - "Full acceptance requires local old parser repo, pnpm, and ~/sg_stats/raw_replays. If the all-raw benchmark is too expensive during execution, record a blocking gap rather than marking Phase 5.2 accepted."
  - "Default parser output has a hard product cap: every successful default parse artifact must be <= 100 KB (100,000 bytes)."
must_haves:
  truths:
    - "D-15: benchmark evidence first captures current old/new baseline on chosen workloads, then proves Phase 5.2 implementation."
    - "D-16: selected single-replay x3 gate uses automatic stable large-replay selection and records selected identity."
    - "D-17: full-corpus x10 gate is end-to-end sequential evidence: old WORKER_COUNT=1 and new sequential artifact writing."
    - "D-18: full-corpus gate covers all files in ~/sg_stats/raw_replays, not just replay-list rows."
    - "D-19: full-corpus gate has a zero-failure policy unless an explicit user-approved allowlist exists."
    - "D-20: all-raw successful artifacts pass median <= 5% and p95 <= 10% artifact/raw ratio, and every successful default artifact is <= 100 KB (100,000 bytes)."
    - "D-21: parity uses a derived legacy view from minimal tables."
  artifacts:
    - path: ".planning/generated/phase-05/benchmarks/benchmark-report.json"
      provides: "Phase 5.2 benchmark acceptance evidence"
      contains: "all_raw_corpus"
---

<objective>
Replace the Phase 5.1 benchmark report and script with Phase 5.2 acceptance gates for selected large replay x3, all-raw corpus x10, zero failed/skipped artifacts, artifact-size percentiles, and the hard 100 KB maximum default artifact size.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-CONTEXT.md
@.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-RESEARCH.md
@crates/parser-harness/src/benchmark_report.rs
@crates/parser-harness/tests/benchmark_report.rs
@crates/parser-harness/benches/parser_pipeline.rs
@scripts/benchmark-phase5.sh
@README.md
@AGENTS.md
</context>

<decision_coverage>
- D-15, D-16, D-17, D-18, D-19, D-20, D-21.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Define Phase 5.2 benchmark acceptance report schema</name>
  <files>crates/parser-harness/src/benchmark_report.rs, crates/parser-harness/src/bin/benchmark-report-check.rs, crates/parser-harness/tests/benchmark_report.rs</files>
  <read_first>
    - `crates/parser-harness/src/benchmark_report.rs`
    - `crates/parser-harness/src/bin/benchmark-report-check.rs`
    - `crates/parser-harness/tests/benchmark_report.rs`
    - `.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-CONTEXT.md`
  </read_first>
  <action>
    Update benchmark report types to represent Phase 5.2 gates explicitly.

    Add or refactor persisted report fields so `benchmark-report.json` contains:

    - `phase: "05.2"`.
    - `old_baseline_profile` containing `WORKER_COUNT=1`.
    - `artifact_size_limit_bytes: 100000`.
    - `selected_large_replay` object with `selection_policy: "largest .json by byte size under ~/sg_stats/raw_replays; tie-break lexicographic path"`, `path`, `raw_bytes`, `sha256`, `old_wall_time_ms`, `new_wall_time_ms`, `speedup`, `x3_status`, `parity_status`, `artifact_bytes`, `artifact_raw_ratio`, and `artifact_size_status`.
    - `all_raw_corpus` object with `selector: "~/sg_stats/raw_replays/**/*.json sorted lexicographically"`, `attempted_count`, `success_count`, `failed_count`, `skipped_count`, `raw_bytes`, `artifact_bytes`, `old_wall_time_ms`, `new_wall_time_ms`, `speedup`, `x10_status`, `median_artifact_raw_ratio`, `p95_artifact_raw_ratio`, `max_artifact_bytes`, `oversized_artifact_count`, `size_gate_status`, `zero_failure_status`, and `triage`.
    - `allowlist` object with `path` and `approval_status`, or `null` when no allowlist is used.
    - `rss_note`.

    Validator rules:

    - selected `x3_status` is `pass` only when `speedup >= 3.0` and `parity_status == "passed"`;
    - all-raw `x10_status` is `pass` only when `speedup >= 10.0`;
    - zero-failure passes only when `failed_count == 0` and `skipped_count == 0`, unless allowlist approval status is `accepted_by_user`;
    - selected `artifact_size_status` is `pass` only when `artifact_bytes <= 100000`;
    - all-raw `size_gate_status` is `pass` only when `median_artifact_raw_ratio <= 0.05`, `p95_artifact_raw_ratio <= 0.10`, `max_artifact_bytes <= 100000`, and `oversized_artifact_count == 0`;
    - fail/unknown statuses require triage text mentioning `bottleneck`, `parity`, `artifact`, and `failure`.
  </action>
  <verify>
    <automated>cargo test -p parser-harness benchmark_report && rg -n "selected_large_replay|all_raw_corpus|x3_status|x10_status|median_artifact_raw_ratio|p95_artifact_raw_ratio|max_artifact_bytes|oversized_artifact_count|artifact_size_limit_bytes|zero_failure_status|WORKER_COUNT=1" crates/parser-harness/src crates/parser-harness/tests && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `crates/parser-harness/src/benchmark_report.rs` contains `selected_large_replay`.
    - `crates/parser-harness/src/benchmark_report.rs` contains `all_raw_corpus`.
    - `crates/parser-harness/src/benchmark_report.rs` contains `x3_status`.
    - `crates/parser-harness/src/benchmark_report.rs` contains `x10_status`.
    - `crates/parser-harness/src/benchmark_report.rs` contains `artifact_size_limit_bytes`.
    - `crates/parser-harness/src/benchmark_report.rs` contains `max_artifact_bytes`.
    - `crates/parser-harness/tests/benchmark_report.rs` rejects median ratio above `0.05`.
    - `crates/parser-harness/tests/benchmark_report.rs` rejects p95 ratio above `0.10`.
    - `crates/parser-harness/tests/benchmark_report.rs` rejects an artifact size above `100000`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update benchmark script for selected large replay and all-raw corpus</name>
  <files>scripts/benchmark-phase5.sh</files>
  <read_first>
    - `scripts/benchmark-phase5.sh`
    - `crates/parser-harness/src/benchmark_report.rs`
    - `crates/parser-harness/benches/parser_pipeline.rs`
  </read_first>
  <action>
    Rewrite `scripts/benchmark-phase5.sh --ci` so it still runs a portable smoke path, but generates a Phase 5.2 report shape and documents full acceptance prerequisites.

    Implement selected large replay selection:

    - enumerate `"$RAW_REPLAYS_DIR"/*.json` recursively;
    - choose the largest file by byte size;
    - tie-break by lexicographic full path;
    - record path, bytes, and SHA-256.

    Implement all-raw corpus runner:

    - enumerate every `*.json` file under `"$RAW_REPLAYS_DIR"` sorted lexicographically;
    - run `target/release/replay-parser-2 parse "$replay" --output "$output"` sequentially for every file;
    - never skip missing replay-list rows because the selector is raw files, not `replaysList.json`;
    - count every attempted, successful, failed, and skipped artifact;
    - write a machine-readable failure list under `.planning/generated/phase-05/benchmarks/all-raw-failures.json`;
    - write a machine-readable oversized-artifact list under `.planning/generated/phase-05/benchmarks/all-raw-oversized-artifacts.json`;
    - compute artifact/raw ratios for successful artifacts and record median, p95, max artifact bytes, and oversized artifact count.

    Implement old baseline commands:

    - selected old baseline uses the existing old-parser runner pattern with `HOME=<generated-fake-home> WORKER_COUNT=1 pnpm exec tsx ...` when metadata exists;
    - full old baseline uses `HOME=<generated-fake-home> WORKER_COUNT=1 pnpm run parse` or a documented deterministic old runner; if old cannot cover every raw file, report `x10_status: "unknown"` with triage instead of pass.

    Keep `--ci` allowed to emit unknown acceptance when prerequisites are missing, but the validator must still accept the report as a smoke report only when triage is complete. Full acceptance requires a report with selected x3 pass, all-raw x10 pass, zero-failure pass, ratio size-gate pass, and every successful default artifact <= 100 KB (100,000 bytes).
  </action>
  <verify>
    <automated>bash -n scripts/benchmark-phase5.sh && scripts/benchmark-phase5.sh --ci && cargo run -p parser-harness --bin benchmark-report-check -- --report .planning/generated/phase-05/benchmarks/benchmark-report.json && rg -n "selected_large_replay|all_raw_corpus|all-raw-failures|all-raw-oversized-artifacts|WORKER_COUNT=1|median_artifact_raw_ratio|p95_artifact_raw_ratio|max_artifact_bytes|100000|raw_replays" scripts/benchmark-phase5.sh .planning/generated/phase-05/benchmarks/benchmark-report.json && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/benchmark-phase5.sh` contains `all-raw-failures.json`.
    - `scripts/benchmark-phase5.sh` contains `all-raw-oversized-artifacts.json`.
    - `scripts/benchmark-phase5.sh` contains `WORKER_COUNT=1`.
    - `scripts/benchmark-phase5.sh` contains `median_artifact_raw_ratio`.
    - `scripts/benchmark-phase5.sh` contains `p95_artifact_raw_ratio`.
    - `scripts/benchmark-phase5.sh` contains `100000`.
    - `scripts/benchmark-phase5.sh --ci` exits 0.
    - `cargo run -p parser-harness --bin benchmark-report-check -- --report .planning/generated/phase-05/benchmarks/benchmark-report.json` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update benchmark names, docs, and acceptance instructions</name>
  <files>crates/parser-harness/benches/parser_pipeline.rs, README.md</files>
  <read_first>
    - `crates/parser-harness/benches/parser_pipeline.rs`
    - `README.md`
    - `.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-CONTEXT.md`
  </read_first>
  <action>
    Rename Criterion benchmark surfaces from compact terminology to minimal terminology:

    - `parse_only_minimal_decode`
    - `facts_only_minimal_projection`
    - `end_to_end_minimal_parse_replay`

    Update README benchmark section with exact acceptance language:

    - selected large replay x3 gate: automatic largest-raw replay selection, old/new wall times, parity, artifact size;
    - all-raw x10 gate: every file in `~/sg_stats/raw_replays` attempted sequentially, old `WORKER_COUNT=1`, new sequential artifact writing;
    - zero failed/skipped artifacts unless an explicit user-approved allowlist exists;
    - median artifact/raw ratio <= 5%, p95 <= 10%, and every successful default artifact <= 100 KB (100,000 bytes);
    - tiny fixtures are smoke evidence only and do not define acceptance.
  </action>
  <verify>
    <automated>cargo bench -p parser-harness --bench parser_pipeline -- --sample-size 10 && rg -n "parse_only_minimal_decode|facts_only_minimal_projection|end_to_end_minimal_parse_replay|x3|x10|median artifact/raw ratio|p95|100 KB|100,000 bytes|zero failed/skipped" crates/parser-harness/benches/parser_pipeline.rs README.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `crates/parser-harness/benches/parser_pipeline.rs` contains `parse_only_minimal_decode`.
    - `crates/parser-harness/benches/parser_pipeline.rs` contains `end_to_end_minimal_parse_replay`.
    - `README.md` contains `median artifact/raw ratio <= 5%`.
    - `README.md` contains `p95 <= 10%`.
    - `README.md` contains `100 KB`.
    - `README.md` contains `WORKER_COUNT=1`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Benchmark script to performance claims | Reports can mislead if missing old baseline, skipped files, or artifact-size failures are accepted as pass. |
| Raw corpus filesystem | Local `~/sg_stats/raw_replays` may contain malformed or unexpected files; every attempt must be counted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-05-01 | Spoofing | Performance pass status | mitigate | Validator derives x3/x10 pass only from numeric speedup and parity/status rules. |
| T-05.2-05-02 | Repudiation | Corpus coverage | mitigate | Report records attempted/success/failed/skipped counts and all-raw failure list. |
| T-05.2-05-03 | Tampering | Artifact-size evidence | mitigate | Validator enforces median, p95, max-byte, and oversized-artifact thresholds from recorded artifact evidence. |
</threat_model>

<verification>
- `cargo test -p parser-harness benchmark_report`
- `bash -n scripts/benchmark-phase5.sh`
- `scripts/benchmark-phase5.sh --ci`
- `cargo run -p parser-harness --bin benchmark-report-check -- --report .planning/generated/phase-05/benchmarks/benchmark-report.json`
- `cargo bench -p parser-harness --bench parser_pipeline -- --sample-size 10`
- `git diff --check`
</verification>

<success_criteria>
- Benchmark report schema can distinguish smoke evidence from full acceptance.
- Selected large replay x3, all-raw x10, zero-failure, ratio artifact-size, and hard 100 KB max artifact gates are enforceable.
- README documents exact Phase 5.2 performance acceptance requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-05-SUMMARY.md`.
</output>
