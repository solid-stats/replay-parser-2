---
phase: 05.2-minimal-artifact-and-performance-acceptance
plan: 01
subsystem: parser-contract
tags: [rust, serde, schemars, json-schema, minimal-artifact]
requires:
  - phase: 05.2-00
    provides: Accepted minimal artifact compatibility gate
provides:
  - Phase 5.2 v3 minimal flat artifact contract
  - Retired issue 13 vehicle score contract vocabulary
  - Generated parse-artifact v3 schema and minimal examples
affects: [parser-contract, parser-core, parser-cli, parser-harness, server-2]
tech-stack:
  added: []
  patterns: [minimal flat artifact rows, generated schema freshness tests]
key-files:
  created:
    - crates/parser-contract/src/minimal.rs
    - crates/parser-contract/examples/parse_artifact_success.v3.json
    - crates/parser-contract/examples/parse_failure.v3.json
    - schemas/parse-artifact-v3.schema.json
  modified:
    - crates/parser-contract/src/artifact.rs
    - crates/parser-contract/src/aggregates.rs
    - crates/parser-contract/src/events.rs
    - crates/parser-contract/src/schema.rs
    - crates/parser-contract/src/version.rs
    - crates/parser-contract/tests/schema_contract.rs
key-decisions:
  - "The public parser contract is now v3.0.0 and uses minimal flat tables: players, player_stats, kills, destroyed_vehicles, and diagnostics."
  - "Issue 13 vehicle score contract types, schema helpers, projection keys, and contract tests were removed from v1 while ordinary vehicle context fields remain."
  - "side_facts remains in the ParseArtifact contract but is defaultable so minimal examples can omit rule-id/source-reference provenance."
patterns-established:
  - "Generated schema is byte-compared against schemas/parse-artifact-v3.schema.json."
  - "Vehicle context tests preserve raw_weapon and attacker vehicle id/name/class without issue 13 score taxonomy."
requirements-completed: [OUT-09, OUT-10, OUT-11, OUT-12, AGG-12]
duration: 10 min
completed: 2026-05-01
---

# Phase 05.2 Plan 01: Contract v3 Minimal Artifact Summary

**v3 parser contract with minimal flat tables and no active issue 13 vehicle-score surfaces**

## Performance

- **Duration:** 10 min
- **Started:** 2026-05-01T02:08:27Z
- **Completed:** 2026-05-01T02:19:01Z
- **Tasks:** 3
- **Files modified:** 18

## Accomplishments

- Added `minimal.rs` with public Serde/JsonSchema rows for players, player stats, kills, and destroyed vehicles.
- Replaced `ParseArtifact` compact `participants`/`facts`/`summaries` sections with v3 minimal arrays and bumped `CURRENT_CONTRACT_VERSION` to `3.0.0`.
- Removed active issue 13 vehicle-score vocabulary from contract types, schema helper definitions, projection keys, and contract tests.
- Added v3 success/failure examples and committed a fresh `schemas/parse-artifact-v3.schema.json`.

## Task Commits

1. **Task 1: Define v3 minimal table contract and artifact envelope** - `892cc25` (feat)
2. **Task 2: Remove issue #13 vehicle-score contract surfaces** - `f7227d2` (feat)
3. **Task 3: Regenerate v3 schema and minimal examples** - `cc27c28` (feat)

## Files Created/Modified

- `crates/parser-contract/src/minimal.rs` - Minimal flat row contract types.
- `crates/parser-contract/src/artifact.rs` - v3 artifact envelope fields and defaultable `side_facts`.
- `crates/parser-contract/src/aggregates.rs` - Removed retired score contribution kind/value types.
- `crates/parser-contract/src/events.rs` - Removed retired score taxonomy from vehicle context.
- `crates/parser-contract/src/schema.rs` - Removed retired score helper definitions, schema conditions, and projection keys.
- `crates/parser-contract/examples/parse_artifact_success.v3.json` - Minimal success example.
- `crates/parser-contract/examples/parse_failure.v3.json` - Minimal failure example with empty tables.
- `schemas/parse-artifact-v3.schema.json` - Generated v3 schema.
- `crates/parser-contract/tests/*` - Updated contract tests for v3 tables and active vehicle context.

## Decisions Made

- Kept `side_facts` in the contract per Task 1, but made it `serde(default)` so the minimal v3 examples do not need rule/source provenance fields.
- Used exact-key grep checks for removed compact fields because the plan's broad `facts` pattern also matches retained `side_facts` and schema prose.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Updated failure contract constructors**
- **Found during:** Task 1
- **Issue:** `ParseArtifact` field replacement meant `failure_contract.rs` constructors would not compile with the package tests.
- **Fix:** Updated those constructors to use `players`, `player_stats`, `kills`, and `destroyed_vehicles`.
- **Files modified:** `crates/parser-contract/tests/failure_contract.rs`
- **Verification:** `cargo test -p parser-contract --test failure_contract`
- **Committed in:** `892cc25`

**2. [Rule 3 - Blocking] Defaulted side_facts for minimal examples**
- **Found during:** Task 3
- **Issue:** The plan kept `side_facts` in `ParseArtifact`, but the minimal success example could not include `rule_id` or source-reference provenance.
- **Fix:** Added `#[serde(default)]` to `ParseArtifact::side_facts` and omitted `side_facts` from v3 examples while preserving the contract field.
- **Files modified:** `crates/parser-contract/src/artifact.rs`
- **Verification:** `cargo test -p parser-contract --test artifact_envelope --test schema_contract`
- **Committed in:** `cc27c28`

---

**Total deviations:** 2 auto-fixed blocking issues.
**Impact on plan:** Both changes were necessary to keep the v3 minimal examples and contract tests coherent without removing retained `side_facts`.

## Issues Encountered

- The plan's Cargo verification commands used multiple positional test filters, which Cargo rejects. Equivalent integration-test binary commands using repeated `--test` flags were run instead.
- The plan's broad example/schema grep for `facts` is a false positive against retained `side_facts` and schema descriptions. Exact-key grep for `"participants"`, `"facts"`, `"summaries"`, plus retired score strings, passed.

## Verification

- `cargo test -p parser-contract --test artifact_envelope --test aggregate_contract --test combat_event_contract --test source_ref_contract --test version_contract --test schema_contract` passed.
- `cargo test -p parser-contract` passed.
- `cargo run -p parser-contract --example export_schema > /tmp/phase052-schema.json` and `cmp /tmp/phase052-schema.json schemas/parse-artifact-v3.schema.json` passed.
- `rg -n "VehicleScore|vehicle_score|vehicle-score|VehicleScoreInput|VehicleScoreSign" crates/parser-contract/src crates/parser-contract/tests` returned no matches.
- Exact compact-key/schema grep returned no `"participants"`, `"facts"`, `"summaries"`, `vehicle_score`, or `VehicleScore` matches in the v3 success example/schema.
- `git diff --check` passed.

## Known Stubs

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Ready for `05.2-02`: parser-core can now be updated to construct the v3 minimal rows and remove the remaining issue 13 implementation surfaces outside `parser-contract`.

## Self-Check: PASSED

- Found `crates/parser-contract/src/minimal.rs`.
- Found `crates/parser-contract/examples/parse_artifact_success.v3.json`.
- Found `crates/parser-contract/examples/parse_failure.v3.json`.
- Found `schemas/parse-artifact-v3.schema.json`.
- Found `.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-01-SUMMARY.md`.
- Verified task commits exist: `892cc25`, `f7227d2`, `cc27c28`.
- Re-ran parser-contract tests and schema freshness checks successfully.

---
*Phase: 05.2-minimal-artifact-and-performance-acceptance*
*Completed: 2026-05-01*
