---
phase: 05.2-minimal-artifact-and-performance-acceptance
plan: 05
subsystem: parser-harness
tags: [rust, benchmark, acceptance-gates, minimal-artifact, performance]
requires:
  - phase: 05.2-03
    provides: CLI default v3 minimal artifact output and explicit debug sidecar
  - phase: 05.2-04
    provides: Derived legacy comparison from minimal tables
provides:
  - Phase 05.2 benchmark report schema with selected_large_replay and all_raw_corpus gates
  - Hard default artifact size validation using artifact_size_limit_bytes 100000
  - Selected large replay x3, parity, and artifact-size status evidence
  - All-raw corpus x10, zero-failure, median/p95, max-byte, and oversized-artifact evidence hooks
affects: [parser-harness, benchmark, README, Phase 05.2, Phase 6]
tech-stack:
  added: []
  patterns:
    - benchmark validator rejects false pass statuses and accepts smoke unknowns only with complete triage
    - benchmark script records machine-readable all-raw failures and oversized artifact lists
    - Criterion parser pipeline names use minimal artifact terminology
key-files:
  created:
    - .planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-05-SUMMARY.md
  modified:
    - crates/parser-harness/src/benchmark_report.rs
    - crates/parser-harness/src/bin/benchmark-report-check.rs
    - crates/parser-harness/tests/benchmark_report.rs
    - crates/parser-harness/benches/parser_pipeline.rs
    - scripts/benchmark-phase5.sh
    - README.md
key-decisions:
  - "Phase 05.2 benchmark reports use report_version 2 with selected_large_replay, all_raw_corpus, allowlist, rss_note, and artifact_size_limit_bytes."
  - "The default artifact hard limit is exactly 100000 bytes; selected size passes only with artifact_bytes <= 100000."
  - "All-raw size passes only with median artifact/raw ratio <= 0.05, p95 <= 0.10, max_artifact_bytes <= 100000, and oversized_artifact_count == 0."
  - "scripts/benchmark-phase5.sh --ci emits structurally valid smoke reports with unknown statuses when full prerequisites are absent; full acceptance still requires pass statuses."
patterns-established:
  - "Benchmark gate statuses are validated from numeric evidence rather than trusted as free-form report claims."
  - "All-raw runner counts every raw *.json attempt and writes JSON failure/oversized evidence for triage."
requirements-completed: [TEST-06, TEST-13, TEST-15, PARS-12]
duration: 15m
completed: 2026-05-01
---

# Phase 05.2 Plan 05: Benchmark Acceptance Gates Summary

**Phase 05.2 benchmark reports now enforce selected x3, all-raw x10, zero-failure, percentile, and exact 100000-byte artifact gates**

## Performance

- **Duration:** 15m
- **Started:** 2026-05-01T03:27:59Z
- **Completed:** 2026-05-01T03:42:59Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- Replaced the Phase 5.1 compact benchmark report vocabulary with explicit Phase 5.2 `selected_large_replay`, `all_raw_corpus`, `allowlist`, `rss_note`, and `artifact_size_limit_bytes` fields.
- Added validator coverage for false x3/x10 pass claims, zero-failure policy, median/p95 artifact ratios, max artifact bytes, oversized artifact counts, and selected artifact size over `100000`.
- Rewrote `scripts/benchmark-phase5.sh --ci` to choose the largest raw replay deterministically, write all-raw failure and oversized-artifact JSON files, and emit the new report shape.
- Renamed Criterion benchmark surfaces to `parse_only_minimal_decode`, `facts_only_minimal_projection`, and `end_to_end_minimal_parse_replay`.
- Documented full benchmark acceptance language in README.

## Task Commits

Each task was committed atomically:

1. **Task 1: Define Phase 5.2 benchmark acceptance report schema** - `81e5a02` (feat)
2. **Task 2: Update benchmark script for selected large replay and all-raw corpus** - `eef2750` (feat)
3. **Task 3: Update benchmark names, docs, and acceptance instructions** - `78b8af7` (docs)

## Files Created/Modified

- `crates/parser-harness/src/benchmark_report.rs` - Defines and validates Phase 05.2 benchmark acceptance report fields and gate status rules.
- `crates/parser-harness/src/bin/benchmark-report-check.rs` - Reports Phase 05.2 gate statuses from validated benchmark JSON.
- `crates/parser-harness/tests/benchmark_report.rs` - Covers exact `100000` limit, selected artifact oversize rejection, all-raw median/p95/max/oversized rejection, x3/x10 false-pass rejection, triage, and allowlist behavior.
- `scripts/benchmark-phase5.sh` - Generates selected-large replay evidence, all-raw runner hooks, failure/oversized JSON evidence, and Phase 05.2 report JSON.
- `crates/parser-harness/benches/parser_pipeline.rs` - Uses minimal benchmark names and minimal table rows.
- `README.md` - Documents selected x3, all-raw x10, zero failed/skipped, median/p95, and hard 100 KB acceptance.

## Decisions Made

- Kept `scripts/benchmark-phase5.sh --ci` as a portable smoke/report-generation path: it can validate unknown statuses when prerequisites are absent, but it cannot mark full acceptance unless all gates pass.
- Separated Criterion benchmark execution from the benchmark report script. Criterion is validated by its own cargo bench gate; the acceptance report focuses on selected/all-raw evidence.
- Preserved `WORKER_COUNT=1` as mandatory old baseline text even when old baseline execution is unavailable in smoke mode.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Removed stale Criterion execution from the benchmark report script**
- **Found during:** Task 2 (Update benchmark script for selected large replay and all-raw corpus)
- **Issue:** The existing script invoked `cargo bench`, but the pre-Task-3 benchmark still referenced removed `artifact.summaries` fields and blocked `scripts/benchmark-phase5.sh --ci`.
- **Fix:** Kept `benchmark-phase5.sh` focused on report generation and wrote a bench-log note; Task 3 then updated Criterion names and minimal table usage and verified `cargo bench`.
- **Files modified:** `scripts/benchmark-phase5.sh`, `crates/parser-harness/benches/parser_pipeline.rs`
- **Verification:** `scripts/benchmark-phase5.sh --ci`, `cargo bench -p parser-harness --bench parser_pipeline -- --sample-size 10`, and `git diff --check` passed.
- **Committed in:** `eef2750`, `78b8af7`

**2. [Rule 3 - Blocking] Supported the actual replay-list metadata shape**
- **Found during:** Task 2 (Update benchmark script for selected large replay and all-raw corpus)
- **Issue:** The metadata lookup assumed `replaysList.json` was a top-level array, but the current corpus file is an object whose replay rows are under `replays`.
- **Fix:** Updated metadata and replay-count parsing to handle both top-level arrays and object-wrapped `replays` arrays.
- **Files modified:** `scripts/benchmark-phase5.sh`
- **Verification:** `scripts/benchmark-phase5.sh --ci` generated a valid report.
- **Committed in:** `eef2750`

---

**Total deviations:** 2 auto-fixed blocking issues.
**Impact on plan:** Both fixes were required to make the planned acceptance gates executable against the current Phase 05.2 minimal artifact and local corpus shape.

## Issues Encountered

- The latest generated smoke report is structurally valid but not an acceptance pass. It selected `/home/afgan0r/sg_stats/raw_replays/2021_10_31__00_13_51_ocap.json`, recorded `artifact_bytes: 203683`, and therefore set `selected_large_replay.artifact_size_status: fail` against the exact `100000` byte limit.
- The smoke report records `selected_large_replay.x3_status: unknown` because the old `WORKER_COUNT=1` selected baseline/parity did not run in the portable `--ci` path.
- The smoke report records all-raw x10/size/zero-failure statuses as `unknown` because `RUN_PHASE5_FULL_CORPUS=1` was not enabled.

## Verification

- `cargo test -p parser-harness benchmark_report` passed.
- `bash -n scripts/benchmark-phase5.sh` passed.
- `scripts/benchmark-phase5.sh --ci` passed and generated a structurally valid Phase 05.2 report.
- `cargo run -p parser-harness --bin benchmark-report-check -- --report .planning/generated/phase-05/benchmarks/benchmark-report.json` passed.
- `cargo bench -p parser-harness --bench parser_pipeline -- --sample-size 10` passed.
- `rg -n "selected_large_replay|all_raw_corpus|all-raw-failures|all-raw-oversized-artifacts|WORKER_COUNT=1|median_artifact_raw_ratio|p95_artifact_raw_ratio|max_artifact_bytes|100000|raw_replays" scripts/benchmark-phase5.sh .planning/generated/phase-05/benchmarks/benchmark-report.json` found the expected evidence fields.
- `rg -n "parse_only_minimal_decode|facts_only_minimal_projection|end_to_end_minimal_parse_replay|x3|x10|median artifact/raw ratio|p95|100 KB|100,000 bytes|zero failed/skipped" crates/parser-harness/benches/parser_pipeline.rs README.md` found the expected benchmark/docs language.
- `git diff --check` passed.

## Known Stubs

None. Empty arrays and empty strings in `scripts/benchmark-phase5.sh` are runtime accumulators/defaults for generated evidence, not user-facing stub data.

## Authentication Gates

None.

## User Setup Required

Full acceptance evidence still requires local old parser prerequisites and explicit full-corpus execution: old parser repo, pnpm, `~/sg_stats/raw_replays`, and `RUN_PHASE5_FULL_CORPUS=1` plus old baseline coverage when measuring all-raw x10.

## Next Phase Readiness

Ready for Plan 06 final quality gates and blocker handoff. Phase 6 remains blocked until the generated benchmark report has selected x3 pass, selected artifact-size pass, all-raw x10 pass, zero-failure pass, and all-raw size-gate pass.

## Self-Check: PASSED

- Found `.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-05-SUMMARY.md`.
- Found key modified files on disk.
- Verified task commits exist: `81e5a02`, `eef2750`, `78b8af7`.
- Re-ran plan verification commands successfully: benchmark report tests, script syntax, script smoke, report checker, Criterion bench, rg gates, and `git diff --check`.

---
*Phase: 05.2-minimal-artifact-and-performance-acceptance*
*Completed: 2026-05-01*
