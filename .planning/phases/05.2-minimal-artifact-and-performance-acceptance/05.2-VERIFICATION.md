---
phase: "05.2-minimal-artifact-and-performance-acceptance"
verified_at: "2026-05-02T07:19:43Z"
status: accepted_with_user_overrides
score: "benchmark gaps accepted by product owner after implementation"
gaps_found: 0
human_needed: false
overrides_applied: 0
---

# Phase 05.2 Verification - Minimal Artifact and Performance Acceptance

## Verdict

Phase 05.2 execution produced the minimal v3 default artifact, debug sidecar path, issue #13 removal, derived legacy comparison, and benchmark gate machinery. After quick tasks `260502-ecp`, `260502-i8w`, and `260502-jeh`, the selected default artifact is below the hard 100 KB limit and all-raw max artifact size is below the hard 100 KB limit.

On 2026-05-02 the product owner accepted the remaining benchmark gaps: current measured performance is sufficient, p95 artifact/raw ratio above 10% is acceptable, and the 4 known malformed/non-JSON all-raw files are acceptable when old/new error parity matches.

Phase 6 can proceed.

On 2026-05-02, a follow-up five-sample year-edge old/new parity review covered
364 selected replay entries across 291 unique replay files. The new parser
succeeded on all selected entries. The review found no new statistics mismatch
classes beyond the documented retained weapon rows, duplicate-slot
`isDeadByTeamkill` merge behavior, and the old `teamkillers` merge bug. This
parity follow-up is non-blocking for Phase 05.2/Phase 5 readiness.

## Goal-Backward Check

| Success Criterion | Verdict | Evidence |
|-------------------|---------|----------|
| Product-owner compatibility acceptance is recorded before changing the default parser artifact contract. | pass | `05.2-SERVER-COMPATIBILITY.md` records the minimal flat artifact decision and user approval from Plan 00. |
| Default v3 success artifact uses minimal flat tables: `players[]`, `player_stats[]`, `kills[]`, `destroyed_vehicles[]`, and `diagnostics[]`. | pass | Contract/schema/examples, parser-core output tests, and CLI parse tests cover the v3 minimal default artifact. |
| Default rows exclude debug-only provenance such as source refs, rule IDs, event indexes, normalized event snapshots, and entity snapshots. | pass | Review fix CR-02 introduced minimal diagnostics; parser-core and parser-cli regression tests pass. |
| Full provenance remains available only through an explicit debug sidecar. | pass | `parse_replay_debug` and `--debug-artifact <path>` behavior are covered, including the overwrite rejection regression. |
| GitHub issue #13 `vehicle_score` output and scoring implementation are removed from active v1/v3 parser surfaces. | pass | Phase 05.2 summaries and final boundary checks record removal; stale v2 schema was deleted from active schemas. |
| Old-vs-new comparison derives legacy review surfaces from minimal tables and excludes removed vehicle score parity. | pass | Plan 04 comparison report tests and summaries record derived legacy comparison surfaces. |
| Benchmark checker distinguishes structural report validity from acceptance pass/fail. | pass | `benchmark-report-check --mode structural` passes, while `--mode acceptance` rejects the current non-acceptance report. |
| Selected large replay default artifact is <= 100 KB. | pass | Latest selected artifact evidence records `artifact_bytes=40780`, below `artifact_size_limit_bytes=100000`. |
| Selected large replay x3 speed and parity evidence pass. | accepted | Historical x3/parity statuses remain reported, but the product owner accepted current performance on 2026-05-02. |
| All-raw corpus performance, failure compatibility, and size gates pass. | accepted | Latest all-raw evidence records speedup `2.1277x`, max artifact bytes `48313`, zero oversized artifacts, and 4 known malformed/non-JSON failures accepted when old/new error parity matches. |

**Score:** accepted complete after product-owner benchmark overrides.

## Accepted Benchmark Evidence

Latest benchmark report: `.planning/generated/phase-05/benchmarks/benchmark-report.json`.

| Gate | Current Value | Required Value | Status |
|------|---------------|----------------|--------|
| Hard default artifact limit | `artifact_size_limit_bytes=100000` | `100000` | pass |
| Selected artifact bytes | `40780` | `<= 100000` | pass |
| Selected x3 status | `fail`/`unknown` in recent reports | reported, not blocking after 2026-05-02 acceptance | accepted |
| Selected parity status | `human_review`/`not_run` in recent reports | reported, not blocking after 2026-05-02 acceptance | accepted |
| All-raw attempted count | `23473` | all raw files attempted | pass |
| All-raw x10 status | `fail` at `2.1277x` | reported, not blocking after 2026-05-02 acceptance | accepted |
| All-raw max artifact bytes | `48313` | `<= 100000` | pass |
| All-raw p95 ratio | `0.12417910447761193` | reported trend evidence | accepted |
| All-raw known failures | `4` | match accepted malformed-file parity evidence | accepted |

Selected replay:

`/home/afgan0r/sg_stats/raw_replays/2021_10_31__00_13_51_ocap.json`

## Five-Sample Parity Follow-Up

Full rollup: `.planning/quick/260502-year-edge-parity-five-sample-rollup/`.

| Item | Value |
|------|-------|
| Sample runs | `5` |
| Selected replay entries | `364` |
| Unique selected replay files | `291` |
| New parser successes | `364` |
| Old parser comparable successes | `305` |
| Old parser skipped rows | `59` |
| Stats-only matches | `212` |
| Stats-only mismatches | `93` |
| New parser failures | `0` |

Known mismatch detail rows:

- `104` retained `Throw`/`Binoculars` weapon rows.
- `26` duplicate-slot `isDeadByTeamkill` rows where the old parser keeps a
  true flag through boolean-OR merging and the new parser follows the latest
  counted merged-player death.
- `2` old-parser `teamkillers` merge-bug rows.

No new mismatch class appeared after five deterministic samples. The accepted
difference classes are documented in
`.planning/quick/260502-year-edge-parity-five-sample-rollup/KNOWN-DIFFERENCES.md`.

## Requirement Coverage

| Requirement | Verdict | Evidence |
|-------------|---------|----------|
| OUT-09 | pass | Default output is now compact/minimal and excludes full normalized event/entity dumps. |
| OUT-10 | pass | Heavy audit/debug/parity/source-reference detail is debug-sidecar or raw-reprocessing material, not ordinary default output. |
| OUT-11 | pass | Default v1/v3 success artifacts use `players[]`, `player_stats[]`, `kills[]`, `destroyed_vehicles[]`, and `diagnostics[]`. |
| OUT-12 | pass | Default kill/destruction rows keep current stats context while detailed evidence is debug-sidecar-only. |
| PARS-12 | pass | Selective/default path avoids the retired full artifact surfaces and default debug provenance. |
| AGG-12 | pass | GitHub issue #13 vehicle score and `vehicle_score` output are removed from active parser contract and default artifact surfaces. |
| TEST-14 | pass | Old-vs-new comparison reports are summary-first and derive comparable legacy surfaces from minimal tables. |
| TEST-06 | pass | Benchmark reports record selected/all-raw performance; current measured performance is accepted by the product owner. |
| TEST-13 | pass | Reports contain required performance, size, percentile, parity, failure, and x3/x10 status evidence. |
| TEST-15 | pass | Selected and all-raw max artifact sizes are below `100000`; p95 is report-only after user acceptance. |

## Automated Checks

Post-review-fix checks observed by the orchestrator:

| Command | Result |
|---------|--------|
| `cargo test -p parser-harness benchmark_report` | pass |
| `cargo test -p parser-cli parse_command` | pass |
| `cargo test -p parser-contract schema_helpers` | pass |
| `cargo test -p parser-contract schema_contract` | pass |
| `cargo test -p parser-core` | pass |
| `bash -n scripts/benchmark-phase5.sh` | pass |
| `cargo fmt --all -- --check` | pass |
| `cargo clippy --workspace --all-targets -- -D warnings` | pass |
| `cargo test --workspace -- --list` | pass/listed test binaries only |
| `cargo run -p parser-harness --bin benchmark-report-check -- --report .planning/generated/phase-05/benchmarks/benchmark-report.json --mode structural` | pass |
| `cargo run -p parser-harness --bin benchmark-report-check -- --report .planning/generated/phase-05/benchmarks/benchmark-report.json --mode acceptance` | expected fail: current report is not acceptance evidence |
| `gsd-sdk query verify.schema-drift 05.2` | pass: `drift_detected=false`, `blocking=false` |
| `gsd-sdk query verify.codebase-drift` | skipped/non-blocking due sandbox EPERM |

Full `cargo test --workspace` was not rerun after the review-fix commits because the user reported IDE instability from the previous full test run. Plan 06 recorded a full workspace test pass before the later code-review fixes; focused post-fix tests covered the changed areas.

## Review Gate

`05.2-REVIEW.md` found six issues. `05.2-REVIEW-FIX.md` records all six fixed:

| Finding | Status |
|---------|--------|
| CR-01 benchmark checker accepted non-acceptance reports | fixed |
| CR-02 partial default artifacts leaked debug-only provenance | fixed |
| CR-03 schema allowed removed/debug-only fields | fixed |
| WR-01 benchmark script broke on paths/metadata with spaces | fixed |
| WR-02 debug sidecar could overwrite default artifact | fixed |
| WR-03 debug event source refs used mismatched rule IDs | fixed |

## Cross-Application Boundary

No Phase 05.2 implementation moves canonical identity, PostgreSQL persistence, public APIs, replay discovery, raw object staging, moderation, Steam OAuth, public UI, or bounty payout calculation into this parser. `server-2` adaptation remains later work and Phase 6 worker integration can proceed after the 2026-05-02 benchmark acceptance update.

## Security Gate

Security enforcement is enabled, but this verification did not find a Phase 05.2 `*-SECURITY.md` artifact. This does not change the benchmark acceptance update; run `$gsd-secure-phase 05.2` if a later release process requires a dedicated security artifact.

## Gap Closure Required

No Phase 05.2 benchmark gap remains blocking after the 2026-05-02 product-owner acceptance update. Future benchmark reports should continue to record x3/x10 statuses, median/p95 ratios, max artifact bytes, oversized count, and failure parity evidence.

## Result

Phase 05.2 is accepted complete with user-approved benchmark overrides. Phase 6 can proceed.

---

_Verified: 2026-05-01T12:06:30+07:00_
_Verifier: orchestrator fallback after verifier agent did not return_
