---
phase: 05.2-minimal-artifact-and-performance-acceptance
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-SERVER-COMPATIBILITY.md
autonomous: false
requirements:
  - OUT-09
  - OUT-10
  - OUT-11
  - OUT-12
requirements_addressed: [OUT-09, OUT-10, OUT-11, OUT-12]
user_setup:
  - "Approve the compatibility review checkpoint before Plan 01 changes the public parser artifact contract if only brief-level downstream evidence exists."
must_haves:
  truths:
    - "D-22: product-owner compatibility acceptance is recorded before or during implementation: server-2 will adapt later to the minimal flat artifact."
    - "D-01: the review names the new default tables: players[], player_stats[], kills[], destroyed_vehicles[], diagnostics[]."
    - "D-05: the review records that default kill/destruction rows do not include frame, time, event index, JSON path, source refs, rule IDs, source ref dumps, normalized event snapshots, or entity snapshots."
    - "D-09: the review records that GitHub issue #13 vehicle score is removed from v1 implementation, not merely hidden."
    - "Parser still does not own canonical identity, PostgreSQL persistence, public APIs, web UI behavior, replay discovery, raw object staging, or bounty payout calculation."
  artifacts:
    - path: ".planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-SERVER-COMPATIBILITY.md"
      provides: "Minimal artifact downstream compatibility and approval gate"
      contains: "approval_status"
  key_links:
    - from: ".planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-CONTEXT.md"
      to: ".planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-SERVER-COMPATIBILITY.md"
      via: "D-22"
      pattern: "server-2 will adapt later"
---

<objective>
Record and approve the cross-application compatibility decision required before Phase 5.2 replaces the Phase 5.1 compact artifact with the minimal flat v1 artifact.

Purpose: prevent a breaking parser contract change from being implemented without an explicit `server-2` adaptation note and preserved Solid Stats ownership boundaries.
Output: `05.2-SERVER-COMPATIBILITY.md` and a human approval checkpoint when local downstream evidence is brief-level only.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-CONTEXT.md
@.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-RESEARCH.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-SERVER-COMPATIBILITY.md
@gsd-briefs/server-2.md
@gsd-briefs/replay-parser-2.md
@gsd-briefs/replays-fetcher.md
@gsd-briefs/web.md
@AGENTS.md
</context>

<decision_coverage>
- D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08: minimal default artifact shape and row policy.
- D-09, D-10: issue #13 removal while preserving ordinary vehicle context.
- D-11, D-12, D-13, D-14: explicit internal debug sidecar boundary.
- D-22: product-owner compatibility acceptance.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Write minimal artifact compatibility review</name>
  <files>.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-SERVER-COMPATIBILITY.md</files>
  <read_first>
    - `.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-CONTEXT.md`
    - `.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-SERVER-COMPATIBILITY.md`
    - `gsd-briefs/server-2.md`
    - `gsd-briefs/replay-parser-2.md`
    - `gsd-briefs/replays-fetcher.md`
    - `gsd-briefs/web.md`
    - `AGENTS.md`
  </read_first>
  <action>
    Create `05.2-SERVER-COMPATIBILITY.md` with these exact sections:

    - `## Review Scope`
    - `## Evidence Read`
    - `## Breaking Artifact Changes`
    - `## server-2 Adaptation Expectation`
    - `## Default Artifact Policy`
    - `## Debug Sidecar Policy`
    - `## Boundaries Preserved`
    - `## Compatibility Risks`
    - `## Approval`

    In `## Breaking Artifact Changes`, list these removed Phase 5.1 default fields:
    - `participants`
    - `facts`
    - `summaries`
    - `facts.combat`
    - `facts.aggregate_contributions`
    - `summaries.projections`
    - `vehicle_score.inputs`
    - `vehicle_score.denominator_inputs`

    In the same section, list these new default fields:
    - `players`
    - `player_stats`
    - `kills`
    - `destroyed_vehicles`
    - `diagnostics`

    In `## Default Artifact Policy`, state these exact constraints:
    - default JSON is minified unless `--pretty` is supplied;
    - default `kills[]` and `destroyed_vehicles[]` contain no `frame`, `time`, `event_index`, `event_id`, `json_path`, `source_refs`, `rule_id`, normalized event snapshot, or entity snapshot;
    - default v1 contains no GitHub issue #13 `vehicle_score` output or scoring weights;
    - ordinary `vehicleKills`, `killsFromVehicle`, weapon, attacker vehicle, and destroyed vehicle/static facts remain.

    In `## server-2 Adaptation Expectation`, state that `server-2` will adapt later to the minimal flat artifact and is not implemented in this repository during Phase 5.2.

    In `## Boundaries Preserved`, explicitly state:
    - parser preserves observed identity only; canonical identity belongs to `server-2`;
    - parser does not write PostgreSQL business tables;
    - parser does not own public APIs, web UI behavior, replay discovery, raw object staging, moderation, Steam OAuth, or bounty payout calculation;
    - `replays-fetcher` continues to own production replay discovery and S3 raw object writes.

    Set frontmatter `approval_status: needs_user_acceptance` if only local briefs are available as downstream evidence. Set `approval_status: accepted_from_local_evidence` only if actual adjacent implementation docs prove compatibility.
  </action>
  <verify>
    <automated>rg -n "approval_status|players|player_stats|kills|destroyed_vehicles|server-2 will adapt|canonical identity|PostgreSQL|public APIs|web UI|replay discovery|bounty payout|vehicle_score" .planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-SERVER-COMPATIBILITY.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `05.2-SERVER-COMPATIBILITY.md` contains `approval_status`.
    - `05.2-SERVER-COMPATIBILITY.md` contains `server-2 will adapt later to the minimal flat artifact`.
    - `05.2-SERVER-COMPATIBILITY.md` contains `players`, `player_stats`, `kills`, and `destroyed_vehicles`.
    - `05.2-SERVER-COMPATIBILITY.md` contains `default v1 contains no GitHub issue #13 vehicle_score output`.
    - `05.2-SERVER-COMPATIBILITY.md` contains `canonical identity belongs to server-2`.
  </acceptance_criteria>
  <done>Minimal artifact compatibility review exists and names the approval state.</done>
</task>

<task type="checkpoint:decision">
  <name>Task 2: Approve minimal contract implementation gate if local evidence is insufficient</name>
  <done>
    If `05.2-SERVER-COMPATIBILITY.md` has `approval_status: needs_user_acceptance`, stop before Plan 01 and ask the user to approve proceeding with the minimal artifact implementation based on brief-level downstream evidence. After approval, update the compatibility artifact to `approval_status: accepted_by_user` and record the checkpoint result in `05.2-00-SUMMARY.md`. If it has `approval_status: accepted_from_local_evidence`, execution may continue without a manual pause.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parser contract to `server-2` | Breaking artifact fields cross into backend ingestion expectations. |
| Planning approval to implementation | Missing approval could let executors change public parser output blindly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-00-01 | Tampering | Parser artifact contract | mitigate | Require `05.2-SERVER-COMPATIBILITY.md` and approval status before Plan 01 modifies contract code. |
| T-05.2-00-02 | Repudiation | Product-owner acceptance | mitigate | Record `approval_status`, evidence read, and checkpoint result in compatibility artifact or summary. |
</threat_model>

<verification>
- `05.2-SERVER-COMPATIBILITY.md` exists and contains the required sections.
- If `approval_status` is `needs_user_acceptance`, execution pauses before Plan 01.
</verification>

<success_criteria>
- Compatibility review states removed fields, added fields, default/debug policy, preserved boundaries, risks, and approval status.
- No Phase 5.2 contract implementation starts before the review gate has a recorded status.
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-00-SUMMARY.md`.
</output>
