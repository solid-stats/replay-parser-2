---
phase: 05.2-minimal-artifact-and-performance-acceptance
plan: 01
type: execute
wave: 2
depends_on:
  - 05.2-00
files_modified:
  - crates/parser-contract/src/artifact.rs
  - crates/parser-contract/src/minimal.rs
  - crates/parser-contract/src/compact.rs
  - crates/parser-contract/src/aggregates.rs
  - crates/parser-contract/src/events.rs
  - crates/parser-contract/src/schema.rs
  - crates/parser-contract/src/version.rs
  - crates/parser-contract/src/lib.rs
  - crates/parser-contract/examples/parse_artifact_success.v3.json
  - crates/parser-contract/examples/parse_failure.v3.json
  - schemas/parse-artifact-v3.schema.json
  - crates/parser-contract/tests/artifact_envelope.rs
  - crates/parser-contract/tests/aggregate_contract.rs
  - crates/parser-contract/tests/combat_event_contract.rs
  - crates/parser-contract/tests/schema_contract.rs
  - crates/parser-contract/tests/source_ref_contract.rs
  - crates/parser-contract/tests/version_contract.rs
autonomous: true
requirements:
  - OUT-09
  - OUT-10
  - OUT-11
  - OUT-12
  - AGG-12
requirements_addressed: [OUT-09, OUT-10, OUT-11, OUT-12, AGG-12]
must_haves:
  truths:
    - "D-01: default v1 success artifact uses flat tables: players[], player_stats[], kills[], destroyed_vehicles[], diagnostics[]."
    - "D-02: players[] includes observed player participants only, including connected/backfilled and legacy-eligible players."
    - "D-03: player_stats[] contains counters only and does not include kdRatio, score, killsFromVehicleCoef, or final bounty/rotation values."
    - "D-04: kills[] includes enemy kills, teamkills, suicides, null-killer deaths, and unknown player deaths."
    - "D-05: default rows do not include frame, time, event index, JSON path, source refs, rule IDs, normalized event snapshots, or entity snapshots."
    - "D-06 and D-07: default rows preserve compact identity and gameplay context for current stats and bounty inputs, including ordinary vehicle context."
    - "D-08: default artifact JSON can be minified by CLI; pretty output is explicit."
    - "D-09 and D-10: issue #13 vehicle score contract and vehicle_score output are removed while ordinary vehicleKills and killsFromVehicle remain."
  artifacts:
    - path: "schemas/parse-artifact-v3.schema.json"
      provides: "Minimal artifact contract schema"
      contains: "destroyed_vehicles"
    - path: "crates/parser-contract/examples/parse_artifact_success.v3.json"
      provides: "Minimal success example"
      contains: "player_stats"
---

<objective>
Replace the Phase 5.1 compact public contract with a Phase 5.2 v3 minimal flat artifact contract and remove issue #13 vehicle-score contract surfaces from v1.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-CONTEXT.md
@.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-RESEARCH.md
@.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-PATTERNS.md
@crates/parser-contract/src/artifact.rs
@crates/parser-contract/src/compact.rs
@crates/parser-contract/src/aggregates.rs
@crates/parser-contract/src/events.rs
@crates/parser-contract/src/schema.rs
@crates/parser-contract/src/version.rs
@crates/parser-contract/tests/schema_contract.rs
@AGENTS.md
</context>

<decision_coverage>
- D-01 through D-10.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Define v3 minimal table contract and artifact envelope</name>
  <files>crates/parser-contract/src/artifact.rs, crates/parser-contract/src/minimal.rs, crates/parser-contract/src/lib.rs, crates/parser-contract/src/version.rs, crates/parser-contract/tests/artifact_envelope.rs, crates/parser-contract/tests/version_contract.rs</files>
  <read_first>
    - `crates/parser-contract/src/artifact.rs`
    - `crates/parser-contract/src/compact.rs`
    - `crates/parser-contract/src/identity.rs`
    - `crates/parser-contract/src/events.rs`
    - `crates/parser-contract/src/version.rs`
    - `crates/parser-contract/tests/artifact_envelope.rs`
    - `crates/parser-contract/tests/version_contract.rs`
  </read_first>
  <action>
    Add `crates/parser-contract/src/minimal.rs` and export it from `lib.rs`.

    Define these public Serde/JsonSchema types with deterministic field order:

    - `MinimalPlayerRow` with fields:
      `player_id: String`, `source_entity_id: i64`, `observed_name: Option<String>`,
      `side: Option<EntitySide>`, `group: Option<String>`, `role: Option<String>`,
      `steam_id: Option<String>`, `compatibility_key: String`.
    - `MinimalPlayerStatsRow` with fields:
      `player_id: String`, `source_entity_id: i64`, `kills: u64`, `deaths: u64`,
      `teamkills: u64`, `suicides: u64`, `null_killer_deaths: u64`,
      `unknown_deaths: u64`, `#[serde(rename = "vehicleKills")] vehicle_kills: u64`,
      `#[serde(rename = "killsFromVehicle")] kills_from_vehicle: u64`.
    - `KillClassification` enum serialized as snake_case with variants:
      `EnemyKill`, `Teamkill`, `Suicide`, `NullKiller`, `Unknown`.
    - `DestroyedVehicleClassification` enum serialized as snake_case with variants:
      `Enemy`, `Friendly`, `UnknownSide`.
    - `MinimalKillRow` with fields:
      `killer_player_id: Option<String>`, `killer_source_entity_id: Option<i64>`,
      `killer_name: Option<String>`, `killer_side: Option<EntitySide>`,
      `victim_player_id: Option<String>`, `victim_source_entity_id: Option<i64>`,
      `victim_name: Option<String>`, `victim_side: Option<EntitySide>`,
      `classification: KillClassification`, `weapon: Option<String>`,
      `attacker_vehicle_entity_id: Option<i64>`, `attacker_vehicle_name: Option<String>`,
      `attacker_vehicle_class: Option<String>`, `bounty_eligible: bool`,
      `bounty_exclusion_reasons: Vec<String>`.
    - `MinimalDestroyedVehicleRow` with fields:
      `attacker_player_id: Option<String>`, `attacker_source_entity_id: Option<i64>`,
      `attacker_name: Option<String>`, `attacker_side: Option<EntitySide>`,
      `classification: DestroyedVehicleClassification`, `weapon: Option<String>`,
      `attacker_vehicle_entity_id: Option<i64>`, `attacker_vehicle_name: Option<String>`,
      `attacker_vehicle_class: Option<String>`, `destroyed_entity_id: Option<i64>`,
      `destroyed_entity_type: Option<String>`, `destroyed_name: Option<String>`,
      `destroyed_class: Option<String>`, `destroyed_side: Option<EntitySide>`.

    Update `ParseArtifact` to replace `participants`, `facts`, and `summaries` with:
    `players: Vec<MinimalPlayerRow>`, `player_stats: Vec<MinimalPlayerStatsRow>`,
    `kills: Vec<MinimalKillRow>`, and `destroyed_vehicles: Vec<MinimalDestroyedVehicleRow>`.
    Keep `contract_version`, `parser`, `source`, `status`, `produced_at`, `diagnostics`,
    `replay`, `side_facts`, `failure`, and `extensions`.

    Bump `CURRENT_CONTRACT_VERSION` and `ContractVersion::current()` to `3.0.0`.
  </action>
  <verify>
    <automated>cargo test -p parser-contract artifact_envelope version_contract && rg -n "MinimalPlayerRow|MinimalPlayerStatsRow|MinimalKillRow|MinimalDestroyedVehicleRow|players|player_stats|destroyed_vehicles|3\\.0\\.0" crates/parser-contract/src crates/parser-contract/tests && ! rg -n "pub participants|pub facts|pub summaries" crates/parser-contract/src/artifact.rs && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `crates/parser-contract/src/minimal.rs` contains `pub struct MinimalPlayerRow`.
    - `crates/parser-contract/src/minimal.rs` contains `#[serde(rename = "vehicleKills")]`.
    - `crates/parser-contract/src/minimal.rs` contains `#[serde(rename = "killsFromVehicle")]`.
    - `crates/parser-contract/src/artifact.rs` contains `pub players: Vec<MinimalPlayerRow>`.
    - `crates/parser-contract/src/artifact.rs` contains `pub destroyed_vehicles: Vec<MinimalDestroyedVehicleRow>`.
    - `crates/parser-contract/src/version.rs` contains `CURRENT_CONTRACT_VERSION: &str = "3.0.0"`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Remove issue #13 vehicle-score contract surfaces</name>
  <files>crates/parser-contract/src/aggregates.rs, crates/parser-contract/src/events.rs, crates/parser-contract/src/schema.rs, crates/parser-contract/tests/aggregate_contract.rs, crates/parser-contract/tests/source_ref_contract.rs, crates/parser-contract/tests/combat_event_contract.rs, crates/parser-contract/tests/schema_contract.rs</files>
  <read_first>
    - `crates/parser-contract/src/aggregates.rs`
    - `crates/parser-contract/src/events.rs`
    - `crates/parser-contract/src/schema.rs`
    - `crates/parser-contract/tests/aggregate_contract.rs`
    - `crates/parser-contract/tests/schema_contract.rs`
  </read_first>
  <action>
    Remove the active v1 issue #13 contract vocabulary:

    - Delete `AggregateContributionKind::VehicleScoreInput`.
    - Delete `VehicleScoreSign`.
    - Delete `VehicleScoreInputValue`.
    - Delete `VehicleScoreCategory`.
    - Remove `attacker_vehicle_category` and `target_category` from `VehicleContext`.
    - Remove `VehicleScoreInputValue` helper definition and schema condition from `schema.rs`.
    - Remove `vehicle_score.inputs` and `vehicle_score.denominator_inputs` from `AggregateProjectionKey`.
    - Delete or rewrite contract tests that assert vehicle-score inputs. Preserve tests for ordinary vehicle context fields: `raw_weapon`, `attacker_vehicle_entity_id`, `attacker_vehicle_name`, and `attacker_vehicle_class`.
  </action>
  <verify>
    <automated>cargo test -p parser-contract aggregate_contract source_ref_contract combat_event_contract schema_contract && ! rg -n "VehicleScore|vehicle_score|vehicle-score|VehicleScoreInput|VehicleScoreSign" crates/parser-contract/src crates/parser-contract/tests && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "VehicleScore|vehicle_score|vehicle-score|VehicleScoreInput|VehicleScoreSign" crates/parser-contract/src crates/parser-contract/tests` exits non-zero.
    - `crates/parser-contract/src/events.rs` still contains `attacker_vehicle_entity_id`.
    - `crates/parser-contract/src/events.rs` still contains `attacker_vehicle_name`.
    - `crates/parser-contract/src/events.rs` still contains `attacker_vehicle_class`.
    - `crates/parser-contract/src/schema.rs` does not contain `vehicle_score.inputs`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Regenerate v3 schema and minimal examples</name>
  <files>crates/parser-contract/examples/parse_artifact_success.v3.json, crates/parser-contract/examples/parse_failure.v3.json, schemas/parse-artifact-v3.schema.json, crates/parser-contract/tests/schema_contract.rs</files>
  <read_first>
    - `crates/parser-contract/examples/parse_artifact_success.v2.json`
    - `crates/parser-contract/examples/parse_failure.v2.json`
    - `schemas/parse-artifact-v2.schema.json`
    - `crates/parser-contract/tests/schema_contract.rs`
    - `crates/parser-contract/src/schema.rs`
  </read_first>
  <action>
    Create v3 example files and schema:

    - `parse_artifact_success.v3.json` must include `contract_version: "3.0.0"`, `players`, `player_stats`, `kills`, `destroyed_vehicles`, and `diagnostics`.
    - `parse_artifact_success.v3.json` must not include `participants`, `facts`, `summaries`, `source_refs`, `rule_id`, `frame`, `event_index`, `json_path`, or `vehicle_score`.
    - `parse_failure.v3.json` must include the v3 envelope and empty arrays for `players`, `player_stats`, `kills`, and `destroyed_vehicles`.
    - Replace schema tests so committed schema path is `schemas/parse-artifact-v3.schema.json`, success example path is `parse_artifact_success.v3.json`, and failure example path is `parse_failure.v3.json`.
    - Export the fresh schema with `cargo run -p parser-contract --example export_schema > schemas/parse-artifact-v3.schema.json`.
  </action>
  <verify>
    <automated>cargo run -p parser-contract --example export_schema > /tmp/phase052-schema.json && cmp /tmp/phase052-schema.json schemas/parse-artifact-v3.schema.json && cargo test -p parser-contract schema_contract && rg -n "parse-artifact-v3|parse_artifact_success\\.v3|parse_failure\\.v3|players|player_stats|destroyed_vehicles" crates/parser-contract/tests crates/parser-contract/examples schemas/parse-artifact-v3.schema.json && ! rg -n "participants|facts|summaries|vehicle_score|VehicleScore" crates/parser-contract/examples/parse_artifact_success.v3.json schemas/parse-artifact-v3.schema.json && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `schemas/parse-artifact-v3.schema.json` exists.
    - `crates/parser-contract/examples/parse_artifact_success.v3.json` contains `"players"`.
    - `crates/parser-contract/examples/parse_artifact_success.v3.json` contains `"player_stats"`.
    - `crates/parser-contract/examples/parse_artifact_success.v3.json` contains `"destroyed_vehicles"`.
    - `crates/parser-contract/examples/parse_artifact_success.v3.json` does not contain `vehicle_score`.
    - `cargo test -p parser-contract schema_contract` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parser contract to worker/server | Public JSON schema changes what future worker artifacts will deliver. |
| Debug/audit evidence to default artifact | Audit-only fields must not leak into ordinary ingestion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-01-01 | Tampering | Public artifact schema | mitigate | Schema/example tests assert exact v3 fields and absence of removed fields. |
| T-05.2-01-02 | Information Disclosure | Default artifact rows | mitigate | Minimal row types omit source refs, rule IDs, frames, event indexes, JSON paths, and snapshots. |
| T-05.2-01-03 | Repudiation | Vehicle score removal | mitigate | Remove active issue #13 types/tests/schema, and keep ordinary vehicle context tests. |
</threat_model>

<verification>
- `cargo test -p parser-contract artifact_envelope aggregate_contract combat_event_contract source_ref_contract version_contract schema_contract`
- `cargo run -p parser-contract --example export_schema > /tmp/phase052-schema.json && cmp /tmp/phase052-schema.json schemas/parse-artifact-v3.schema.json`
- `! rg -n "VehicleScore|vehicle_score|vehicle-score|VehicleScoreInput|VehicleScoreSign" crates/parser-contract/src crates/parser-contract/tests`
- `git diff --check`
</verification>

<success_criteria>
- Public contract version is `3.0.0`.
- Default artifact contract exposes minimal flat tables and no Phase 5.1 compact `participants`/`facts`/`summaries`.
- Issue #13 vehicle-score contract vocabulary is absent from v1 contract source, schema, examples, and contract tests.
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-01-SUMMARY.md`.
</output>
