---
phase: 05.2-minimal-artifact-and-performance-acceptance
plan: 03
subsystem: parser-cli
tags: [rust, cli, schema, minimal-artifact, debug-artifact, documentation]
requires:
  - phase: 05.2-01
    provides: Phase 5.2 v3 minimal flat artifact contract
  - phase: 05.2-02
    provides: Parser-core minimal artifact rows and debug sidecar API
provides:
  - CLI parse minified v3 minimal JSON by default
  - Explicit CLI pretty formatting with `--pretty`
  - Explicit internal debug sidecar output with `--debug-artifact`
  - v3 schema command tests and README command documentation
affects: [parser-cli, parser-contract, parser-core, server-2, Phase 05.2]
tech-stack:
  added: []
  patterns:
    - default CLI output strips debug-only replay metadata provenance before serialization
    - debug sidecar is opt-in and uses parser-core `parse_replay_debug`
    - schema command freshness compares against committed v3 schema
key-files:
  created:
    - .planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-03-SUMMARY.md
  modified:
    - crates/parser-cli/src/main.rs
    - crates/parser-cli/tests/parse_command.rs
    - crates/parser-cli/tests/schema_command.rs
    - schemas/parse-artifact-v3.schema.json
    - README.md
key-decisions:
  - "CLI parse now writes minified v3 minimal JSON by default; human-readable JSON requires `--pretty`."
  - "CLI debug sidecar output is explicit internal tooling through `--debug-artifact <path>` and uses parser-core `parse_replay_debug` only when requested."
  - "Public schema and README references now use `schemas/parse-artifact-v3.schema.json` and v3 example artifacts."
patterns-established:
  - "Command-level tests assert default parse output is one-line minimal JSON and lacks debug-only provenance keys."
  - "Command-level tests assert debug sidecar files contain source refs, rule IDs, frames, and event indexes while ordinary artifacts do not."
requirements-completed: [OUT-09, OUT-10, OUT-11, OUT-12, AGG-12]
duration: 9m22s
completed: 2026-05-01
---

# Phase 05.2 Plan 03: CLI Minimal Artifact Summary

**CLI parse now emits minified v3 minimal artifacts by default, with explicit pretty formatting, opt-in debug sidecars, and v3 public schema/docs**

## Performance

- **Duration:** 9m22s
- **Started:** 2026-05-01T02:52:06Z
- **Completed:** 2026-05-01T03:01:28Z
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments

- Changed `replay-parser-2 parse` default output to minified v3 minimal JSON with exactly one trailing newline.
- Added `--pretty` for explicit human-readable minimal JSON.
- Added `--debug-artifact <path>` for deterministic full normalized debug output while keeping ordinary artifacts minimal.
- Updated schema command tests and README command/status sections to v3 schema/example paths and no issue #13 vehicle score output.
- Refreshed the committed v3 schema so CLI schema output and parser-contract schema generation match exactly.

## Task Commits

Each task was committed atomically:

1. **Task 1: Make parse output minified by default with explicit pretty mode** - `3e9d3af` (feat)
2. **Task 2: Add explicit debug sidecar CLI output** - `e15aac5` (feat)
3. **Task 3: Update schema command tests and README for v3 minimal output** - `613f563` (docs)

## Files Created/Modified

- `crates/parser-cli/src/main.rs` - Adds `--pretty`, `--debug-artifact`, public minimal serialization, and opt-in debug sidecar writing.
- `crates/parser-cli/tests/parse_command.rs` - Covers minified default output, explicit pretty output, default minimal fields, debug sidecar output, and no default debug file.
- `crates/parser-cli/tests/schema_command.rs` - Compares schema output against `schemas/parse-artifact-v3.schema.json` and checks minimal row symbols.
- `schemas/parse-artifact-v3.schema.json` - Refreshed to match the current generated v3 schema.
- `README.md` - Documents v3 schema/examples, minified minimal parse output, `--pretty`, `--debug-artifact`, and no issue #13 vehicle score output.

## Decisions Made

- Kept `parse_replay_debug` behind the CLI debug flag rather than building debug detail on the normal parse path.
- Sanitized replay metadata provenance in CLI public output so ordinary artifacts do not expose debug-only `rule_id`, `json_path`, `frame`, or `event_index` keys.
- Treated the v3 schema command freshness failure as a blocking stale-generated-artifact issue and refreshed the committed schema.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Removed replay metadata provenance from default CLI JSON**
- **Found during:** Task 1 (Make parse output minified by default with explicit pretty mode)
- **Issue:** The new default-output test exposed `rule_id`, `json_path`, `frame`, and `event_index` through replay metadata `FieldPresence` source references, despite the Phase 5.2 default artifact invariant that debug provenance stays out of ordinary output.
- **Fix:** Added CLI public-output sanitization for replay metadata sources before serialization while leaving parser-core debug output intact.
- **Files modified:** `crates/parser-cli/src/main.rs`, `crates/parser-cli/tests/parse_command.rs`
- **Verification:** `cargo test -p parser-cli parse_command`, grep acceptance checks, and `git diff --check` passed.
- **Committed in:** `3e9d3af`

**2. [Rule 3 - Blocking] Refreshed stale committed v3 schema**
- **Found during:** Task 3 (Update schema command tests and README for v3 minimal output)
- **Issue:** After schema-command tests switched from v2 to v3, exact freshness comparison failed because `schemas/parse-artifact-v3.schema.json` still contained an obsolete generated default object for `side_facts`.
- **Fix:** Updated the committed v3 schema to match current `parse_artifact_schema()` output.
- **Files modified:** `schemas/parse-artifact-v3.schema.json`
- **Verification:** `cargo test -p parser-cli schema_command` and `cargo test -p parser-contract schema_contract` passed.
- **Committed in:** `613f563`

---

**Total deviations:** 2 auto-fixed issues (1 missing critical, 1 blocking).
**Impact on plan:** Both fixes were required to enforce minimal default output and keep v3 schema command freshness truthful. No adjacent application ownership changed.

## Issues Encountered

- The plan-level command `cargo test -p parser-cli parse_command schema_command` uses two positional Cargo test filters and Cargo rejects it. Equivalent checks were run separately: `cargo test -p parser-cli parse_command` and `cargo test -p parser-cli schema_command`.

## Verification

- `cargo test -p parser-cli parse_command` passed.
- `cargo test -p parser-cli schema_command` passed.
- `cargo test -p parser-contract schema_contract` passed.
- `rg -n "parse-artifact-v3|--debug-artifact|--pretty|minified minimal" README.md crates/parser-cli/src/main.rs crates/parser-cli/tests` found the expected v3 command/doc references.
- `rg -n "parse-artifact-v2|parse_artifact_success\\.v2|vehicle_score" crates/parser-cli/tests/schema_command.rs README.md` returned no matches.
- `git diff --check` passed.
- `cargo fmt --all -- --check` passed.

## Known Stubs

None.

## Authentication Gates

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Ready for Phase 05.2 Plan 04 derived legacy comparison work. CLI parse now exposes the minimal v3 artifact by default, while full normalized evidence is available only through the explicit debug sidecar.

## Self-Check: PASSED

- Found `.planning/phases/05.2-minimal-artifact-and-performance-acceptance/05.2-03-SUMMARY.md`.
- Found `crates/parser-cli/src/main.rs`.
- Found `schemas/parse-artifact-v3.schema.json`.
- Verified task commits exist: `3e9d3af`, `e15aac5`, `613f563`.
- Re-ran plan-level verification with equivalent split Cargo commands successfully.

---
*Phase: 05.2-minimal-artifact-and-performance-acceptance*
*Completed: 2026-05-01*
