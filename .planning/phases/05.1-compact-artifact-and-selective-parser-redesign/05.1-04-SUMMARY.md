---
phase: 05.1-compact-artifact-and-selective-parser-redesign
plan: 04
subsystem: parser-cli
tags: [parser-cli, compact-artifact, schema, golden-fixtures, README]
requires:
  - phase: 05.1-03
    provides: Compact parser-core participants, facts, summaries, side facts, and determinism
provides:
  - CLI parse tests locked to compact top-level artifact fields
  - CLI schema tests locked to compact contract symbols
  - Golden manifest traceability for OUT-09, OUT-10, PARS-12, and TEST-13
  - README command/status handoff for compact parser output
requirements-completed: [OUT-09, OUT-10, PARS-12, TEST-13]
completed: 2026-04-29
---

# Phase 05.1 Plan 04: Compact CLI and Docs Summary

## Accomplishments

- Confirmed `replay-parser-2 parse` writes the compact `ParseArtifact` exposed by parser-core and tightened CLI tests around `participants`, `facts`, `summaries`, `side_facts`, and `failure`.
- Added CLI failure-path assertions proving malformed input still writes a structured failed compact artifact and exits non-zero.
- Added CLI schema command assertions for `ObservedParticipantRef`, `CombatFact`, `ParseFactSection`, `ParseSummarySection`, `participants`, `facts`, and `summaries`.
- Updated the golden fixture manifest so compact default expectations trace to `OUT-09`, `OUT-10`, `PARS-12`, and `TEST-13`.
- Updated README command and status text to describe compact server-facing parse artifacts and compact parser contract schema while preserving parser ownership boundaries.

## Files Modified

- `crates/parser-cli/src/main.rs` - CLI help text now names compact parse artifacts and compact schema export.
- `crates/parser-cli/tests/parse_command.rs` - Parse command success/failure tests assert compact root fields and removed full-dump field absence.
- `crates/parser-cli/tests/schema_command.rs` - Schema command tests assert compact contract symbols.
- `crates/parser-core/tests/fixtures/golden/manifest.json` - Compact output requirements and expected fields are documented in golden fixture traceability.
- `README.md` - Current status and user commands describe compact server-facing artifact/schema behavior and out-of-scope boundaries.

## Verification

- `cargo fmt --all`
- `cargo test -p parser-cli --test parse_command --test schema_command`
- `cargo test -p parser-contract schema_contract`
- `cargo test -p parser-core golden_fixture_manifest`
- `rg -n "participants|facts|summaries|ObservedParticipantRef|ParseFactSection" crates/parser-cli/tests crates/parser-cli/src/main.rs`
- `rg -n "\"entities\"|\"events\"|\"aggregates\"" crates/parser-cli/tests/parse_command.rs` returned no matches.
- `rg -n "OUT-09|OUT-10|PARS-12|TEST-13|participants|facts|summaries|entities|events" crates/parser-core/tests/fixtures/golden/manifest.json README.md`
- `rg -n "compact server-facing artifact|AI agents using the GSD workflow|not implemented" README.md`
- `git diff --check`

## Deviations

- The requested `cargo test -p parser-cli parse_command schema_command` form is not accepted by Cargo because it treats the second bare argument as unexpected. The equivalent integration-test target command was run instead.

## Remaining Risks

- Artifact-size and whole-list/corpus benchmark acceptance remain for later Phase 5.1 plans.
- Parser-core still maps some internal normalized semantics into compact public sections; later benchmark work must prove this is sufficient for the Phase 5.1 performance target.

---
*Phase: 05.1-compact-artifact-and-selective-parser-redesign*
*Completed: 2026-04-29*
