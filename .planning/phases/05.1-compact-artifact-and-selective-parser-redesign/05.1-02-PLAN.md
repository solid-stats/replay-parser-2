---
phase: 05.1-compact-artifact-and-selective-parser-redesign
plan: 02
type: execute
wave: 3
depends_on:
  - 05.1-01
files_modified:
  - crates/parser-core/Cargo.toml
  - crates/parser-core/src/artifact.rs
  - crates/parser-core/src/raw.rs
  - crates/parser-core/src/raw_compact.rs
  - crates/parser-core/src/lib.rs
  - crates/parser-core/tests/parser_core_api.rs
  - crates/parser-core/tests/schema_drift_status.rs
autonomous: true
requirements:
  - PARS-12
requirements_addressed: [PARS-12]
must_haves:
  truths:
    - "D-16: normal/default parser path is compact-first and no longer translates whole replay JSON into a full artifact."
    - "D-17: normal ingestion path does not parse a full `serde_json::Value` DOM."
    - "D-18: selective extraction preserves event index, frame, entity ID, JSON path, and rule ID coordinates."
    - "D-19: rewrite risk is mitigated by keeping existing parser-core public API and behavior tests green."
  artifacts:
    - path: "crates/parser-core/src/raw_compact.rs"
      provides: "Borrowed selective OCAP root/entity/event extraction"
      contains: "RawOcapRoot"
    - path: "crates/parser-core/src/artifact.rs"
      provides: "Compact-first parse orchestration"
      contains: "parse_replay"
  key_links:
    - from: "crates/parser-core/src/raw_compact.rs"
      to: "crates/parser-core/src/artifact.rs"
      via: "selective root decode"
      pattern: "RawOcapRoot"
---

<objective>
Replace the parser normal path with selective OCAP extraction that avoids full `serde_json::Value` DOM parsing.

Purpose: rebuild the parser hot path around compact statistics extraction before worker integration.
Output: selective raw extraction module and compact-first parse orchestration behind the unchanged `parse_replay(ParserInput)` API.
</objective>

<execution_context>
@/home/alexandr/.codex/get-shit-done/workflows/execute-plan.md
@/home/alexandr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-CONTEXT.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-RESEARCH.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-PATTERNS.md
@crates/parser-core/src/artifact.rs
@crates/parser-core/src/raw.rs
@crates/parser-core/src/input.rs
@crates/parser-core/src/lib.rs
@crates/parser-core/tests/parser_core_api.rs
@crates/parser-core/tests/schema_drift_status.rs
@AGENTS.md
</context>

<decision_coverage>
- D-16, D-17, D-18, D-19, D-20: selective compact parser path and rewrite acceptance gates.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add borrowed selective raw OCAP root extraction</name>
  <files>crates/parser-core/Cargo.toml, crates/parser-core/src/raw_compact.rs, crates/parser-core/src/lib.rs, crates/parser-core/src/raw.rs, crates/parser-core/tests/schema_drift_status.rs</files>
  <read_first>
    - `crates/parser-core/Cargo.toml`
    - `crates/parser-core/src/raw.rs`
    - `crates/parser-core/tests/schema_drift_status.rs`
    - `.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-RESEARCH.md`
  </read_first>
  <behavior>
    - Valid OCAP root JSON can expose top-level metadata plus borrowed `entities` and `events` raw arrays.
    - Missing or drifted `entities`/`events` arrays produce the same style of structured diagnostics as current raw accessors.
    - Source coordinates for entity and killed/connected event observations remain stable.
  </behavior>
  <action>
    Update `crates/parser-core/Cargo.toml` to enable the `raw_value` feature:

    ```toml
    serde_json = { version = "1", features = ["raw_value"] }
    ```

    Create `crates/parser-core/src/raw_compact.rs` and export it from `lib.rs` with `pub mod raw_compact;`.

    Define:

    - `RawOcapRoot<'a>` with borrowed top-level scalar fields for `missionName`, `worldName`, `missionAuthor`, `playersCount`, `captureDelay`, `endFrame`, `EditorMarkers`, and `Markers`
    - borrowed `entities: Option<&'a serde_json::value::RawValue>`
    - borrowed `events: Option<&'a serde_json::value::RawValue>`
    - `RawEntityCompact` with only `id`, `type`, `name`, `class`, `_class`, `side`, `group`, `description`, `isPlayer`, and known SteamID/identity source fields already supported by current tests
    - `RawEventCompact` visitor or equivalent typed extractor that returns only connected events and killed events while skipping other event tuple types without preserving them

    Add functions:

    - `decode_compact_root(bytes: &[u8]) -> Result<RawOcapRoot<'_>, CompactDecodeError>`
    - `compact_entities(root: &RawOcapRoot<'_>) -> Vec<RawEntityObservation>`
    - `compact_connected_events(root: &RawOcapRoot<'_>) -> Vec<ConnectedEventObservation>`
    - `compact_killed_events(root: &RawOcapRoot<'_>) -> Vec<KilledEventObservation>`

    Reuse existing `ConnectedEventObservation`, `KilledEventObservation`, and `KilledEventKillInfo` names or move them without changing their source-coordinate fields. Preserve JSON paths like `$.entities[0]` and `$.events[7]`.
  </action>
  <verify>
    <automated>cargo test -p parser-core raw schema_drift_status && rg -n "raw_value|RawOcapRoot|decode_compact_root|compact_killed_events|compact_entities" crates/parser-core/Cargo.toml crates/parser-core/src && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml` contains `features = ["raw_value"]` for parser-core `serde_json`.
    - `raw_compact.rs` contains `RawOcapRoot`.
    - `raw_compact.rs` contains `decode_compact_root`.
    - `raw_compact.rs` contains `compact_killed_events`.
    - `raw_compact.rs` contains `$.events[` or code that constructs equivalent JSON paths.
    - `cargo test -p parser-core raw schema_drift_status` exits 0.
  </acceptance_criteria>
  <done>Selective raw extraction exists and preserves source coordinates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Switch parse_replay to compact-first decode without full DOM</name>
  <files>crates/parser-core/src/artifact.rs, crates/parser-core/src/raw.rs, crates/parser-core/src/raw_compact.rs, crates/parser-core/tests/parser_core_api.rs</files>
  <read_first>
    - `crates/parser-core/src/artifact.rs`
    - `crates/parser-core/src/raw.rs`
    - `crates/parser-core/src/raw_compact.rs`
    - `crates/parser-core/tests/parser_core_api.rs`
    - `crates/parser-contract/src/artifact.rs`
  </read_first>
  <behavior>
    - `parse_replay(ParserInput)` still returns a compact `ParseArtifact`.
    - Invalid JSON returns structured failed artifact with `json.decode`.
    - Non-object JSON returns structured failed artifact with `schema.root_object`.
    - Normal parser-core source files do not contain `serde_json::from_slice::<Value>` or `serde_json::from_str::<Value>`.
  </behavior>
  <action>
    Rewrite `crates/parser-core/src/artifact.rs` so `parse_replay` calls `decode_compact_root(input.bytes)` instead of `serde_json::from_slice::<Value>(input.bytes)`.

    Maintain these failure behaviors:

    - malformed JSON builds `FailureSpec::JsonDecode` with `ParseStage::JsonDecode`, error code `json.decode`, rule ID `failure.json.decode`
    - non-object/root-shape decode builds `FailureSpec::RootNotObject` with `ParseStage::Schema`, error code `schema.root_object`, rule ID `failure.schema.root_object`

    Replace `success_artifact` input from `&Map<String, Value>` to `&RawOcapRoot<'_>`.

    Build the compact `ParseArtifact` fields added in Plan 01 with empty `participants`, default `facts`, default `summaries`, and default `side_facts` until Plan 03 wires semantics. This task only changes the decode boundary and failure shell.

    Remove normal-path imports of `serde_json::{Map, Value}` from `artifact.rs`. Keep `serde_json::Value` only where contract summary/projection payloads require it outside parser-core normal decode.
  </action>
  <verify>
    <automated>cargo test -p parser-core parser_core_api schema_drift_status && ! rg -n "from_slice::<Value>|from_str::<Value>|serde_json::\\{Map, Value\\}" crates/parser-core/src/artifact.rs crates/parser-core/src/raw.rs crates/parser-core/src/raw_compact.rs && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `artifact.rs` contains `decode_compact_root`.
    - `artifact.rs` does not contain `from_slice::<Value>`.
    - `artifact.rs` does not contain `serde_json::{Map, Value}`.
    - `cargo test -p parser-core parser_core_api schema_drift_status` exits 0.
  </acceptance_criteria>
  <done>`parse_replay` no longer starts with a full `serde_json::Value` DOM.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Replay bytes to parser-core | Untrusted OCAP JSON enters selective parser. |
| Raw extraction to compact facts | Source coordinates must survive partial extraction. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-02-01 | Denial of Service | JSON decode | mitigate | Avoid full replay DOM and skip irrelevant heavy fields with typed/borrowed extraction. |
| T-05.1-02-02 | Tampering | Source refs | mitigate | Tests and acceptance criteria require event/entity JSON path, frame, event index, and entity ID preservation. |
</threat_model>

<verification>
- `cargo test -p parser-core raw parser_core_api schema_drift_status`
- Grep proves normal parser-core decode path does not use `serde_json::from_slice::<Value>`.
</verification>

<success_criteria>
- Selective decode compiles and handles malformed/root-shape failures.
- Existing parser-core public API remains `parse_replay(ParserInput)`.
- Normal parser hot path avoids full `serde_json::Value` DOM.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-02-SUMMARY.md`.
</output>
