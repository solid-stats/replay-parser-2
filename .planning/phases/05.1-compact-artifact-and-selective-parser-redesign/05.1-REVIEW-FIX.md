---
phase: 05.1-compact-artifact-and-selective-parser-redesign
artifact: code-review-fix-summary
created: 2026-04-29
status: fixes-applied-with-acceptance-gap
source_review: 05.1-REVIEW.md
---

# Phase 05.1 Code Review Fix Summary

## Fixed Blockers

| Finding | Status | Fix |
|---------|--------|-----|
| CR-01: Breaking compact contract still reports `1.0.0` | Fixed | Bumped `CURRENT_CONTRACT_VERSION` and `ContractVersion::current()` to `2.0.0`; renamed committed schema/examples to `parse-artifact-v2.schema.json`, `parse_artifact_success.v2.json`, and `parse_failure.v2.json`; updated tests and README references. |
| CR-02: Parse-only compact benchmark full-DOM decodes JSON | Fixed | `parse_only_compact_decode` now benchmarks `parser_core::raw_compact::decode_compact_root`; `benchmark_report` tests fail if the bench reintroduces `from_slice::<Value>`. |
| CR-03: Selected benchmark evidence mixes different workloads | Fixed | `selected_evidence` now describes only the small-CI fixture metrics and compact artifact-size evidence. Optional curated old/new comparison evidence is separated under `old_new_command` when prerequisites are available. |
| CR-04: Selected old-vs-new parity structurally unable to pass | Fixed for structural blocker | The old/new script classifies parity from legacy-equivalent surfaces instead of compact-only review surfaces. Compact participants/facts remain in comparison detail for human review, but they no longer make legacy parity structurally impossible. |

## Fixed Warnings

| Finding | Status | Fix |
|---------|--------|-----|
| WR-01: SteamID alias source refs point to canonical key | Fixed | `RawEntityCompact` records the actual matched `SteamID` alias key, and entity normalization emits the real JSON path for `steamID`, `steamId`, or `steam_id`. |
| WR-02: Markdown Top Diffs lists compatible surfaces | Fixed | `ComparisonReviewSummary` filters compatible findings out of `top_diffs`; compatible reports render `- None` under `Top Diffs`. |

## Remaining Acceptance Gap

The generated benchmark report is still not an acceptance pass:

- selected `ten_x_status`: `unknown`
- selected `parity_status`: `not_run`
- whole-list/corpus evidence: unavailable because `RUN_PHASE5_FULL_CORPUS` was not enabled
- selected artifact/raw ratio: `59.97366881` on the tiny CI fixture

This is now a recorded evidence gap rather than a known implementation bug in the benchmark surfaces. Phase 6 remains blocked until whole-list/corpus benchmark and parity evidence pass, or the user explicitly accepts the benchmark/parity gap.

## Verification

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `cargo doc --workspace --no-deps`
- `scripts/coverage-gate.sh --check`
- `scripts/fault-report-gate.sh`
- `scripts/benchmark-phase5.sh --ci`
- Boundary grep checks for full-DOM hot-path decode and removed artifact fields.
