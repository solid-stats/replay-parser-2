---
phase: 05.1
slug: compact-artifact-and-selective-parser-redesign
status: ready
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-29
---

# Phase 05.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust `cargo test`, `assert_cmd`, contract schema tests, Criterion benchmark gate |
| **Config file** | `Cargo.toml`, `.cargo/config.toml`, `scripts/coverage-gate.sh`, `scripts/fault-report-gate.sh`, `scripts/benchmark-phase5.sh` |
| **Quick run command** | `cargo test -p parser-contract && cargo test -p parser-core && cargo test -p parser-harness && cargo test -p parser-cli` |
| **Full suite command** | `cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace && cargo doc --workspace --no-deps && scripts/coverage-gate.sh --check && scripts/fault-report-gate.sh && scripts/benchmark-phase5.sh --ci` |
| **Estimated runtime** | ~300 seconds, benchmark/coverage dependent |

## Sampling Rate

- **After every task commit:** Run that task's `<automated>` command.
- **After every plan wave:** Run `cargo test -p parser-contract && cargo test -p parser-core && cargo test -p parser-harness && cargo test -p parser-cli`.
- **Before `$gsd-verify-work`:** Full suite must be green, or failures must be recorded as explicit UAT/verification gaps.
- **Max feedback latency:** 300 seconds for normal code/test waves; benchmark full-corpus evidence may record explicit unavailable rationale when local corpus or old parser cannot run.

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 05.1-00-01 | 00 | 1 | OUT-09/OUT-10 | T-05.1-00-01 | Contract change cannot proceed without recorded downstream compatibility review | docs gate | `rg -n "server-2|parse_results|canonical identity|artifact reference|approval_status" .planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-SERVER-COMPATIBILITY.md && git diff --check` | ✅ | ⬜ pending |
| 05.1-01-01 | 01 | 2 | OUT-09/OUT-10 | T-05.1-01-01 | Default artifact excludes full replay-shaped dumps while keeping status/failure/source invariants | contract unit | `cargo test -p parser-contract artifact_envelope schema_contract failure_contract && git diff --check` | ✅ | ⬜ pending |
| 05.1-01-02 | 01 | 2 | OUT-09 | T-05.1-01-02 | Schema/examples expose compact participant/fact fields and omit top-level `entities`/`events` | schema | `cargo run -p parser-contract --example export_schema > /tmp/parse-artifact-v1.schema.json && cmp /tmp/parse-artifact-v1.schema.json schemas/parse-artifact-v1.schema.json && cargo test -p parser-contract schema_contract && git diff --check` | ✅ | ⬜ pending |
| 05.1-02-01 | 02 | 3 | PARS-12 | T-05.1-02-01 | Untrusted replay JSON is selectively decoded without full DOM on normal path | unit/grep | `cargo test -p parser-core raw && rg -n "from_slice::<Value>|from_str::<Value>" crates/parser-core/src && git diff --check` | ✅ | ⬜ pending |
| 05.1-02-02 | 02 | 3 | PARS-12 | T-05.1-02-02 | Heavy OCAP arrays skip irrelevant fields and preserve source coordinates | unit | `cargo test -p parser-core metadata entities events schema_drift_status && git diff --check` | ✅ | ⬜ pending |
| 05.1-03-01 | 03 | 4 | OUT-09/PARS-12 | T-05.1-03-01 | Compact facts preserve audit evidence for combat, bounty, vehicle score, and summaries | behavior | `cargo test -p parser-core aggregate_projection vehicle_score fault_injection_regressions side_facts deterministic_output && git diff --check` | ✅ | ⬜ pending |
| 05.1-03-02 | 03 | 4 | OUT-10/PARS-12 | T-05.1-03-02 | Normal output contains no full entity/event arrays and preserves deterministic order | behavior/grep | `cargo test -p parser-core deterministic_output parser_core_api && rg -n "\"entities\"|\"events\"" crates/parser-core/tests crates/parser-contract/examples && git diff --check` | ✅ | ⬜ pending |
| 05.1-04-01 | 04 | 5 | OUT-09/OUT-10 | T-05.1-04-01 | CLI parse/schema emit compact contract consistently | CLI/contract | `cargo test -p parser-cli parse_command schema_command && cargo test -p parser-contract schema_contract && git diff --check` | ✅ | ⬜ pending |
| 05.1-04-02 | 04 | 5 | OUT-09/TEST-13 | T-05.1-04-02 | Golden fixtures and README reflect compact artifact workflow | integration/docs | `cargo test -p parser-core && rg -n "compact|artifact-size|Phase 5.1|replay-parser-2 parse" README.md && git diff --check` | ✅ | ⬜ pending |
| 05.1-05-01 | 05 | 5 | TEST-14 | T-05.1-05-01 | Comparison default output is human-reviewable Markdown | harness/CLI | `cargo test -p parser-harness comparison_report && cargo test -p parser-cli compare_command && git diff --check` | ✅ | ⬜ pending |
| 05.1-06-01 | 06 | 6 | TEST-06/TEST-13/PARS-12 | T-05.1-06-01 | Benchmark report cannot pass without compact size and selected plus whole-list/corpus evidence | harness/script | `cargo test -p parser-harness benchmark_report && scripts/benchmark-phase5.sh --ci && git diff --check` | ✅ | ⬜ pending |
| 05.1-07-01 | 07 | 7 | TEST-06/TEST-13/TEST-14 | T-05.1-07-01 | Final release gates catch coverage, fault, benchmark, schema, and boundary regressions | full suite | `cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace && cargo doc --workspace --no-deps && scripts/coverage-gate.sh --check && scripts/fault-report-gate.sh && scripts/benchmark-phase5.sh --ci && git diff --check` | ✅ | ⬜ pending |

## Wave 0 Requirements

Existing infrastructure covers all phase requirements.

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| User acceptance of contract implementation when actual `server-2` schema evidence is missing | OUT-09, OUT-10, INT-02 | The repo only has adjacent-app briefs; changing artifact shape affects future ingestion and may require owner acceptance before code changes | Review `05.1-SERVER-COMPATIBILITY.md`; approve or request changes at Plan 00 checkpoint if `approval_status` is not `accepted_from_local_evidence`. |

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 300s for normal task gates
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending execution
