---
phase: 05.1-compact-artifact-and-selective-parser-redesign
plan: 06
type: execute
wave: 6
depends_on:
  - 05.1-04
  - 05.1-05
files_modified:
  - crates/parser-harness/src/benchmark_report.rs
  - crates/parser-harness/tests/benchmark_report.rs
  - crates/parser-harness/benches/parser_pipeline.rs
  - crates/parser-harness/src/bin/benchmark-report-check.rs
  - scripts/benchmark-phase5.sh
  - README.md
autonomous: true
requirements:
  - PARS-12
  - TEST-06
  - TEST-13
requirements_addressed: [PARS-12, TEST-06, TEST-13]
must_haves:
  truths:
    - "D-21: benchmark evidence includes selected replay/sample checks and whole-list/corpus evidence or explicit unavailable rationale."
    - "D-22: 10x remains blocking unless bottleneck/parity triage and explicit user acceptance are recorded."
    - "D-23: report includes raw input bytes, compact artifact bytes, artifact/raw ratio, selected/full workload identity, stage throughput, memory/RSS note, parity status, and 10x status."
    - "D-10: compactness threshold is evidence-derived through artifact/raw ratio and benchmark report validation."
    - "D-20: benchmark measures the implemented compact path, not the removed full artifact path."
  artifacts:
    - path: "crates/parser-harness/src/benchmark_report.rs"
      provides: "Compact artifact benchmark report schema"
      contains: "artifact_raw_ratio"
    - path: "scripts/benchmark-phase5.sh"
      provides: "Selected and whole-list/corpus benchmark gate"
      contains: "whole_list"
  key_links:
    - from: "scripts/benchmark-phase5.sh"
      to: "crates/parser-harness/src/benchmark_report.rs"
      via: "JSON report validation"
      pattern: "compact_artifact_bytes"
---

<objective>
Upgrade benchmark reporting to prove compact artifact size and selected plus whole-list/corpus performance evidence.

Purpose: make the 10x claim truthful for the redesigned parser path and prevent one-replay evidence from passing Phase 5.1.
Output: benchmark report schema, validator tests, benchmark script updates, and README benchmark guidance.
</objective>

<execution_context>
@/home/alexandr/.codex/get-shit-done/workflows/execute-plan.md
@/home/alexandr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-CONTEXT.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-RESEARCH.md
@crates/parser-harness/src/benchmark_report.rs
@crates/parser-harness/benches/parser_pipeline.rs
@scripts/benchmark-phase5.sh
@README.md
@AGENTS.md
</context>

<decision_coverage>
- D-20, D-21, D-22, D-23: compact-path benchmark evidence, whole-list/corpus scope, and 10x triage.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend benchmark report schema for compact artifact and corpus evidence</name>
  <files>crates/parser-harness/src/benchmark_report.rs, crates/parser-harness/tests/benchmark_report.rs, crates/parser-harness/src/bin/benchmark-report-check.rs</files>
  <read_first>
    - `crates/parser-harness/src/benchmark_report.rs`
    - `crates/parser-harness/tests/benchmark_report.rs`
    - `crates/parser-harness/src/bin/benchmark-report-check.rs`
    - `.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-CONTEXT.md`
  </read_first>
  <behavior>
    - Benchmark report validation fails without compact artifact size evidence.
    - Benchmark report validation fails without whole-list/corpus evidence or explicit unavailable rationale.
    - `ten_x_status=fail` requires triage that mentions bottleneck, parity, and compact artifact size.
  </behavior>
  <action>
    Update `benchmark_report.rs`:

    Add `ArtifactSizeEvidence` with:

    - `raw_input_bytes: u64`
    - `compact_artifact_bytes: u64`
    - `artifact_raw_ratio: f64`

    Add `BenchmarkEvidence` with:

    - `workload_name: String`
    - `tier: BenchmarkTier`
    - `workload: BenchmarkWorkload`
    - `artifact_size: ArtifactSizeEvidence`
    - `parse_only: BenchmarkMetric`
    - `aggregate_only: BenchmarkMetric`
    - `end_to_end: BenchmarkMetric`
    - `parity_status: ParityStatus`
    - `ten_x_status: TenXStatus`
    - `triage: Option<String>`

    Update `BenchmarkReport` to contain:

    - `selected_evidence: BenchmarkEvidence`
    - `whole_list_or_corpus_evidence: Option<BenchmarkEvidence>`
    - `whole_list_unavailable_reason: Option<String>`

    Validator rules:

    - `selected_evidence.artifact_size.compact_artifact_bytes > 0`
    - `selected_evidence.artifact_size.raw_input_bytes > 0`
    - `artifact_raw_ratio` must equal `compact_artifact_bytes / raw_input_bytes` within `0.0001`
    - either `whole_list_or_corpus_evidence` exists or `whole_list_unavailable_reason` is non-empty
    - if any evidence has `ten_x_status=Fail`, its triage must mention `bottleneck`, `parity`, and `artifact`
    - if any evidence has `ten_x_status=Unknown`, its triage must be non-empty
    - reports with only selected evidence and no unavailable reason fail validation

    Update report-check binary and tests for valid selected+whole-list, valid selected+unavailable-reason, missing artifact size rejected, missing whole-list evidence rejected, and failed 10x without artifact triage rejected.
  </action>
  <verify>
    <automated>cargo test -p parser-harness benchmark_report && rg -n "ArtifactSizeEvidence|BenchmarkEvidence|compact_artifact_bytes|artifact_raw_ratio|whole_list_or_corpus_evidence|whole_list_unavailable_reason" crates/parser-harness/src crates/parser-harness/tests && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `benchmark_report.rs` contains `ArtifactSizeEvidence`.
    - `benchmark_report.rs` contains `compact_artifact_bytes`.
    - `benchmark_report.rs` contains `whole_list_or_corpus_evidence`.
    - Tests reject missing whole-list/corpus evidence.
    - `cargo test -p parser-harness benchmark_report` exits 0.
  </acceptance_criteria>
  <done>Benchmark report schema enforces compact size and workload scope evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Update benchmark script and benches for compact selected and whole-list/corpus runs</name>
  <files>crates/parser-harness/benches/parser_pipeline.rs, scripts/benchmark-phase5.sh, README.md</files>
  <read_first>
    - `crates/parser-harness/benches/parser_pipeline.rs`
    - `scripts/benchmark-phase5.sh`
    - `crates/parser-harness/src/benchmark_report.rs`
    - `.planning/phases/01-legacy-baseline-and-corpus/corpus-manifest.md`
    - `.planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md`
    - `README.md`
  </read_first>
  <action>
    Update `parser_pipeline.rs` so benchmark names refer to the compact path:

    - `parse_only_compact_decode`
    - `facts_only_compact_projection`
    - `end_to_end_compact_parse_replay`

    Update `scripts/benchmark-phase5.sh --ci`:

    - keep output under `.planning/generated/phase-05/benchmarks/` for backward compatibility unless a new Phase 5.1 subdirectory already exists; report JSON must include `phase: "05.1"`
    - build release CLI once
    - run selected compact parse on the curated Phase 5 replay or fallback fixture
    - compute selected `raw_input_bytes` with `wc -c`
    - compute selected `compact_artifact_bytes` with `wc -c` on the new artifact
    - compute `artifact_raw_ratio`
    - record selected parity status from the comparison command's Markdown/JSON detail outputs
    - run whole-list/corpus mode when `~/sg_stats/lists/replaysList.json`, `~/sg_stats/raw_replays`, old parser repo, and `RUN_PHASE5_FULL_CORPUS=1` are available
    - when full mode is unavailable, set `whole_list_unavailable_reason` to a concrete string that names the missing prerequisite or says `RUN_PHASE5_FULL_CORPUS not enabled`
    - validate the report with `cargo run -p parser-harness --bin benchmark-report-check -- --report "$REPORT_PATH"`

    Update README benchmark section so Phase 5.1 requires artifact size and selected plus whole-list/corpus evidence before the 10x claim can pass.
  </action>
  <verify>
    <automated>cargo bench -p parser-harness --bench parser_pipeline -- --sample-size 10 && scripts/benchmark-phase5.sh --ci && rg -n "parse_only_compact_decode|facts_only_compact_projection|end_to_end_compact_parse_replay|compact_artifact_bytes|whole_list_unavailable_reason|RUN_PHASE5_FULL_CORPUS|phase.*05\\.1" crates/parser-harness/benches/parser_pipeline.rs scripts/benchmark-phase5.sh README.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `parser_pipeline.rs` contains `parse_only_compact_decode`.
    - `scripts/benchmark-phase5.sh` contains `compact_artifact_bytes`.
    - `scripts/benchmark-phase5.sh` contains `whole_list_unavailable_reason`.
    - `scripts/benchmark-phase5.sh` contains `RUN_PHASE5_FULL_CORPUS`.
    - `scripts/benchmark-phase5.sh --ci` exits 0.
  </acceptance_criteria>
  <done>Benchmark gate reports compact selected and whole-list/corpus evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Benchmark script to release claim | Generated evidence controls whether 10x claim is accepted. |
| Local corpus paths to benchmark report | Historical data paths may be missing or too expensive for CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-06-01 | Repudiation | 10x evidence | mitigate | Validator requires selected and whole-list/corpus evidence or explicit unavailable rationale. |
| T-05.1-06-02 | Tampering | Artifact-size metric | mitigate | Validator checks raw bytes, artifact bytes, and ratio consistency. |
</threat_model>

<verification>
- `cargo test -p parser-harness benchmark_report`
- `cargo bench -p parser-harness --bench parser_pipeline -- --sample-size 10`
- `scripts/benchmark-phase5.sh --ci`
</verification>

<success_criteria>
- Benchmark reports include compact artifact size and artifact/raw ratio.
- Reports cannot pass with one-replay evidence alone unless whole-list/corpus unavailability is explicit.
- 10x fail/unknown statuses require bottleneck/parity/artifact triage.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-06-SUMMARY.md`.
</output>
