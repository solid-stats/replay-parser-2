---
phase: "05.1-compact-artifact-and-selective-parser-redesign"
verified: "2026-04-29T09:38:37Z"
status: gaps_found
score: "6/7 must-haves verified"
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: "6/7 must-haves verified"
  gaps_closed:
    - "Parse-only compact benchmark now measures parser_core::raw_compact::decode_compact_root instead of serde_json::Value full-DOM decode."
    - "Breaking compact artifact contract now reports contract version 2.0.0 and uses v2 schema/example artifact filenames."
    - "Selected benchmark evidence now describes only the small-CI fixture metrics; optional curated old/new evidence is separated under old_new_command when available."
    - "Curated old/new parity classification is no longer structurally blocked by compact-only participants/facts because script parity is derived from legacy-equivalent surfaces."
  gaps_remaining:
    - "Benchmark/parity acceptance remains open: selected ten_x_status=unknown, selected parity_status=not_run, no whole-list/corpus evidence because RUN_PHASE5_FULL_CORPUS was not enabled, and selected artifact/raw ratio is 59.97366881 on a tiny fixture."
  regressions: []
gaps:
  - truth: "Benchmark evidence proves compact selected and whole-list/corpus performance, parity, compactness, and 10x readiness for the compact artifact path."
    status: failed
    reason: "The previous implementation blockers are fixed, but the generated report is still not acceptance evidence: selected ten_x_status is unknown, selected parity_status is not_run, whole_list_or_corpus_evidence is null with unavailable reason RUN_PHASE5_FULL_CORPUS not enabled, and selected artifact/raw ratio is 59.97366881 on the tiny CI fixture."
    artifacts:
      - path: ".planning/generated/phase-05/benchmarks/benchmark-report.json"
        issue: "Structurally valid report, but selected evidence is not a parity or 10x pass and no whole-list/corpus evidence was generated."
      - path: "scripts/benchmark-phase5.sh"
        issue: "Full corpus remains gated behind RUN_PHASE5_FULL_CORPUS=1; selected evidence intentionally records parity not_run and ten_x unknown for the small-CI fixture."
    missing:
      - "Run whole-list/corpus benchmark evidence with RUN_PHASE5_FULL_CORPUS=1 and required old-parser prerequisites, or record explicit acceptance of the missing corpus evidence."
      - "Run old-vs-new parity on an acceptance workload and produce passed/failed/human_review evidence outside the small-CI selected_evidence bucket."
      - "Produce 10x pass evidence, remediate performance, or record explicit user acceptance of the benchmark/parity gap before Phase 6."
      - "Provide corpus-sized artifact/raw ratio evidence; the tiny selected fixture ratio is evidence only, not proof of material compactness."
---

# Phase 5.1: Compact Artifact and Selective Parser Redesign Verification Report

**Phase Goal:** `server-2` receives a compact, deterministic parser result that contains the statistics and minimal contribution evidence it needs, while parser performance is rebuilt around selective OCAP extraction instead of full JSON-to-JSON translation.
**Verified:** 2026-04-29T09:38:37Z
**Status:** gaps_found
**Re-verification:** Yes - after review fixes

## Method

I did not trust SUMMARY claims. I rechecked the previous verification gap, `05.1-REVIEW-FIX.md`, the review findings, source files, tests, and `.planning/generated/phase-05/benchmarks/benchmark-report.json`.

`gsd-sdk query roadmap.get-phase 05.1 --raw` was attempted again, but this environment's `gsd-sdk` has no `query` command, so roadmap and requirement data were read directly from `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Default artifact excludes full normalized event/entity dumps and contains compact server-facing sections. | VERIFIED | `ParseArtifact` still exposes compact `participants`, `facts`, `summaries`, and `side_facts`, and generated benchmark/example artifacts have no top-level `entities`, `events`, or `aggregates`. |
| 2 | Heavy event/entity/audit detail is not required for ordinary ingestion, and annual/yearly stats do not force a large v1 artifact. | VERIFIED | README and roadmap continue to state compact default output, optional deeper/debug material, and yearly nomination deferral. No default full dump fields are present in schema/examples. |
| 3 | Normal parser path has a selective extraction boundary instead of full `serde_json::Value` DOM parsing. | VERIFIED | `parse_replay` calls `decode_compact_root` in `crates/parser-core/src/artifact.rs`; `decode_compact_root` deserializes `RawOcapRoot` with borrowed `RawValue` fields. Grep found no `from_slice::<Value>` in parser-core hot-path files. |
| 4 | CLI parse/schema use the compact v2 contract by default. | VERIFIED | `CURRENT_CONTRACT_VERSION` is `2.0.0`; schema/examples are `parse-artifact-v2.schema.json`, `parse_artifact_success.v2.json`, and `parse_failure.v2.json`; CLI schema tests pass against the committed v2 schema. |
| 5 | Comparison reports are summary-first and keep machine-readable detail separate. | VERIFIED | CLI compare still renders Markdown by default with optional JSON detail output. `ComparisonReviewSummary` now filters compatible findings out of `top_diffs`, and the regression test passes. |
| 6 | Benchmark report evidence proves selected and whole-list/corpus compact-path parity and 10x readiness. | FAILED | The implementation blockers are fixed, but the generated report records selected `ten_x_status=unknown`, selected `parity_status=not_run`, `whole_list_or_corpus_evidence=null`, `whole_list_unavailable_reason="RUN_PHASE5_FULL_CORPUS not enabled"`, and artifact/raw ratio `59.97366881` on the tiny fixture. |
| 7 | Final handoff docs record implemented status and preserve the Phase 6 blocker. | VERIFIED | README and planning state continue to block Phase 6 until full-corpus benchmark/parity evidence passes or the gap is explicitly accepted. |

**Score:** 6/7 truths verified

## Review Fix Verification

| Review Gap | Status | Evidence |
|------------|--------|----------|
| Parse-only benchmark used `serde_json::Value`. | CLOSED | `crates/parser-harness/benches/parser_pipeline.rs` imports `raw_compact::decode_compact_root` and `parse_only_compact_decode` calls it directly. `benchmark_report_should_measure_selective_compact_decode_not_full_value_decode` rejects `from_slice::<Value>`. |
| Breaking compact contract still advertised `1.0.0`/v1. | CLOSED | `crates/parser-contract/src/version.rs` sets `CURRENT_CONTRACT_VERSION` to `2.0.0` and `ContractVersion::current()` returns `Version::new(2, 0, 0)`. Only v2 schema/example files exist under `schemas/` and `crates/parser-contract/examples/`. |
| Selected benchmark evidence mixed small fixture metrics with curated old/new parity. | CLOSED | `benchmark-report.json` `selected_evidence` is `small_ci`, uses only `aggregate-combat.ocap.json`, and records parity `not_run`/10x `unknown`. `scripts/benchmark-phase5.sh` writes optional curated evidence separately under `old_new_command`. |
| Curated parity was structurally blocked by compact-only participants/facts. | CLOSED | `scripts/benchmark-phase5.sh` computes curated `parity_status` from `status`, `legacy.player_game_results`, and `legacy.relationships` only. Compact-only surfaces remain in comparison detail for human review but do not force curated parity failure. |

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `crates/parser-harness/benches/parser_pipeline.rs` | Parse-only compact benchmark measures selective decode | VERIFIED | Calls `decode_compact_root(black_box(AGGREGATE_FIXTURE))`; no `serde_json::Value` decode in the bench. |
| `crates/parser-harness/tests/benchmark_report.rs` | Regression coverage for benchmark/report fixes | VERIFIED | Includes test rejecting `from_slice::<Value>` in the compact parse-only bench; report validation tests pass. |
| `crates/parser-contract/src/version.rs` | Breaking compact contract version | VERIFIED | Current contract version is `2.0.0`; tests assert this. |
| `schemas/parse-artifact-v2.schema.json` | Committed current schema | VERIFIED | Present, matches fresh schema generation via parser-contract tests, and has compact fields without removed top-level dumps. |
| `crates/parser-contract/examples/parse_artifact_success.v2.json` | Current success example | VERIFIED | Deserializes and validates against the v2 schema; `contract_version` is `2.0.0`. |
| `crates/parser-contract/examples/parse_failure.v2.json` | Current failure example | VERIFIED | Deserializes and validates against the v2 schema; `contract_version` is `2.0.0`. |
| `scripts/benchmark-phase5.sh` | Separates selected CI evidence from optional curated old/new evidence and avoids compact-only parity block | VERIFIED | Selected evidence is fixed to small-CI fixture status `not_run`/`unknown`; optional `old_new_command` is separate; curated parity filters to legacy-equivalent surfaces. |
| `.planning/generated/phase-05/benchmarks/benchmark-report.json` | Current benchmark evidence | PARTIAL / FAILED ACCEPTANCE | Structurally valid, but not an acceptance pass: selected 10x unknown, selected parity not run, no corpus evidence, tiny-fixture artifact/raw ratio `59.97366881`. |
| `crates/parser-core/src/raw_compact.rs` | Selective root/entity/event extraction | VERIFIED | Uses borrowed `RawOcapRoot`/`RawValue` and selective entity/event accessors. |
| `crates/parser-core/src/entities.rs` | SteamID alias source refs fixed | VERIFIED | `entity_steam_id` builds the source path from the actual matched alias key; regression test covers `steamID`, `steamId`, and `steam_id`. |

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `parse_replay` | `decode_compact_root` | Parser-core selective root decode | WIRED | Normal parser entrypoint uses the compact root boundary before metadata/entity/event normalization. |
| `parse_only_compact_decode` | `decode_compact_root` | Criterion benchmark import/use | WIRED | The benchmark now measures the same selective root decoder instead of a full `Value` DOM. |
| `ContractVersion::current()` | v2 schema/examples/tests | Version tests and schema tests | WIRED | Contract tests, schema tests, examples, and CLI schema tests all point at v2 artifacts. |
| `scripts/benchmark-phase5.sh` | `selected_evidence` | Generated benchmark report | WIRED / PARTIAL | Data flow is consistent for small-CI selected evidence, but selected evidence intentionally has parity not run and 10x unknown. |
| `scripts/benchmark-phase5.sh` | optional `old_new_command` | Old parser runner and compare output | WIRED WHEN PREREQS EXIST | Curated old/new parity is separate from selected evidence and filters compact-only surfaces out of pass/fail classification. This run did not produce `old_new_command`. |
| `scripts/benchmark-phase5.sh` | whole-list/corpus evidence | `RUN_PHASE5_FULL_CORPUS=1` branch | NOT PRESENT IN REPORT | Current report records unavailable reason instead of evidence. |

## Data-Flow Trace

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `ParseArtifact` | `participants`, `facts`, `summaries`, `side_facts` | input bytes -> `decode_compact_root` -> normalization -> compact mapping | Yes | VERIFIED |
| Parse-only benchmark | `parse_only.wall_time_ms` | Criterion `parse_only_compact_decode` -> `decode_compact_root` | Yes | VERIFIED |
| Selected benchmark evidence | `selected_evidence` | small-CI fixture bytes, compact artifact bytes, Criterion metrics | Partial | FLOWING, but deliberately no selected parity or 10x decision. |
| Optional curated old/new evidence | `old_new_command` | old parser runner + new release CLI + comparison report | Conditional | WIRED, but absent in current report because prerequisites were not run/available. |
| Whole-list/corpus evidence | `whole_list_or_corpus_evidence` | `RUN_PHASE5_FULL_CORPUS=1` branch | No current data | FAILED ACCEPTANCE: report contains null evidence and unavailable reason. |

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Contract v2/schema/examples are coherent | `cargo test -p parser-contract` | 71 tests passed, including v2 version, schema freshness, and example validation tests | PASS |
| Benchmark and comparison regression tests pass | `cargo test -p parser-harness --test benchmark_report --test comparison_report` | 20 tests passed, including selective benchmark and top-diff filtering regressions | PASS |
| SteamID alias path regression passes | `cargo test -p parser-core entity_normalization_should_preserve_steam_id_presence_and_missing_state_in_participants` | Targeted parser-core regression passed | PASS |
| CLI schema emits current committed schema | `cargo test -p parser-cli schema_command` | 4 schema command tests passed | PASS |
| Generated benchmark report validates structurally | `cargo run -p parser-harness --bin benchmark-report-check -- --report .planning/generated/phase-05/benchmarks/benchmark-report.json` | `benchmark_report_valid=true`; selected 10x `Unknown`; selected parity `NotRun`; whole-list evidence `false` | PASS for schema, FAIL for acceptance |

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| OUT-09 | Phase 5.1 | Compact default artifact excluding full dumps | SATISFIED | Contract, schema, examples, parser output, and generated benchmark artifact use compact root fields and omit `entities`/`events`/`aggregates`. |
| OUT-10 | Phase 5.1 | Heavy debug/parity detail not required for ordinary ingestion | SATISFIED | Default artifact remains compact; compare details and raw replay reprocessing remain outside ordinary ingestion. |
| PARS-12 | Phase 5.1 | Selective extraction path avoids unnecessary full DOM/default JSON-to-JSON reserialization | SATISFIED | Parser path and parse-only benchmark use `decode_compact_root`; no checked hot-path `Value` full-DOM decode was found. |
| TEST-06 | Phase 5 / 5.1 | Approximately 10x faster target or accepted gap | BLOCKED | Current report has selected `ten_x_status=unknown`; no accepted override exists. |
| TEST-13 | Phase 5.1 | Benchmark report includes compact size, throughput, RSS, parity, 10x, selected and whole-list/corpus evidence | PARTIAL / BLOCKED | Report includes selected compact metrics and an unavailable reason, but no whole-list/corpus evidence, no selected parity, no selected 10x decision, and only tiny-fixture artifact/raw ratio evidence. |
| TEST-14 | Phase 5.1 | Summary-first old-vs-new comparison reports with detailed machine-readable evidence separated | SATISFIED | Markdown summary/default detail separation remains implemented; top diffs now exclude compatible findings. |

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | - | - | - | No blocker implementation anti-patterns found in the rechecked review-fix files. |

The previous blocker anti-pattern in `crates/parser-harness/benches/parser_pipeline.rs` is closed. The remaining issue is evidence acceptance, not a known source-code stub.

## Human Verification Required

None for UI, visual behavior, or external service behavior. A human decision is required only if the team wants to override the benchmark/parity blocker and proceed to Phase 6 without passing evidence.

## Gaps Summary

The review-fix implementation gaps were fixed. The parse-only benchmark now uses the selective compact root decoder, the compact contract is versioned as `2.0.0` with v2 schema/examples, selected benchmark evidence no longer mixes the tiny fixture with curated old/new parity, and curated parity classification is no longer structurally blocked by compact-only participants/facts.

Phase 5.1 still does not meet the benchmark/parity acceptance contract. The generated report remains selected `ten_x_status=unknown`, selected `parity_status=not_run`, no whole-list/corpus evidence because `RUN_PHASE5_FULL_CORPUS` was not enabled, and artifact/raw ratio `59.97366881` on a tiny fixture. Phase 6 remains blocked until full-corpus benchmark/parity evidence passes or this gap is explicitly accepted.

---

_Verified: 2026-04-29T09:38:37Z_
_Verifier: the agent (gsd-verifier)_
