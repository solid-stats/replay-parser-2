# Phase 5.1: Compact Artifact and Selective Parser Redesign - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution
> agents. Decisions are captured in CONTEXT.md — this log preserves the
> alternatives considered.

**Date:** 2026-04-29T13:37:29+07:00
**Phase:** 5.1-compact-artifact-and-selective-parser-redesign
**Areas discussed:** Artifact cutline, Audit evidence, Selective parser path,
Benchmark and comparison gates

---

## Artifact Cutline

| Decision Point | Options Considered | Selected |
|----------------|--------------------|----------|
| Contract move | New compact contract; transitional shape; dual artifact modes | New compact contract |
| Default sections | Stats artifact; minimal aggregates; evidence rows | Stats artifact |
| Top-level `entities`/`events` | Remove them; compact versions; deprecated empty | Remove them |
| Schema versioning | Breaking version; same version; agent decides | Breaking version |
| Section names | Name them now; describe only; legacy namespaces | Describe only |
| CLI default output | Compact default; full CLI artifact; two outputs | Compact default |
| Participant identity granularity | Observed refs; entity snapshots; IDs only | Observed refs after risk review |
| Full artifact status | Debug sidecar; remove entirely; internal only | Remove entirely |
| Failure artifact shape | Same compact envelope; failure-only schema; agent decides | Same compact envelope |
| `server-2` review | Required before execution; during implementation; local docs enough | Required before execution |
| Old full schema | No public schema; archive schema; keep supported | No public schema |
| Size target | Relative target; hard target now; no size target | Relative target |

**Notes:** The user initially selected IDs-only participant references, then
accepted the risk correction to keep minimal observed refs so `server-2` does
not need to parse raw OCAP for ordinary identity ingestion.

---

## Audit Evidence

| Decision Point | Options Considered | Selected |
|----------------|--------------------|----------|
| Evidence level | Contribution proof; projection proof; raw refs only | Contribution proof after risk review |
| Facts vs projections | Facts + summaries; facts only; summaries only | Facts + summaries |
| Bounty boundary | Kill facts, no points; summary only; agent decides | Kill facts, no points |
| Vehicle score boundary | Inputs, no final score; per-replay score; server only | Inputs, no final score |
| Observed identity for server | Minimal observed set; name + id only; agent decides | Minimal observed set |
| Deep detail fallback | Targeted explain; raw only; small sidecars | Raw only |

**Notes:** The user asked why bounty would be calculated during parsing. The
decision was clarified: parser never calculates final bounty points because
bounty depends on previous rotation and other replays. Parser emits compact
per-replay kill/stat facts for `server-2` calculation.

---

## Selective Parser Path

| Decision Point | Options Considered | Selected |
|----------------|--------------------|----------|
| Strategy | Compact-first path; two-phase transition; accepted plan only | Compact-first path |
| Normal-path DOM policy | No full DOM normal path; DOM allowed initially; agent decides | No full DOM normal path |
| Source refs under selective parsing | Raw coordinates; synthetic refs; agent decides | Raw coordinates |
| Existing normalized-event architecture | Refactor to facts; keep internal events; rewrite core | Rewrite core |
| Rewrite risk confirmation | Controlled rewrite; full rewrite override; back to hot path | Full rewrite override |
| Minimum acceptance | Implemented + measured; prototype + plan; plan accepted | Implemented + measured |

**Notes:** The user explicitly accepted full rewrite override despite the risk
that Phase 3/4 edge-case semantics must be restored and proven after rewrite.

---

## Benchmark and Comparison Gates

| Decision Point | Options Considered | Selected |
|----------------|--------------------|----------|
| Benchmark scope | Selected + whole-list; whole-list only; curated only | Selected + whole-list |
| 10x gate | Blocking unless accepted; advisory; whole-list only | Blocking unless accepted |
| Artifact size metric | Raw vs compact; compact only; agent decides | Raw vs compact |
| Comparison default shape | Summary + evidence file; single JSON only; Markdown only | Markdown preferred |
| Comparison gate scope | Human default, optional JSON; Markdown only; agent decides | Human default, optional JSON |
| Parity status | No unexplained blocker; strict pass; human review ok | No unexplained blocker |

**Notes:** The user does not need comparison reports in CI. Markdown summary is
the default human review surface; optional structured evidence remains allowed
for audit/debug.

## the agent's Discretion

- Exact compact section names and Rust type names.
- Exact artifact-size threshold after corpus evidence.
- Exact selective parsing technique, provided normal path avoids full replay DOM
  translation.
- Exact Markdown/JSON comparison file layout.

## Deferred Ideas

None.
