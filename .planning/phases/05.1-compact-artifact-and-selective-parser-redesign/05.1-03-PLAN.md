---
phase: 05.1-compact-artifact-and-selective-parser-redesign
plan: 03
type: execute
wave: 4
depends_on:
  - 05.1-02
files_modified:
  - crates/parser-core/src/artifact.rs
  - crates/parser-core/src/entities.rs
  - crates/parser-core/src/events.rs
  - crates/parser-core/src/aggregates.rs
  - crates/parser-core/src/side_facts.rs
  - crates/parser-core/tests/aggregate_projection.rs
  - crates/parser-core/tests/vehicle_score.rs
  - crates/parser-core/tests/fault_injection_regressions.rs
  - crates/parser-core/tests/side_facts.rs
  - crates/parser-core/tests/deterministic_output.rs
autonomous: true
requirements:
  - OUT-09
  - OUT-10
  - PARS-12
requirements_addressed: [OUT-09, OUT-10, PARS-12]
must_haves:
  truths:
    - "D-06: compact participant refs include source entity ID, observed name, side, group/squad/role, SteamID presence/value, and source refs."
    - "D-12: compact combat/stat facts include valid enemy kill facts and exclusion reasons for teamkill, suicide, null killer, vehicle victim, and unknown actor cases."
    - "D-13: summaries support parity/review and fast ingestion sanity checks while facts remain recalculation source."
    - "D-14: vehicle score emits contribution facts and denominator eligibility without final cross-replay score."
    - "D-20: compact path is implemented and measured by behavior tests before Phase 6."
  artifacts:
    - path: "crates/parser-core/src/artifact.rs"
      provides: "Compact artifact construction"
      contains: "participants"
    - path: "crates/parser-core/src/aggregates.rs"
      provides: "Contribution facts and summary projections"
      contains: "vehicle_score.denominator_inputs"
    - path: "crates/parser-core/tests/deterministic_output.rs"
      provides: "Deterministic compact output tests"
      contains: "compact"
  key_links:
    - from: "crates/parser-core/src/events.rs"
      to: "crates/parser-core/src/aggregates.rs"
      via: "combat facts feed contributions"
      pattern: "CombatFact"
---

<objective>
Wire compact participant refs, combat facts, contribution facts, summaries, side facts, and determinism into parser-core.

Purpose: preserve Phase 3/4 semantics in the compact artifact instead of dropping audit evidence during the rewrite.
Output: compact artifact population and behavior tests for core, aggregate, bounty, vehicle score, side fact, and deterministic surfaces.
</objective>

<execution_context>
@/home/alexandr/.codex/get-shit-done/workflows/execute-plan.md
@/home/alexandr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-CONTEXT.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-RESEARCH.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-PATTERNS.md
@crates/parser-contract/src/compact.rs
@crates/parser-core/src/artifact.rs
@crates/parser-core/src/entities.rs
@crates/parser-core/src/events.rs
@crates/parser-core/src/aggregates.rs
@crates/parser-core/src/side_facts.rs
@crates/parser-core/tests/aggregate_projection.rs
@crates/parser-core/tests/vehicle_score.rs
@AGENTS.md
</context>

<decision_coverage>
- D-02, D-05, D-06, D-08, D-11 through D-15, D-18, D-19, D-20: compact parser output preserves required semantics.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert observed entities into compact participant refs</name>
  <files>crates/parser-core/src/entities.rs, crates/parser-core/src/artifact.rs, crates/parser-core/tests/parser_core_api.rs, crates/parser-core/tests/schema_drift_status.rs</files>
  <read_first>
    - `crates/parser-core/src/entities.rs`
    - `crates/parser-core/src/artifact.rs`
    - `crates/parser-contract/src/compact.rs`
    - `crates/parser-core/tests/parser_core_api.rs`
    - `crates/parser-core/tests/schema_drift_status.rs`
  </read_first>
  <behavior>
    - Participant refs preserve observed identity fields without serializing full entity snapshots.
    - Connected-player backfill and duplicate-slot same-name compatibility remain observable through participant refs, summaries, diagnostics, or compatibility source refs.
  </behavior>
  <action>
    Keep existing entity normalization logic as an internal typed step if it is still the safest way to preserve Phase 3 behavior, but change public artifact construction so it maps normalized entities into `ObservedParticipantRef` values.

    Mapping rules:

    - `source_entity_id` copies the normalized entity source ID.
    - `observed_name` copies observed entity name/nickname presence.
    - `side` copies observed side presence.
    - `group` copies group/squad presence.
    - `role` copies description/role presence.
    - `steam_id` copies existing SteamID identity presence or explicit unknown when absent.
    - `source_refs` includes the entity source refs already attached by entity normalization.

    Do not add top-level `entities` to `ParseArtifact`. Add tests that parse `duplicate-slot-same-name.ocap.json` and connected-player fixtures and assert `artifact.participants` contains the expected source IDs and observed names while serialized JSON has no top-level `entities`.
  </action>
  <verify>
    <automated>cargo test -p parser-core parser_core_api schema_drift_status && rg -n "participants|ObservedParticipantRef|duplicate-slot" crates/parser-core/src crates/parser-core/tests && ! rg -n "artifact\\.entities|\"entities\"" crates/parser-core/tests/parser_core_api.rs && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Parser-core populates `artifact.participants`.
    - Tests assert at least one participant source entity ID.
    - Tests assert serialized parser output has no top-level `entities`.
    - `cargo test -p parser-core parser_core_api schema_drift_status` exits 0.
  </acceptance_criteria>
  <done>Observed identity survives as compact participant refs only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Populate compact combat facts, contribution facts, summaries, and side facts</name>
  <files>crates/parser-core/src/artifact.rs, crates/parser-core/src/events.rs, crates/parser-core/src/aggregates.rs, crates/parser-core/src/side_facts.rs, crates/parser-core/tests/aggregate_projection.rs, crates/parser-core/tests/vehicle_score.rs, crates/parser-core/tests/fault_injection_regressions.rs, crates/parser-core/tests/side_facts.rs</files>
  <read_first>
    - `crates/parser-core/src/events.rs`
    - `crates/parser-core/src/aggregates.rs`
    - `crates/parser-core/src/side_facts.rs`
    - `crates/parser-contract/src/compact.rs`
    - `crates/parser-contract/src/aggregates.rs`
    - `crates/parser-core/tests/aggregate_projection.rs`
    - `crates/parser-core/tests/vehicle_score.rs`
  </read_first>
  <behavior>
    - Enemy kill, teamkill, suicide, null killer, vehicle victim, and unknown actor cases remain visible as compact combat facts or exclusions.
    - `facts.aggregate_contributions` contains legacy, relationship, bounty, and vehicle score contribution refs.
    - `summaries.projections` contains legacy player results, relationships, bounty inputs, vehicle score inputs, and vehicle score denominator inputs.
    - Parser never computes final bounty points or final cross-replay vehicle score.
  </behavior>
  <action>
    Change aggregate derivation to consume compact combat facts or the internal combat observations produced from selective events and return:

    - `facts.combat` containing one `CombatFact` per source killed tuple that current Phase 4 behavior normalized into a combat event
    - `facts.aggregate_contributions` containing all existing aggregate contribution refs
    - `summaries.projections` containing the existing projection keys:
      - `legacy.player_game_results`
      - `legacy.relationships`
      - `legacy.game_type_compatibility`
      - `legacy.squad_inputs`
      - `legacy.rotation_inputs`
      - `bounty.inputs`
      - `vehicle_score.inputs`
      - `vehicle_score.denominator_inputs`

    Preserve existing rule IDs, contribution ID sorting, projection key sorting, vehicle score award/penalty weights, teamkill penalty clamp, and side fact output.

    Update tests to stop reading `artifact.events` and `artifact.aggregates`; they must read `artifact.facts.combat`, `artifact.facts.aggregate_contributions`, and `artifact.summaries.projections`.
  </action>
  <verify>
    <automated>cargo test -p parser-core aggregate_projection vehicle_score fault_injection_regressions side_facts && rg -n "facts\\.combat|aggregate_contributions|summaries\\.projections|vehicle_score\\.denominator_inputs|bounty\\.inputs" crates/parser-core/src crates/parser-core/tests && ! rg -n "artifact\\.events|artifact\\.aggregates" crates/parser-core/src crates/parser-core/tests && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Parser-core writes `facts.combat`.
    - Parser-core writes `facts.aggregate_contributions`.
    - Parser-core writes `summaries.projections`.
    - Tests assert `vehicle_score.denominator_inputs`.
    - No parser-core tests reference `artifact.events`.
    - No parser-core tests reference `artifact.aggregates`.
    - `cargo test -p parser-core aggregate_projection vehicle_score fault_injection_regressions side_facts` exits 0.
  </acceptance_criteria>
  <done>Compact facts and summaries preserve Phase 4 semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove compact determinism and absence of full dumps</name>
  <files>crates/parser-core/tests/deterministic_output.rs, crates/parser-core/tests/parser_core_api.rs</files>
  <read_first>
    - `crates/parser-core/tests/deterministic_output.rs`
    - `crates/parser-core/tests/parser_core_api.rs`
    - `crates/parser-core/tests/fixtures/aggregate-combat.ocap.json`
    - `crates/parser-core/src/artifact.rs`
  </read_first>
  <behavior>
    - Repeated compact parses serialize byte-identically.
    - Compact populated output has non-empty participants, combat facts, contributions, and projections.
    - Serialized populated output omits top-level `entities` and `events`.
  </behavior>
  <action>
    Update `deterministic_output.rs` tests to compact fields.

    Add or update tests named:

    - `deterministic_output_should_serialize_compact_artifact_identically`
    - `deterministic_output_should_keep_compact_contributions_sorted_by_id`
    - `deterministic_output_should_omit_full_entity_and_event_dumps`

    Assertions:

    - parsing `aggregate-combat.ocap.json` twice serializes to identical JSON strings
    - `participants` is non-empty
    - `facts.combat` is non-empty
    - `facts.aggregate_contributions` is non-empty
    - `summaries.projections` is non-empty
    - contribution IDs are sorted ascending
    - projection keys are sorted
    - `produced_at` remains `None`
    - serialized JSON root has no `entities` and no `events`
  </action>
  <verify>
    <automated>cargo test -p parser-core deterministic_output parser_core_api && rg -n "deterministic_output_should_serialize_compact_artifact_identically|deterministic_output_should_omit_full_entity_and_event_dumps|produced_at" crates/parser-core/tests/deterministic_output.rs && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `deterministic_output.rs` contains `deterministic_output_should_serialize_compact_artifact_identically`.
    - `deterministic_output.rs` contains `deterministic_output_should_omit_full_entity_and_event_dumps`.
    - New tests assert `participants` is non-empty.
    - New tests assert `facts.combat` is non-empty.
    - New tests assert serialized root lacks `entities` and `events`.
    - `cargo test -p parser-core deterministic_output parser_core_api` exits 0.
  </acceptance_criteria>
  <done>Compact output is deterministic and free of full top-level dumps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal observations to compact artifact | Rich internal semantics are reduced into server-facing facts. |
| Parser facts to `server-2` recalculation | Backend relies on source refs and contribution facts for audit and recalculation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-03-01 | Information Disclosure | Default artifact | mitigate | Tests assert top-level full `entities` and `events` dumps are absent. |
| T-05.1-03-02 | Tampering | Contribution evidence | mitigate | Preserve non-empty source refs, rule IDs, and sorted contribution IDs in tests. |
| T-05.1-03-03 | Repudiation | Bounty and vehicle score inputs | mitigate | Keep exclusion reasons, raw/applied weights, and denominator eligibility facts. |
</threat_model>

<verification>
- `cargo test -p parser-core aggregate_projection vehicle_score fault_injection_regressions side_facts deterministic_output parser_core_api`
- Grep proves tests and parser-core no longer use `artifact.events` or `artifact.aggregates`.
</verification>

<success_criteria>
- Compact artifact contains participants, combat facts, aggregate contribution facts, projections, side facts, diagnostics, and failure/status data.
- Full normalized event/entity dumps are not in the default serialized artifact.
- Phase 3/4 behavior tests remain green after field migration.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-03-SUMMARY.md`.
</output>
