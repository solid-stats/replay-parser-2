---
phase: 05.1-compact-artifact-and-selective-parser-redesign
plan: 03
subsystem: parser-core
tags: [parser-core, compact-artifact, participants, aggregates, determinism]
requires:
  - phase: 05.1-02
    provides: Selective parser-core decode boundary and compact artifact envelope
provides:
  - Compact participant refs with SteamID presence/value preservation
  - Compact combat and aggregate contribution test oracles
  - Deterministic compact serialization and removed full-dump assertions
requirements-completed: [OUT-09, OUT-10, PARS-12]
completed: 2026-04-29
---

# Phase 05.1 Plan 03: Compact Core Semantics Summary

## Accomplishments

- Preserved observed SteamID values in compact `participants` while keeping missing SteamIDs explicit as `MissingSteamId`.
- Strengthened aggregate projection tests to assert all compact contribution families: legacy, relationship, bounty, and vehicle score.
- Extended projection-key assertions to include `vehicle_score.inputs` and `vehicle_score.denominator_inputs`.
- Added tests proving parser-core emits bounty and vehicle-score inputs without final bounty points or final cross-replay vehicle score.
- Renamed and extended deterministic tests for compact output: byte-identical serialization, non-empty participants/facts/projections, sorted contribution IDs/projection keys, `produced_at: None`, and no serialized top-level `entities`/`events`.
- Kept combat fact rule/source-reference coverage explicit for compact combat facts.

## Task Commits

1. `af91063` - `feat: preserve steam ids in compact participants`
2. `cee72e2` - `test: lock compact artifact facts and determinism`
3. `5b0b059` - `style: apply rustfmt to schema contract test`

## Deviations

- `cargo fmt --all` reformatted one existing `parser-contract` test expression outside the Plan 03 primary parser-core scope. The change is formatting-only and was committed separately as `5b0b059`.
- Added focused assertions in `entity_normalization.rs` and `combat_event_semantics.rs` because those existing parser-core tests are the closest behavior-level surfaces for participant identity and combat rule/source-ref preservation.

## Verification

- `cargo fmt --all`
- `cargo test -p parser-core aggregate_projection`
- `cargo test -p parser-core vehicle_score`
- `cargo test -p parser-core fault_injection_regressions`
- `cargo test -p parser-core side_facts`
- `cargo test -p parser-core deterministic_output`
- `cargo test -p parser-core parser_core_api`
- `cargo test -p parser-core`
- Grep gate: no `artifact.events` or `artifact.aggregates` references in `crates/parser-core/src` or `crates/parser-core/tests`.
- Grep gate: serialized root `entities`/`events` checks are absence checks in `deterministic_output.rs`.
- `git diff --check`

## Remaining Risks

- Parser-core still uses internal normalized entities/events before compacting into the public artifact; this preserves Phase 3/4 behavior but remains a Phase 5.1 performance consideration for later benchmark plans.
- Artifact-size and whole-list/corpus benchmark acceptance remain for later Phase 5.1 plans.
