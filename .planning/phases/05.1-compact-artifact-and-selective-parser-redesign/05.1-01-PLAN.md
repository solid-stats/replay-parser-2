---
phase: 05.1-compact-artifact-and-selective-parser-redesign
plan: 01
type: execute
wave: 2
depends_on:
  - 05.1-00
files_modified:
  - crates/parser-contract/src/artifact.rs
  - crates/parser-contract/src/compact.rs
  - crates/parser-contract/src/lib.rs
  - crates/parser-contract/examples/parse_artifact_success.v1.json
  - crates/parser-contract/examples/parse_failure.v1.json
  - crates/parser-contract/tests/artifact_envelope.rs
  - crates/parser-contract/tests/schema_contract.rs
  - crates/parser-contract/tests/failure_contract.rs
  - schemas/parse-artifact-v1.schema.json
autonomous: true
requirements:
  - OUT-09
  - OUT-10
requirements_addressed: [OUT-09, OUT-10]
must_haves:
  truths:
    - "D-01: compact artifact redesign is a breaking schema change reflected in contract Rust types, examples, and schema."
    - "D-02: default artifact contains replay/source metadata, compact participant refs, compact stat/combat facts, summaries, side facts, diagnostics, version data, and failure/status data."
    - "D-03 and D-07: default artifact no longer has top-level `entities` or `events` fields."
    - "D-04: final section/type names are concrete planner choices that preserve locked compact semantics."
    - "D-08: success, partial, failed, and skipped outputs share the same compact envelope."
  artifacts:
    - path: "crates/parser-contract/src/compact.rs"
      provides: "Compact participant, fact, and summary contract types"
      contains: "ObservedParticipantRef"
    - path: "crates/parser-contract/src/artifact.rs"
      provides: "Compact ParseArtifact envelope"
      contains: "participants"
    - path: "schemas/parse-artifact-v1.schema.json"
      provides: "Committed compact schema"
      contains: "ParseFactSection"
  key_links:
    - from: "crates/parser-contract/src/artifact.rs"
      to: "crates/parser-contract/examples/parse_artifact_success.v1.json"
      via: "schema/example freshness"
      pattern: "participants"
---

<objective>
Replace the public default contract with a compact artifact envelope and refresh schema/examples/tests.

Purpose: remove full normalized event/entity dumps from ordinary ingestion while preserving auditable facts and failure/status invariants.
Output: compact contract types, updated examples, regenerated schema, and contract tests.
</objective>

<execution_context>
@/home/alexandr/.codex/get-shit-done/workflows/execute-plan.md
@/home/alexandr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-CONTEXT.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-RESEARCH.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-PATTERNS.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-SERVER-COMPATIBILITY.md
@crates/parser-contract/src/artifact.rs
@crates/parser-contract/src/identity.rs
@crates/parser-contract/src/events.rs
@crates/parser-contract/src/aggregates.rs
@crates/parser-contract/src/side_facts.rs
@crates/parser-contract/src/source_ref.rs
@crates/parser-contract/src/presence.rs
@AGENTS.md
</context>

<decision_coverage>
- D-01 through D-08: compact breaking artifact envelope and examples.
- D-11 through D-15: compact fact/contribution boundary without final bounty or cross-replay score calculation.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add compact contract types and replace artifact fields</name>
  <files>crates/parser-contract/src/artifact.rs, crates/parser-contract/src/compact.rs, crates/parser-contract/src/lib.rs, crates/parser-contract/tests/artifact_envelope.rs, crates/parser-contract/tests/failure_contract.rs</files>
  <read_first>
    - `crates/parser-contract/src/artifact.rs`
    - `crates/parser-contract/src/identity.rs`
    - `crates/parser-contract/src/events.rs`
    - `crates/parser-contract/src/aggregates.rs`
    - `crates/parser-contract/src/source_ref.rs`
    - `crates/parser-contract/tests/artifact_envelope.rs`
    - `crates/parser-contract/tests/failure_contract.rs`
  </read_first>
  <behavior>
    - `ParseArtifact` success JSON serializes `participants`, `facts`, and `summaries`.
    - `ParseArtifact` success JSON does not serialize top-level `entities` or `events`.
    - Failed artifacts still require `failure` and non-failed artifacts still reject `failure`.
  </behavior>
  <action>
    Create `crates/parser-contract/src/compact.rs` and export it from `lib.rs`.

    Define these schema-visible types:

    - `ObservedParticipantRef { source_entity_id: i64, observed_name: FieldPresence<String>, side: FieldPresence<EntitySide>, group: FieldPresence<String>, role: FieldPresence<String>, steam_id: FieldPresence<String>, source_refs: SourceRefs }`
    - `CombatFact { fact_id: String, semantic: CombatSemantic, frame: FieldPresence<u64>, event_index: FieldPresence<u64>, killer: FieldPresence<EventActorRef>, victim: FieldPresence<EventActorRef>, victim_kind: CombatVictimKind, vehicle_context: VehicleContext, bounty: BountyEligibility, legacy_counter_effects: Vec<LegacyCounterEffect>, source_refs: SourceRefs, rule_id: RuleId }`
    - `ParseFactSection { combat: Vec<CombatFact>, aggregate_contributions: Vec<AggregateContributionRef> }`
    - `ParseSummarySection { projections: BTreeMap<String, serde_json::Value> }`

    Implement `Default` for `ParseFactSection` and `ParseSummarySection`.

    Update `ParseArtifact` in `artifact.rs`:

    - remove `entities: Vec<ObservedEntity>`
    - remove `events: Vec<NormalizedEvent>`
    - remove `aggregates: AggregateSection`
    - add `participants: Vec<ObservedParticipantRef>`
    - add `facts: ParseFactSection`
    - add `summaries: ParseSummarySection`
    - keep `side_facts: ReplaySideFacts`

    Keep `validate_status_payload` behavior unchanged.

    Update contract tests so `artifact_envelope.rs` asserts `serialized.get("participants").is_some()`, `serialized.get("facts").is_some()`, `serialized.get("summaries").is_some()`, `serialized.get("entities").is_none()`, and `serialized.get("events").is_none()`.
  </action>
  <verify>
    <automated>cargo test -p parser-contract artifact_envelope failure_contract && rg -n "ObservedParticipantRef|ParseFactSection|ParseSummarySection|participants|facts|summaries" crates/parser-contract/src crates/parser-contract/tests && ! rg -n "pub entities:|pub events:|pub aggregates:" crates/parser-contract/src/artifact.rs && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `compact.rs` contains `pub struct ObservedParticipantRef`.
    - `compact.rs` contains `pub struct CombatFact`.
    - `artifact.rs` contains `pub participants: Vec<ObservedParticipantRef>`.
    - `artifact.rs` does not contain `pub entities:`.
    - `artifact.rs` does not contain `pub events:`.
    - `cargo test -p parser-contract artifact_envelope failure_contract` exits 0.
  </acceptance_criteria>
  <done>Public artifact envelope uses compact participant/fact/summary fields.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh schema and examples for compact envelope</name>
  <files>crates/parser-contract/examples/parse_artifact_success.v1.json, crates/parser-contract/examples/parse_failure.v1.json, crates/parser-contract/tests/schema_contract.rs, schemas/parse-artifact-v1.schema.json</files>
  <read_first>
    - `crates/parser-contract/examples/parse_artifact_success.v1.json`
    - `crates/parser-contract/examples/parse_failure.v1.json`
    - `crates/parser-contract/examples/export_schema.rs`
    - `crates/parser-contract/tests/schema_contract.rs`
    - `schemas/parse-artifact-v1.schema.json`
  </read_first>
  <action>
    Update success and failure examples to the compact envelope.

    Success example must contain:

    - `participants` with at least one `ObservedParticipantRef`
    - `facts.combat` with at least one `CombatFact`
    - `facts.aggregate_contributions` with at least one contribution ref
    - `summaries.projections` with keys `legacy.player_game_results`, `bounty.inputs`, `vehicle_score.inputs`, and `vehicle_score.denominator_inputs`
    - `side_facts`

    Failure example must contain empty `participants`, empty `facts.combat`, empty `facts.aggregate_contributions`, empty `summaries.projections`, `status: "failed"`, and a structured `failure`.

    Update `schema_contract.rs` so it asserts schema/example text contains `ObservedParticipantRef`, `CombatFact`, `ParseFactSection`, `ParseSummarySection`, `participants`, `facts`, and `summaries`, and asserts schema/example text does not contain top-level required fields named `entities` or `events`.

    Regenerate schema with:

    ```bash
    cargo run -p parser-contract --example export_schema > schemas/parse-artifact-v1.schema.json
    ```
  </action>
  <verify>
    <automated>cargo run -p parser-contract --example export_schema > /tmp/parse-artifact-v1.schema.json && cmp /tmp/parse-artifact-v1.schema.json schemas/parse-artifact-v1.schema.json && cargo test -p parser-contract schema_contract && rg -n "\"participants\"|\"facts\"|\"summaries\"|\"ParseFactSection\"" crates/parser-contract/examples schemas/parse-artifact-v1.schema.json && ! rg -n '"entities"|"events"' crates/parser-contract/examples/parse_artifact_success.v1.json crates/parser-contract/examples/parse_failure.v1.json && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `parse_artifact_success.v1.json` contains `"participants"`.
    - `parse_artifact_success.v1.json` contains `"facts"`.
    - `parse_artifact_success.v1.json` contains `"summaries"`.
    - `parse_artifact_success.v1.json` does not contain `"entities"`.
    - `schemas/parse-artifact-v1.schema.json` matches freshly generated schema.
    - `cargo test -p parser-contract schema_contract` exits 0.
  </acceptance_criteria>
  <done>Committed schema and examples match the compact contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Raw parser output to backend ingestion | Compact artifact fields replace public full dump fields. |
| Failure output to operators | Failed artifacts must keep structured failure payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-01-01 | Tampering | `ParseArtifact` schema | mitigate | Schema/example tests assert compact fields exist and removed dump fields are absent. |
| T-05.1-01-02 | Repudiation | Failure artifacts | mitigate | Keep `validate_status_payload` tests for failed/non-failed invariants. |
</threat_model>

<verification>
- `cargo test -p parser-contract artifact_envelope failure_contract schema_contract`
- Fresh schema generation matches `schemas/parse-artifact-v1.schema.json`.
- Grep proves top-level `entities` and `events` are absent from success/failure examples.
</verification>

<success_criteria>
- Compact contract compiles and schema/examples are fresh.
- Default public artifact no longer exposes top-level full entity/event dumps.
- Status/failure invariants still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-01-SUMMARY.md`.
</output>
