---
phase: 05.1-compact-artifact-and-selective-parser-redesign
artifact: security-threat-mitigation-audit
asvs_level: 2
block_on: high
threats_total: 17
threats_closed: 17
threats_open: 0
unregistered_flags: 0
verdict: SECURED
created: 2026-04-29
updated: 2026-04-29
---

# Phase 05.1 Security Audit

## Verdict

SECURED for the declared Phase 05.1 threat register after the review fixes
currently present in the working tree. All 17 declared threats from plans 00-07
resolve to CLOSED or documented accepted risk.

The review fixes do not introduce a new unmapped security/trust-boundary
concern. The remaining benchmark/parity acceptance gap is not security-cleared
by this audit: Phase 6 remains blocked until whole-list/corpus benchmark and
parity evidence pass, or the user explicitly accepts that gap.

## Scope

Audited plans:

- `05.1-00-PLAN.md` - server compatibility gate
- `05.1-01-PLAN.md` - compact schema/contract
- `05.1-02-PLAN.md` - selective parser decode
- `05.1-03-PLAN.md` - compact facts/summaries/side facts
- `05.1-04-PLAN.md` - CLI parse/schema compact output
- `05.1-05-PLAN.md` - comparison output
- `05.1-06-PLAN.md` - benchmark report validation
- `05.1-07-PLAN.md` - final handoff and Phase 6 blocking conditions

Formal `## Threat Flags` sections were not present in the Phase 05.1 summaries.
The review-fix artifact and verification report were reviewed as additional
current-tree context.

Implementation files were read only. Only this security artifact was updated.

## Current-Tree Review-Fix Recheck

| Review Fix | Trust-Boundary Mapping | Result | Evidence |
|------------|------------------------|--------|----------|
| Contract version bump to `2.0.0` and v2 schema/examples | T-05.1-01-01, T-05.1-04-02 | CLOSED | `crates/parser-contract/src/version.rs:6`, `crates/parser-contract/src/version.rs:20`, `crates/parser-contract/tests/version_contract.rs:47`, `crates/parser-contract/tests/schema_contract.rs:22`, `crates/parser-cli/tests/schema_command.rs:28`; only v2 schema/example files are present under `schemas/` and `crates/parser-contract/examples/`. |
| Selective parse-only benchmark evidence | T-05.1-02-01, T-05.1-06-01 | CLOSED | `parse_only_compact_decode` calls `decode_compact_root` in `crates/parser-harness/benches/parser_pipeline.rs:56`; regression test rejects `from_slice::<Value>` in `crates/parser-harness/tests/benchmark_report.rs:131`. Grep found no `from_slice::<Value>` hot-path decode in checked parser-core/bench files. |
| Benchmark evidence separation and curated parity classification | T-05.1-05-01, T-05.1-06-01 | CLOSED | Small-CI selected evidence is always `parity_status: not_run` and `ten_x_status: unknown` in `scripts/benchmark-phase5.sh:400` and `scripts/benchmark-phase5.sh:423`; optional curated evidence is separate under `old_new_command` in `scripts/benchmark-phase5.sh:474`; curated parity filters to legacy-equivalent surfaces in `scripts/benchmark-phase5.sh:241`. |
| SteamID alias source refs | T-05.1-02-02, T-05.1-03-02 | CLOSED | `RawEntityCompact` stores the matched alias key in `crates/parser-core/src/raw_compact.rs:238` and `crates/parser-core/src/raw_compact.rs:254`; `entity_steam_id` emits that key in the JSON path in `crates/parser-core/src/entities.rs:674`; regression assertions cover `steamID`, `steamId`, and `steam_id` in `crates/parser-core/tests/entity_normalization.rs:153`. |
| Comparison top-diff filtering | T-05.1-05-01 | CLOSED | Compatible findings are filtered out of `top_diffs` in `crates/parser-harness/src/summary_report.rs:65`; empty actionable diffs render `- None` in `crates/parser-harness/src/summary_report.rs:121`; regression test covers the compatible-only case in `crates/parser-harness/tests/comparison_report.rs:274`. |

## Verification Commands

Targeted commands run during this re-audit:

```text
cargo test -p parser-contract --locked
cargo test -p parser-core entity_normalization_should_preserve_steam_id_presence_and_missing_state_in_participants --locked
cargo test -p parser-harness --test benchmark_report --test comparison_report --locked
cargo test -p parser-cli --test schema_command --test compare_command --locked
cargo run -p parser-harness --bin benchmark-report-check --locked -- --report .planning/generated/phase-05/benchmarks/benchmark-report.json
git status --short
```

Results: all targeted Cargo tests passed. Benchmark report validation passed
structurally and reported `selected_ten_x_status=Unknown`,
`selected_parity_status=NotRun`, and `whole_list_or_corpus_evidence=false`.
`git status --short` is not clean because the review fixes are still present in
the current working tree; no implementation files were modified by this audit.

## Threat Verification

| Threat ID | Category | Disposition | Status | Evidence |
|-----------|----------|-------------|--------|----------|
| T-05.1-00-01 | Tampering | mitigate | CLOSED | Compatibility gate exists before compact contract code changes with `approval_status: accepted_by_user` in `05.1-SERVER-COMPATIBILITY.md:4`; breaking field changes and preserved boundaries are recorded in `05.1-SERVER-COMPATIBILITY.md:34` and `05.1-SERVER-COMPATIBILITY.md:69`; Plan 00 summary records the gate in `05.1-00-SUMMARY.md:44`. |
| T-05.1-00-02 | Repudiation | mitigate | CLOSED | User acceptance is recorded in `05.1-SERVER-COMPATIBILITY.md:4` and `05.1-SERVER-COMPATIBILITY.md:107`; Plan 00 summary records explicit approval in `05.1-00-SUMMARY.md:46` and `05.1-00-SUMMARY.md:59`. |
| T-05.1-01-01 | Tampering | mitigate | CLOSED | `ParseArtifact` exposes compact `participants`, `facts`, `summaries`, and `side_facts` in `crates/parser-contract/src/artifact.rs:48`; schema tests require compact fields and reject removed top-level `entities`, `events`, and `aggregates` in `crates/parser-contract/tests/schema_contract.rs:86` and `crates/parser-contract/tests/schema_contract.rs:101`; contract version is `2.0.0` in `crates/parser-contract/src/version.rs:6`. |
| T-05.1-01-02 | Repudiation | mitigate | CLOSED | Status/failure invariant is implemented in `crates/parser-contract/src/artifact.rs:73`; failure contract tests require failed artifacts to carry failure payloads and reject invalid status/failure combinations in `crates/parser-contract/tests/failure_contract.rs:166`, `crates/parser-contract/tests/failure_contract.rs:197`, and `crates/parser-contract/tests/failure_contract.rs:220`. |
| T-05.1-02-01 | Denial of Service | mitigate | CLOSED | Normal parser entrypoint calls `decode_compact_root` in `crates/parser-core/src/artifact.rs:36`; selective root decode targets `RawOcapRoot` in `crates/parser-core/src/raw_compact.rs:142`; parse-only benchmark also calls `decode_compact_root` in `crates/parser-harness/benches/parser_pipeline.rs:56`; grep found no `from_slice::<Value>` hot-path decode in checked parser-core/bench files. |
| T-05.1-02-02 | Tampering | mitigate | CLOSED | Selective extraction preserves entity/event JSON coordinates in `crates/parser-core/src/raw_compact.rs:333`, `crates/parser-core/src/raw_compact.rs:445`, and `crates/parser-core/src/raw_compact.rs:450`; compact combat facts carry event source refs and rule IDs in `crates/parser-core/src/artifact.rs:65`; tests assert frame, event index, JSON path, and rule ID in `crates/parser-core/tests/combat_event_semantics.rs:201`. |
| T-05.1-03-01 | Information Disclosure | mitigate | CLOSED | Default artifact root omits full `entities` and `events` in `crates/parser-core/tests/deterministic_output.rs:148`; CLI parse tests reject removed top-level `entities`, `events`, and `aggregates` in `crates/parser-cli/tests/parse_command.rs:53`; schema tests reject the same removed top-level fields in `crates/parser-contract/tests/schema_contract.rs:101`. |
| T-05.1-03-02 | Tampering | mitigate | CLOSED | Compact facts carry source refs and rule IDs in `crates/parser-core/src/artifact.rs:81`; contribution source refs/rule IDs are tested in `crates/parser-core/tests/aggregate_projection.rs:351`; contribution and projection ordering are tested in `crates/parser-core/tests/deterministic_output.rs:112` and `crates/parser-core/tests/deterministic_output.rs:132`; SteamID alias source refs now preserve observed alias paths in `crates/parser-core/tests/entity_normalization.rs:153`. |
| T-05.1-03-03 | Repudiation | mitigate | CLOSED | Bounty exclusions for teamkill/suicide/null killer/vehicle victim are tested in `crates/parser-core/tests/combat_event_semantics.rs:132`; final bounty points are absent in `crates/parser-core/tests/aggregate_projection.rs:270`; vehicle-score penalty, denominator, no-final-score, and source-ref evidence are tested in `crates/parser-core/tests/vehicle_score.rs:212`, `crates/parser-core/tests/vehicle_score.rs:231`, `crates/parser-core/tests/vehicle_score.rs:257`, and `crates/parser-core/tests/vehicle_score.rs:270`. |
| T-05.1-04-01 | Spoofing | accept | CLOSED | Accepted risk is documented below. Code evidence matches the acceptance scope: CLI uses caller-provided `replay_id` and computes local SHA-256 in `crates/parser-cli/src/main.rs:209`; worker checksum verification remains Phase 6 per `README.md:64` and `05.1-SERVER-COMPATIBILITY.md:74`. |
| T-05.1-04-02 | Tampering | mitigate | CLOSED | Schema command emits `parse_artifact_schema` in `crates/parser-cli/src/main.rs:235`; CLI schema tests require compact schema symbols and byte-compare exported schema to `schemas/parse-artifact-v2.schema.json` in `crates/parser-cli/tests/schema_command.rs:46` and `crates/parser-cli/tests/schema_command.rs:98`. |
| T-05.1-05-01 | Repudiation | mitigate | CLOSED | Markdown comparison summary records top diffs and next action for `human_review` findings in `crates/parser-harness/src/summary_report.rs:79` and `crates/parser-harness/src/summary_report.rs:139`; compatible findings are excluded from top diffs in `crates/parser-harness/src/summary_report.rs:65`; regression tests cover both human-review and compatible-only paths in `crates/parser-harness/tests/comparison_report.rs:255` and `crates/parser-harness/tests/comparison_report.rs:274`. |
| T-05.1-05-02 | Information Disclosure | accept | CLOSED | Accepted risk is documented below. Detailed JSON is written only when Markdown `--detail-output` is requested or JSON format is explicitly selected in `crates/parser-cli/src/main.rs:276`; tests assert explicit detail output and reject duplicate detailed outputs in `crates/parser-cli/tests/compare_command.rs:138` and `crates/parser-cli/tests/compare_command.rs:197`. |
| T-05.1-06-01 | Repudiation | mitigate | CLOSED | Benchmark report validation requires selected evidence plus either whole-list/corpus evidence or a concrete unavailable reason in `crates/parser-harness/src/benchmark_report.rs:266`; tests reject missing whole-list evidence in `crates/parser-harness/tests/benchmark_report.rs:81`; generated report records `RUN_PHASE5_FULL_CORPUS not enabled` in `.planning/generated/phase-05/benchmarks/benchmark-report.json:48`; optional curated old/new evidence is separated under `old_new_command` in `scripts/benchmark-phase5.sh:474`. |
| T-05.1-06-02 | Tampering | mitigate | CLOSED | Artifact-size evidence fields exist in `crates/parser-harness/src/benchmark_report.rs:79`; validation rejects zero raw/artifact bytes and invalid ratios in `crates/parser-harness/src/benchmark_report.rs:163` and `crates/parser-harness/src/benchmark_report.rs:174`; tests reject missing artifact bytes and bad ratios in `crates/parser-harness/tests/benchmark_report.rs:47` and `crates/parser-harness/tests/benchmark_report.rs:66`. |
| T-05.1-07-01 | Repudiation | mitigate | CLOSED | README and STATE preserve the failed acceptance state instead of overclaiming: `README.md:13`, `README.md:64`, and `.planning/STATE.md:152` state unknown 10x, parity not run, missing whole-list evidence, and Phase 6 blocking. |
| T-05.1-07-02 | Tampering | mitigate | CLOSED | Roadmap and state reflect 8/8 executed with Phase 6 blocked in `.planning/ROADMAP.md:210`, `.planning/ROADMAP.md:217`, `.planning/STATE.md:28`, and `.planning/STATE.md:150`. |

## Accepted Risks Log

| Threat ID | Accepted Risk | Boundary |
|-----------|---------------|----------|
| T-05.1-04-01 | Local CLI parse can use a caller-provided replay ID and only computes a local SHA-256. This is accepted for local CLI reproducibility; production object checksum verification belongs to Phase 6 worker integration. | Do not treat CLI `replay_id` as authenticated replay identity. `server-2`/worker checksum flow remains the production trust boundary. |
| T-05.1-05-02 | Detailed JSON comparison evidence can disclose detailed mismatch surfaces when explicitly requested. | Default comparison output is summary Markdown; detailed JSON requires `--detail-output` or `--format json`. |

## Unregistered Flags

None. No formal `## Threat Flags` sections were present in Phase 05.1 summaries,
and the review fixes map to existing declared threats rather than introducing a
new unmapped security concern.

## Findings

BLOCKER: none. No declared mitigation is absent.

WARNING: none. No unregistered security flag was found.

## Residual Non-Security Acceptance Gap

- Phase 6 remains blocked. The current benchmark report is structurally valid
  but not acceptance evidence: selected `ten_x_status=unknown`, selected
  `parity_status=not_run`, no whole-list/corpus evidence because
  `RUN_PHASE5_FULL_CORPUS` was not enabled, and selected artifact/raw ratio
  `59.97366881` on the tiny CI fixture. Evidence: `.planning/generated/phase-05/benchmarks/benchmark-report.json:44`,
  `.planning/generated/phase-05/benchmarks/benchmark-report.json:48`,
  `README.md:13`, and `.planning/STATE.md:152`.
- Compact contract compatibility was accepted from brief-level downstream
  evidence. No implemented `server-2` parser-ingestion schema was available;
  `05.1-SERVER-COMPATIBILITY.md:31` and `05.1-SERVER-COMPATIBILITY.md:90`
  document that residual integration risk.
- Phase 05.1 does not implement production worker checksum verification,
  RabbitMQ/S3 delivery, PostgreSQL persistence, public APIs, canonical identity,
  replay discovery, UI, or yearly nomination behavior. Those remain outside
  this parser phase per `README.md:13` and `05.1-SERVER-COMPATIBILITY.md:77`.

## Open Threats

None.
