---
phase: 05.1-compact-artifact-and-selective-parser-redesign
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-SERVER-COMPATIBILITY.md
autonomous: false
requirements:
  - OUT-09
  - OUT-10
requirements_addressed: [OUT-09, OUT-10]
user_setup:
  - "Approve the compatibility review checkpoint before Plan 01 changes the public parser artifact contract if only briefs are available as downstream evidence."
must_haves:
  truths:
    - "D-09: parser contract changes have an explicit server-2 compatibility review before implementation starts."
    - "D-01: the review records this as a breaking compact contract/schema redesign."
    - "D-03 and D-07: the review states that top-level full `entities` and `events` dumps are removed from the supported default artifact."
    - "D-11 through D-15: the review records that parser emits compact facts only; server-2 owns final bounty and cross-replay calculations."
  artifacts:
    - path: ".planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-SERVER-COMPATIBILITY.md"
      provides: "Downstream compatibility and approval gate"
      contains: "approval_status"
  key_links:
    - from: ".planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-CONTEXT.md"
      to: ".planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-SERVER-COMPATIBILITY.md"
      via: "D-09"
      pattern: "server-2 compatibility review"
---

<objective>
Record the cross-application compatibility review that must happen before the compact artifact implementation begins.

Purpose: prevent Phase 5.1 from breaking the future `server-2` ingestion contract without an explicit review trail.
Output: `05.1-SERVER-COMPATIBILITY.md` and a human approval checkpoint when only brief-level downstream evidence exists.
</objective>

<execution_context>
@/home/alexandr/.codex/get-shit-done/workflows/execute-plan.md
@/home/alexandr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-CONTEXT.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-RESEARCH.md
@gsd-briefs/server-2.md
@gsd-briefs/replay-parser-2.md
@gsd-briefs/replays-fetcher.md
@gsd-briefs/web.md
@/home/alexandr/Projects/SolidGames/server-2/gsd-briefs/server-2.md
@/home/alexandr/Projects/SolidGames/server-2/gsd-briefs/replay-parser-2.md
@/home/alexandr/Projects/SolidGames/web/gsd-briefs/web.md
@/home/alexandr/Projects/SolidGames/replays-fetcher/gsd-briefs/replays-fetcher.md
@AGENTS.md
</context>

<decision_coverage>
- D-01, D-03, D-07, D-09: breaking compact contract review before implementation.
- D-11, D-12, D-13, D-14, D-15: server calculation and raw replay reprocessing boundaries.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Write compact artifact compatibility review</name>
  <files>.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-SERVER-COMPATIBILITY.md</files>
  <read_first>
    - `.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-CONTEXT.md`
    - `gsd-briefs/server-2.md`
    - `gsd-briefs/replay-parser-2.md`
    - `gsd-briefs/replays-fetcher.md`
    - `gsd-briefs/web.md`
    - `/home/alexandr/Projects/SolidGames/server-2/gsd-briefs/server-2.md`
    - `/home/alexandr/Projects/SolidGames/server-2/gsd-briefs/replay-parser-2.md`
  </read_first>
  <action>
    Create `05.1-SERVER-COMPATIBILITY.md` with these exact sections:

    - `## Review Scope`
    - `## Evidence Read`
    - `## Breaking Artifact Changes`
    - `## server-2 Ingestion Expectations`
    - `## Boundaries Preserved`
    - `## Compatibility Risks`
    - `## Approval`

    In `## Breaking Artifact Changes`, list:

    - removed default top-level fields: `entities`, `events`
    - added compact fields planned by Phase 5.1: `participants`, `facts`, `summaries`
    - preserved envelope fields: `contract_version`, `parser`, `source`, `status`, `produced_at`, `diagnostics`, `replay`, `side_facts`, `failure`, `extensions`

    In `## server-2 Ingestion Expectations`, state that `server-2` can persist compact participant refs, combat facts, aggregate contribution refs, side facts, and projections from the artifact reference it receives over RabbitMQ/S3 later in Phase 6.

    In `## Boundaries Preserved`, explicitly state:

    - parser does not calculate final bounty points per D-11
    - parser does not calculate final cross-replay vehicle score per D-14
    - parser preserves observed identity only; canonical matching belongs to `server-2`
    - parser does not write PostgreSQL, public APIs, moderation workflow, UI, replay discovery, raw object staging, or yearly nomination product behavior

    In `## Compatibility Risks`, record whether actual adjacent implementation docs were present. If only `gsd-briefs` exist, set `approval_status: needs_user_acceptance`. If actual `server-2` parser-ingestion schema/docs are found and support the compact shape, set `approval_status: accepted_from_local_evidence`.
  </action>
  <verify>
    <automated>rg -n "approval_status|entities|events|participants|facts|summaries|server-2|canonical matching|final bounty|vehicle score|PostgreSQL" .planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-SERVER-COMPATIBILITY.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `05.1-SERVER-COMPATIBILITY.md` contains `approval_status`.
    - `05.1-SERVER-COMPATIBILITY.md` contains `removed default top-level fields: entities, events`.
    - `05.1-SERVER-COMPATIBILITY.md` contains `participants`, `facts`, and `summaries`.
    - `05.1-SERVER-COMPATIBILITY.md` contains `parser does not calculate final bounty`.
    - `05.1-SERVER-COMPATIBILITY.md` contains `canonical matching belongs to server-2`.
  </acceptance_criteria>
  <done>Compatibility review exists and names the implementation gate state.</done>
</task>

<task type="checkpoint:decision">
  <name>Task 2: Approve compact contract implementation gate if local evidence is insufficient</name>
  <done>
    If `05.1-SERVER-COMPATIBILITY.md` has `approval_status: needs_user_acceptance`, stop before Plan 01 and ask the user to approve proceeding with the compact artifact implementation based on brief-level downstream evidence. If it has `approval_status: accepted_from_local_evidence`, execution may continue without a manual approval pause.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parser contract to `server-2` | Breaking artifact fields cross into backend ingestion expectations. |
| Planning review to implementation | Missing review evidence could let executors change public contract blindly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-00-01 | Tampering | Parser artifact contract | mitigate | Require `05.1-SERVER-COMPATIBILITY.md` before Plan 01 modifies contract code. |
| T-05.1-00-02 | Repudiation | User acceptance | mitigate | Record `approval_status` and checkpoint result in the compatibility artifact or summary. |
</threat_model>

<verification>
- `05.1-SERVER-COMPATIBILITY.md` exists and contains the required sections.
- If `approval_status` is `needs_user_acceptance`, execution pauses before Plan 01.
</verification>

<success_criteria>
- Compatibility review states removed fields, added compact fields, preserved boundaries, risks, and approval status.
- No code implementation starts before the review gate has a recorded status.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-00-SUMMARY.md`.
</output>
