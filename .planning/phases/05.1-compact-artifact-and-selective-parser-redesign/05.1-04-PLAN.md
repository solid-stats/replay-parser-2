---
phase: 05.1-compact-artifact-and-selective-parser-redesign
plan: 04
type: execute
wave: 5
depends_on:
  - 05.1-03
files_modified:
  - crates/parser-cli/src/main.rs
  - crates/parser-cli/tests/parse_command.rs
  - crates/parser-cli/tests/schema_command.rs
  - crates/parser-contract/tests/schema_contract.rs
  - crates/parser-core/tests/fixtures/golden/manifest.json
  - README.md
autonomous: true
requirements:
  - OUT-09
  - OUT-10
  - TEST-13
requirements_addressed: [OUT-09, OUT-10, TEST-13]
must_haves:
  truths:
    - "D-05: `replay-parser-2 parse` writes the same compact default artifact future worker ingestion will deliver."
    - "D-01: CLI schema output and examples reflect the breaking compact contract."
    - "D-20: normal compact path exists and is exercised through public CLI tests."
    - "D-23: artifact size evidence is available from CLI/harness paths for benchmark reporting."
  artifacts:
    - path: "crates/parser-cli/src/main.rs"
      provides: "Compact CLI parse/schema behavior"
      contains: "parse_replay"
    - path: "crates/parser-cli/tests/parse_command.rs"
      provides: "CLI compact output tests"
      contains: "participants"
    - path: "README.md"
      provides: "Public compact command status"
      contains: "compact"
  key_links:
    - from: "parser_core::parse_replay"
      to: "replay-parser-2 parse"
      via: "same compact artifact"
      pattern: "participants"
---

<objective>
Update CLI parse/schema/golden docs to emit and validate the compact artifact as the default user and worker-facing output.

Purpose: keep local CLI behavior aligned with the future worker artifact and prevent schema/examples from lagging behind the compact contract.
Output: updated CLI tests, schema command checks, fixture manifest references, and README status.
</objective>

<execution_context>
@/home/alexandr/.codex/get-shit-done/workflows/execute-plan.md
@/home/alexandr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-CONTEXT.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-RESEARCH.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-PATTERNS.md
@crates/parser-cli/src/main.rs
@crates/parser-cli/tests/parse_command.rs
@crates/parser-cli/tests/schema_command.rs
@crates/parser-contract/tests/schema_contract.rs
@README.md
@AGENTS.md
</context>

<decision_coverage>
- D-01, D-05, D-08, D-20, D-23: CLI/default schema compactness and artifact-size evidence.
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update parse and schema commands for compact artifact</name>
  <files>crates/parser-cli/src/main.rs, crates/parser-cli/tests/parse_command.rs, crates/parser-cli/tests/schema_command.rs</files>
  <read_first>
    - `crates/parser-cli/src/main.rs`
    - `crates/parser-cli/tests/parse_command.rs`
    - `crates/parser-cli/tests/schema_command.rs`
    - `crates/parser-contract/src/artifact.rs`
    - `crates/parser-contract/src/compact.rs`
  </read_first>
  <behavior>
    - `replay-parser-2 parse` writes compact artifacts by default.
    - `replay-parser-2 schema` exports compact schema with `participants`, `facts`, and `summaries`.
    - Malformed input still writes structured failed compact artifact and exits non-zero.
  </behavior>
  <action>
    Update CLI code only where it references removed fields or schema expectations. Keep command names unchanged:

    - `replay-parser-2 parse <input> --output <path> [--replay-id <id>]`
    - `replay-parser-2 schema [--output <path>]`

    Update parse-command tests to assert output JSON contains top-level `participants`, `facts`, `summaries`, `side_facts`, and `failure`, and does not contain top-level `entities`, `events`, or `aggregates`.

    Update schema-command tests to assert schema output contains `ObservedParticipantRef`, `CombatFact`, `ParseFactSection`, `ParseSummarySection`, `participants`, `facts`, and `summaries`.

    Keep concise stderr failure summaries exactly as current CLI behavior requires.
  </action>
  <verify>
    <automated>cargo test -p parser-cli parse_command schema_command && rg -n "participants|facts|summaries|ObservedParticipantRef|ParseFactSection" crates/parser-cli/tests crates/parser-cli/src/main.rs && ! rg -n "\"entities\"|\"events\"|\"aggregates\"" crates/parser-cli/tests/parse_command.rs && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `parse_command.rs` asserts `"participants"`.
    - `parse_command.rs` asserts `"facts"`.
    - `parse_command.rs` asserts `"summaries"`.
    - `parse_command.rs` asserts top-level `"entities"` is absent.
    - `cargo test -p parser-cli parse_command schema_command` exits 0.
  </acceptance_criteria>
  <done>CLI parse/schema commands exercise compact default output.</done>
</task>

<task type="auto">
  <name>Task 2: Update fixture manifest and README for compact default output</name>
  <files>crates/parser-core/tests/fixtures/golden/manifest.json, README.md</files>
  <read_first>
    - `crates/parser-core/tests/fixtures/golden/manifest.json`
    - `README.md`
    - `.planning/PROJECT.md`
    - `.planning/ROADMAP.md`
    - `.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-CONTEXT.md`
  </read_first>
  <action>
    Update the golden fixture manifest so Phase 5.1 compact fields are traceable:

    - add or update requirement coverage entries for `OUT-09`, `OUT-10`, `PARS-12`, and `TEST-13`
    - add notes that default assertions target `participants`, `facts`, `summaries`, source refs, and absence of top-level `entities`/`events`

    Update README:

    - current status says Phase 5.1 is being planned/executed to replace the default artifact with compact output
    - `replay-parser-2 parse` description says it writes a compact server-facing artifact
    - schema command description says it exports the compact parser contract schema
    - keep worker mode, PostgreSQL persistence, public APIs, canonical identity, replay discovery, UI, and annual/yearly nomination product behavior outside this parser
    - keep the AI agents plus GSD-only development workflow statement
  </action>
  <verify>
    <automated>rg -n "OUT-09|OUT-10|PARS-12|TEST-13|participants|facts|summaries|entities|events" crates/parser-core/tests/fixtures/golden/manifest.json README.md && rg -n "compact server-facing artifact|AI agents using the GSD workflow|not implemented" README.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Fixture manifest contains `OUT-09`.
    - Fixture manifest contains `PARS-12`.
    - README contains `compact server-facing artifact`.
    - README still states project development uses AI agents and GSD.
    - README keeps worker mode as not implemented until Phase 6.
  </acceptance_criteria>
  <done>Docs and fixture manifest reflect compact CLI output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem replay input to CLI | Local untrusted input is parsed and written as compact JSON. |
| CLI schema to downstream users | Schema output is consumed as integration contract evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-04-01 | Spoofing | CLI source metadata | accept | CLI continues using caller-provided replay ID and local SHA-256; worker checksum verification remains Phase 6. |
| T-05.1-04-02 | Tampering | Schema command | mitigate | Schema command tests compare exported schema with committed compact schema. |
</threat_model>

<verification>
- `cargo test -p parser-cli parse_command schema_command`
- `cargo test -p parser-contract schema_contract`
- README/manifest grep gates for compact fields and boundaries.
</verification>

<success_criteria>
- CLI parse writes compact artifact by default.
- CLI schema exports compact contract.
- Documentation no longer implies the default artifact is a full normalized dump.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-04-SUMMARY.md`.
</output>
