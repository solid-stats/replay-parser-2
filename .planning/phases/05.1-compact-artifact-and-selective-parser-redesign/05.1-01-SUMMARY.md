---
phase: 05.1-compact-artifact-and-selective-parser-redesign
plan: 01
subsystem: contract
tags: [parser-contract, compact-artifact, schema, examples]
requires:
  - phase: 05.1-00
    provides: Server compatibility approval for compact artifact implementation
provides:
  - Compact ParseArtifact envelope with participants, facts, and summaries
  - Schema-visible compact participant, combat fact, fact section, and summary section types
  - Refreshed compact success and failure examples
  - Regenerated parse artifact JSON Schema
affects: [parser-contract, parser-core, parser-cli, parser-harness, server-2]
tech-stack:
  added: []
  patterns:
    - Compact public artifact envelope backed by internal rich parser semantics
    - Schema/example freshness gate for contract changes
key-files:
  created:
    - crates/parser-contract/src/compact.rs
  modified:
    - crates/parser-contract/src/artifact.rs
    - crates/parser-contract/src/lib.rs
    - crates/parser-contract/examples/parse_artifact_success.v1.json
    - crates/parser-contract/examples/parse_failure.v1.json
    - crates/parser-contract/tests/artifact_envelope.rs
    - crates/parser-contract/tests/failure_contract.rs
    - crates/parser-contract/tests/schema_contract.rs
    - crates/parser-core/src/artifact.rs
    - schemas/parse-artifact-v1.schema.json
key-decisions:
  - "The public artifact envelope now exposes compact participants, facts, and summaries instead of top-level entities, events, and aggregates."
  - "parser-core keeps internal normalized entities/events/aggregate derivation for now, then maps them into compact contract sections at the boundary."
patterns-established:
  - "Contract examples and committed schema must prove removed full-dump fields are absent from the default artifact."
requirements-completed: [OUT-09, OUT-10]
duration: 0 min
completed: 2026-04-29
---

# Phase 05.1 Plan 01: Compact Contract Envelope Summary

**Compact parser contract envelope with participant refs, combat facts, contribution facts, summaries, and refreshed schema/examples**

## Performance

- **Duration:** 0 min
- **Started:** 2026-04-29T00:00:00Z
- **Completed:** 2026-04-29T00:00:00Z
- **Tasks:** 2
- **Files modified:** 10

## Accomplishments

- Added `ObservedParticipantRef`, `CombatFact`, `ParseFactSection`, and `ParseSummarySection`.
- Replaced public `ParseArtifact` top-level `entities`, `events`, and `aggregates` with `participants`, `facts`, and `summaries`.
- Refreshed success/failure examples and regenerated `schemas/parse-artifact-v1.schema.json`.
- Preserved failed/non-failed status/failure invariants.

## Task Commits

1. **Task 1 RED: compact artifact expectations** - `8b04bad` (test)
2. **Task 1 GREEN: compact artifact envelope** - `c6f251f` (feat)
3. **Task 2: compact schema and examples** - `066cd40` (test)

## Files Created/Modified

- `crates/parser-contract/src/compact.rs` - Compact participant, combat fact, fact section, and summary section types.
- `crates/parser-contract/src/artifact.rs` - Public compact `ParseArtifact` envelope.
- `crates/parser-contract/src/lib.rs` - Exports compact contract module.
- `crates/parser-core/src/artifact.rs` - Maps current internal parser semantics into compact artifact sections.
- `crates/parser-contract/examples/*.json` - Compact success and failure examples.
- `crates/parser-contract/tests/*.rs` - Contract/schema tests for compact fields and removed full-dump fields.
- `schemas/parse-artifact-v1.schema.json` - Fresh generated compact artifact schema.

## Decisions Made

- Kept existing internal normalized entity/event/aggregate derivation in parser-core for this plan, then mapped it into compact public sections. This avoids throwing away Phase 3/4 semantics before Plan 02/03 selective extraction and compact behavior preservation are in place.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Updated parser-core artifact boundary**
- **Found during:** Task 1 (compact artifact contract replacement)
- **Issue:** Removing public `ParseArtifact.entities/events/aggregates` broke parser-core artifact construction.
- **Fix:** Added a boundary mapper from internal normalized entities/events/aggregates into `participants`, `facts`, and `summaries`.
- **Files modified:** `crates/parser-core/src/artifact.rs`
- **Verification:** `cargo check -p parser-core`
- **Committed in:** `c6f251f`

---

**Total deviations:** 1 auto-fixed blocking issue.
**Impact on plan:** Contract cutline remains compact while preserving existing internal semantics for the next plans.

## Issues Encountered

- `cargo test -p parser-core --no-run` still fails because existing parser-core integration tests reference removed public artifact fields. That is expected follow-on work for Plan 03, where compact participant/fact/summary behavior replaces those test surfaces.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 02 can replace the normal parser hot path with selective extraction. It must preserve the Plan 01 boundary mapping and avoid regressing compact `facts` and `summaries` to empty placeholders.

---
*Phase: 05.1-compact-artifact-and-selective-parser-redesign*
*Completed: 2026-04-29*
