---
phase: 05.1-compact-artifact-and-selective-parser-redesign
plan: 07
type: execute
wave: 7
depends_on:
  - 05.1-04
  - 05.1-05
  - 05.1-06
files_modified:
  - README.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - OUT-09
  - OUT-10
  - PARS-12
  - TEST-06
  - TEST-13
  - TEST-14
requirements_addressed: [OUT-09, OUT-10, PARS-12, TEST-06, TEST-13, TEST-14]
must_haves:
  truths:
    - "All Phase 5.1 requirements OUT-09, OUT-10, PARS-12, TEST-06, TEST-13, and TEST-14 have passing implementation evidence or recorded acceptance gap."
    - "D-20: compact normal path exists, full DOM normal path is removed, quality gates pass, and benchmark report includes selected and whole-list/corpus evidence."
    - "D-22: if 10x still fails, README/STATE/UAT record bottleneck, parity status, and explicit user acceptance requirement before Phase 6."
    - "Phase 6 worker integration remains blocked until compact artifact and selective parser direction are verified or accepted with recorded gaps."
  artifacts:
    - path: "README.md"
      provides: "Final compact artifact handoff"
      contains: "Phase 5.1"
    - path: ".planning/ROADMAP.md"
      provides: "Plan list and wave dependencies"
      contains: "05.1-07-PLAN.md"
    - path: ".planning/STATE.md"
      provides: "Ready-to-execute/verification status"
      contains: "Phase 05.1"
  key_links:
    - from: ".planning/phases/05.1-compact-artifact-and-selective-parser-redesign/*-SUMMARY.md"
      to: "README.md"
      via: "final status handoff"
      pattern: "compact artifact"
---

<objective>
Run final Phase 5.1 gates, update handoff documentation, and preserve the Phase 6 blocking condition until redesign evidence is accepted.

Purpose: leave the repository ready for Phase 5.1 verification or Phase 6 planning only after compact artifact, selective parser, comparison, and benchmark work is proved.
Output: README/state/roadmap handoff and full quality gate evidence.
</objective>

<execution_context>
@/home/alexandr/.codex/get-shit-done/workflows/execute-plan.md
@/home/alexandr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-CONTEXT.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-RESEARCH.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-VALIDATION.md
@.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-SERVER-COMPATIBILITY.md
@README.md
@AGENTS.md
</context>

<decision_coverage>
- D-01 through D-25: final docs and gates confirm every compact artifact, selective parser, benchmark, comparison, and boundary decision remains represented.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Run final compact artifact and selective parser quality gates</name>
  <files>README.md</files>
  <read_first>
    - `.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-VALIDATION.md`
    - `scripts/coverage-gate.sh`
    - `scripts/fault-report-gate.sh`
    - `scripts/benchmark-phase5.sh`
    - `README.md`
  </read_first>
  <action>
    Run the final Phase 5.1 gates and record outcomes in the plan summary:

    ```bash
    cargo fmt --all -- --check
    cargo clippy --workspace --all-targets -- -D warnings
    cargo test --workspace
    cargo doc --workspace --no-deps
    scripts/coverage-gate.sh --check
    scripts/fault-report-gate.sh
    scripts/benchmark-phase5.sh --ci
    git diff --check
    ```

    Also run boundary grep checks:

    ```bash
    ! rg -n "from_slice::<Value>|from_str::<Value>" crates/parser-core/src/artifact.rs crates/parser-core/src/raw.rs crates/parser-core/src/raw_compact.rs
    ! rg -n "artifact\\.events|artifact\\.entities|artifact\\.aggregates" crates/parser-core/src crates/parser-core/tests crates/parser-cli/tests
    ```

    If any gate fails, fix the failure within the owning plan's files before updating README. If the benchmark still records `ten_x_status=fail` or `parity_status=human_review`, record bottleneck, parity status, artifact ratio, and required user acceptance in README and the summary rather than claiming Phase 5.1 success.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace && cargo doc --workspace --no-deps && scripts/coverage-gate.sh --check && scripts/fault-report-gate.sh && scripts/benchmark-phase5.sh --ci && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Workspace format, clippy, tests, and docs pass.
    - Coverage gate passes or produces a recorded accepted gap.
    - Fault gate passes or produces a recorded accepted gap.
    - Benchmark gate writes compact artifact size evidence.
    - Boundary grep finds no normal-path full DOM decode.
  </acceptance_criteria>
  <done>Final quality gates have current evidence for Phase 5.1.</done>
</task>

<task type="auto">
  <name>Task 2: Update README, ROADMAP, and STATE for Phase 5.1 handoff</name>
  <files>README.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - `README.md`
    - `.planning/ROADMAP.md`
    - `.planning/STATE.md`
    - `.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-CONTEXT.md`
    - `.planning/generated/phase-05/benchmarks/benchmark-report.json`
  </read_first>
  <action>
    Update README after gates:

    - state that Phase 5.1 implemented or is verifying compact artifact output, selective parser path, summary-first comparison, and compact benchmark evidence according to actual gate outcome
    - document current compact fields: `participants`, `facts`, `summaries`, `side_facts`, `diagnostics`, `failure`
    - state default output no longer includes top-level full `entities` or `events`
    - document benchmark evidence fields: raw input bytes, compact artifact bytes, artifact/raw ratio, selected workload, whole-list/corpus evidence or unavailable rationale, parity status, and 10x status
    - if 10x/parity is not accepted, state Phase 6 remains blocked pending user acceptance or remediation
    - keep AI agents plus GSD-only development workflow statement

    Update `.planning/ROADMAP.md` Phase 5.1:

    - set `Plans: 8 plans`
    - add execution waves:
      - Wave 1: `05.1-00-PLAN.md`
      - Wave 2: `05.1-01-PLAN.md`
      - Wave 3: `05.1-02-PLAN.md`
      - Wave 4: `05.1-03-PLAN.md`
      - Wave 5: `05.1-04-PLAN.md`, `05.1-05-PLAN.md`
      - Wave 6: `05.1-06-PLAN.md`
      - Wave 7: `05.1-07-PLAN.md`
    - add cross-cutting constraints summarizing compact default artifact, no full DOM normal path, no final bounty calculation, and server-2 compatibility gate

    Update `.planning/STATE.md` current position to show Phase 05.1 has 8 plans and is ready for execution or verification according to actual gate outcome.
  </action>
  <verify>
    <automated>rg -n "participants|facts|summaries|side_facts|compact artifact bytes|artifact/raw ratio|Phase 5\\.1|AI agents using the GSD workflow" README.md && rg -n "05\\.1-00-PLAN|05\\.1-07-PLAN|Wave 5|Plans: 8 plans|server-2 compatibility" .planning/ROADMAP.md && rg -n "Phase 05\\.1|8 plans|ready" .planning/STATE.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - README contains `participants`, `facts`, and `summaries`.
    - README contains `artifact/raw ratio`.
    - ROADMAP contains `05.1-07-PLAN.md`.
    - ROADMAP contains `Plans: 8 plans`.
    - STATE contains `Phase 05.1`.
    - `git diff --check` exits 0.
  </acceptance_criteria>
  <done>Project handoff docs reflect Phase 5.1 reality and Phase 6 readiness/blockers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Final gate evidence to phase handoff | README/STATE must not overclaim unverified performance or parity. |
| Phase 5.1 to Phase 6 | Worker integration depends on accepted compact artifact shape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-07-01 | Repudiation | Benchmark and parity handoff | mitigate | README/summary must record fail/human_review status and required acceptance instead of claiming pass. |
| T-05.1-07-02 | Tampering | Roadmap state | mitigate | Roadmap and STATE updates are grep-verified against plan list and phase status. |
</threat_model>

<verification>
- Full workspace quality, coverage, fault, benchmark, and boundary grep gates.
- README, ROADMAP, and STATE grep gates.
</verification>

<success_criteria>
- All six Phase 5.1 requirements are covered by implemented plans and final gates.
- Phase 6 remains blocked unless compact artifact and selective parser evidence passes or accepted gaps are recorded.
- Public docs accurately describe compact artifact status and remaining limitations.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-compact-artifact-and-selective-parser-redesign/05.1-07-SUMMARY.md`.
</output>
