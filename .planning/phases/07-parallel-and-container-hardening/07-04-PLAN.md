---
phase: 07-parallel-and-container-hardening
plan: 04
type: execute
wave: 5
depends_on:
  - 07-00
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - README.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/07-parallel-and-container-hardening/07-HUMAN-UAT.md
autonomous: true
requirements:
  - WORK-08
  - WORK-09
requirements_addressed: [WORK-08, WORK-09]
must_haves:
  truths:
    - "D-01 through D-13: final docs and evidence reflect every locked Phase 7 decision."
    - "WORK-08 is complete only when multi-worker artifact/idempotency safety is proven by no-network tests and two-worker smoke."
    - "WORK-09 is complete only when structured logs and live/ready probes are implemented, tested, and documented."
    - "README.md remains current and states project development uses only AI agents plus GSD workflow."
---

<objective>
Run final Phase 7 quality gates, record UAT evidence, and update README/ROADMAP/REQUIREMENTS/STATE so the milestone accurately reflects parallel and container hardening completion.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-parallel-and-container-hardening/07-CONTEXT.md
@.planning/phases/07-parallel-and-container-hardening/07-RESEARCH.md
@.planning/phases/07-parallel-and-container-hardening/07-VALIDATION.md
@.planning/phases/07-parallel-and-container-hardening/07-PATTERNS.md
@.planning/phases/06-rabbitmq-s3-worker-integration/06-05-SUMMARY.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
@AGENTS.md
</context>

<decision_coverage>
- D-01 through D-13.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Run final Phase 7 gates and boundary checks</name>
  <files>.planning/phases/07-parallel-and-container-hardening/07-HUMAN-UAT.md</files>
  <read_first>
    - `Cargo.toml`
    - `crates/parser-worker/src/lib.rs`
    - `crates/parser-worker/src/storage.rs`
    - `crates/parser-worker/src/health.rs`
    - `crates/parser-worker/src/logging.rs`
    - `crates/parser-cli/src/main.rs`
    - `Dockerfile`
    - `docker-compose.worker-smoke.yml`
    - `scripts/worker-smoke.sh`
    - `scripts/coverage-gate.sh`
    - `scripts/fault-report-gate.sh`
    - `scripts/benchmark-phase5.sh`
  </read_first>
  <action>
    Run and fix all final gates after Plans 00-03 are implemented:

    - `cargo fmt --all -- --check`
    - `cargo clippy --workspace --all-targets -- -D warnings`
    - `cargo test --workspace`
    - `cargo doc --workspace --no-deps`
    - `scripts/coverage-gate.sh --check`
    - `scripts/fault-report-gate.sh`
    - `scripts/benchmark-phase5.sh --ci`
    - `cargo test -p parser-worker`
    - `cargo test -p parser-cli worker_command healthcheck`
    - `scripts/worker-smoke.sh`
    - `git diff --check`

    Add boundary grep checks:

    - `! rg -n "lapin|aws_sdk_s3|tokio::signal|axum|hyper|/livez|/readyz|HEALTHCHECK" crates/parser-core crates/parser-contract`
    - `! rg -n "parse_replay_debug" crates/parser-worker`
    - `! rg -n "AWS_SECRET_ACCESS_KEY=.*[^*]|AWS_SESSION_TOKEN=.*[^*]|amqp://[^\\s]*:[^*@\\s]+@" README.md crates/parser-worker crates/parser-cli Dockerfile docker-compose.worker-smoke.yml scripts/worker-smoke.sh`
    - `rg -n "worker-a|worker-b|worker_artifact_reused|/livez|/readyz|HEALTHCHECK|Timeweb|s3\\.twcstorage\\.ru" README.md Dockerfile docker-compose.worker-smoke.yml scripts/worker-smoke.sh crates/parser-worker`

    Create `.planning/phases/07-parallel-and-container-hardening/07-HUMAN-UAT.md` with:

    - frontmatter `status: complete`, `phase: 07-parallel-and-container-hardening`, and current timestamp;
    - a test entry for two-worker Docker Compose smoke;
    - a test entry for probe readiness/liveness;
    - a test entry for duplicate artifact reuse and conflict behavior;
    - a test entry for structured log taxonomy and worker IDs;
    - a Timeweb compatibility note that records whether live Timeweb credentials were available. If not available, record `pending: external credentials not supplied` and cite the fallback compare/reuse/conflict behavior.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace && cargo doc --workspace --no-deps && scripts/coverage-gate.sh --check && scripts/fault-report-gate.sh && scripts/benchmark-phase5.sh --ci && cargo test -p parser-worker && cargo test -p parser-cli worker_command healthcheck && scripts/worker-smoke.sh && ! rg -n "lapin|aws_sdk_s3|tokio::signal|axum|hyper|/livez|/readyz|HEALTHCHECK" crates/parser-core crates/parser-contract && ! rg -n "parse_replay_debug" crates/parser-worker && ! rg -n "AWS_SECRET_ACCESS_KEY=.*[^*]|AWS_SESSION_TOKEN=.*[^*]|amqp://[^\\s]*:[^*@\\s]+@" README.md crates/parser-worker crates/parser-cli Dockerfile docker-compose.worker-smoke.yml scripts/worker-smoke.sh && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0.
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0.
    - `cargo test --workspace` exits 0.
    - `scripts/coverage-gate.sh --check` exits 0.
    - `scripts/fault-report-gate.sh` exits 0.
    - `scripts/worker-smoke.sh` exits 0.
    - `.planning/phases/07-parallel-and-container-hardening/07-HUMAN-UAT.md` contains `two-worker`.
    - `.planning/phases/07-parallel-and-container-hardening/07-HUMAN-UAT.md` contains `Timeweb`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update README and planning handoff</name>
  <files>README.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - `README.md`
    - `.planning/ROADMAP.md`
    - `.planning/REQUIREMENTS.md`
    - `.planning/STATE.md`
    - `.planning/phases/07-parallel-and-container-hardening/07-HUMAN-UAT.md`
    - `.planning/phases/07-parallel-and-container-hardening/07-CONTEXT.md`
    - `.planning/phases/07-parallel-and-container-hardening/07-VALIDATION.md`
  </read_first>
  <action>
    Update docs only after final gates pass:

    README must document:

    - `replay-parser-2 worker` supports HTTP `/livez` and `/readyz` probes;
    - probe env/flags `REPLAY_PARSER_PROBE_BIND`, `REPLAY_PARSER_PROBE_PORT`, `REPLAY_PARSER_PROBES_ENABLED`, and `REPLAY_PARSER_WORKER_ID`;
    - readiness means valid config, AMQP connected/consumer active, result publisher path active, S3 reachable, and shutdown not requested;
    - liveness does not fail for ordinary RabbitMQ or S3 outages;
    - default prefetch remains `1`;
    - multiple worker containers are supported through deterministic artifact write/reuse/conflict behavior;
    - duplicate/redelivered jobs with matching artifacts publish completed outcomes idempotently;
    - Docker image remains slim/non-root and has health wiring;
    - `scripts/worker-smoke.sh` builds/runs two worker containers and validates probes, duplicate jobs, artifact reuse/conflict, and structured logs;
    - Timeweb Cloud S3 config uses `s3.twcstorage.ru`, path-style setting, AWS SDK credentials, and conditional-write fallback when needed;
    - project development uses only AI agents plus GSD workflow.

    Update ROADMAP/REQUIREMENTS/STATE:

    - mark Phase 7 plans complete after evidence exists;
    - mark `WORK-08` and `WORK-09` complete/validated;
    - record final command evidence and any Timeweb credential caveat;
    - preserve out-of-scope notes for full-corpus multi-worker stress, in-process task pools, OpenTelemetry exporter, metrics stack, dashboards, production Kubernetes manifests, public UI/API, PostgreSQL, canonical identity, replay discovery, bounty payout, and yearly stats.
  </action>
  <verify>
    <automated>rg -n "WORK-08|WORK-09|/livez|/readyz|REPLAY_PARSER_PROBE_BIND|REPLAY_PARSER_WORKER_ID|prefetch.*1|two worker|worker-a|worker-b|Timeweb|s3\\.twcstorage\\.ru|AI agents plus GSD" README.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `/livez`.
    - `README.md` contains `/readyz`.
    - `README.md` contains `REPLAY_PARSER_WORKER_ID`.
    - `README.md` contains `scripts/worker-smoke.sh`.
    - `.planning/REQUIREMENTS.md` contains `WORK-08`.
    - `.planning/REQUIREMENTS.md` contains `WORK-09`.
    - `.planning/ROADMAP.md` contains `07-04-PLAN.md`.
    - `.planning/STATE.md` contains `Phase 07`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification to phase completion | Phase completion must not be claimed without fresh quality, smoke, boundary, and docs evidence. |
| Documentation to operators and future agents | Incorrect handoff can cause unsafe deployment assumptions. |
| Secret-bearing deployment config to committed docs | README and planning docs must not include real credentials. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Repudiation | Phase completion evidence | mitigate | `07-HUMAN-UAT.md`, README, ROADMAP, REQUIREMENTS, and STATE record final gates and smoke results. |
| T-07-04-02 | Information Disclosure | Docs/examples | mitigate | Secret grep rejects committed AWS secrets and password-bearing AMQP URLs. |
| T-07-04-03 | Elevation of Privilege | Boundary drift | mitigate | Boundary greps reject transport/probe dependencies in parser-core and parser-contract. |
| T-07-04-04 | Denial of Service | Operator expectations | mitigate | Docs preserve prefetch `1`, readiness/liveness semantics, Timeweb fallback, and deferred stress/load scope. |
</threat_model>

<verification>
- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `cargo doc --workspace --no-deps`
- `scripts/coverage-gate.sh --check`
- `scripts/fault-report-gate.sh`
- `scripts/benchmark-phase5.sh --ci`
- `cargo test -p parser-worker`
- `cargo test -p parser-cli worker_command healthcheck`
- `scripts/worker-smoke.sh`
- Boundary greps for parser-core/contract, debug sidecar, secrets, probes/logs/docs
- `git diff --check`
</verification>

<success_criteria>
- WORK-08 and WORK-09 are validated by code tests, Docker smoke, and docs.
- README and planning artifacts accurately describe multi-worker safety, probes, structured logs, container behavior, and Timeweb compatibility caveats.
- Parser-core and parser-contract remain transport-free.
- No secrets or password-bearing examples are committed.
- Repository is ready for milestone completion after Phase 7 verification.
</success_criteria>
