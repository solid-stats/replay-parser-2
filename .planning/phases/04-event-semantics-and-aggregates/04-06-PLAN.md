---
phase: 04-event-semantics-and-aggregates
plan: 06
type: execute
wave: 6
depends_on:
  - 04-04
  - 04-05
files_modified:
  - crates/parser-contract/examples/parse_artifact_success.v1.json
  - schemas/parse-artifact-v1.schema.json
  - crates/parser-contract/tests/schema_contract.rs
  - crates/parser-core/tests/deterministic_output.rs
  - README.md
autonomous: true
requirements:
  - PARS-08
  - PARS-09
  - PARS-10
  - PARS-11
  - AGG-01
  - AGG-02
  - AGG-03
  - AGG-04
  - AGG-05
  - AGG-06
  - AGG-07
  - AGG-08
  - AGG-09
  - AGG-10
  - AGG-11
requirements_addressed: [PARS-08, PARS-09, PARS-10, PARS-11, AGG-01, AGG-02, AGG-03, AGG-04, AGG-05, AGG-06, AGG-07, AGG-08, AGG-09, AGG-10, AGG-11]
must_haves:
  truths:
    - "All Phase 4 requirements PARS-08 through PARS-11 and AGG-01 through AGG-11 are represented in executable parser-contract/parser-core tests."
    - "Contract schema and examples match current Rust types after combat, aggregate, vehicle-score, and side-fact implementation."
    - "Parser-core deterministic output still serializes byte-identically on repeated parses."
    - "README reflects Phase 4 completion only after all Phase 4 implementation and quality gates pass."
    - "No parser code writes PostgreSQL, RabbitMQ, S3, public API, canonical identity, replay discovery, or UI behavior."
  artifacts:
    - path: "README.md"
      provides: "Human-facing Phase 4 handoff"
      contains: "Event Semantics and Aggregates"
    - path: "schemas/parse-artifact-v1.schema.json"
      provides: "Committed schema for updated parse artifact"
      contains: "ReplaySideFacts"
    - path: "crates/parser-core/tests/deterministic_output.rs"
      provides: "Final deterministic output coverage"
      contains: "deterministic_output"
  key_links:
    - from: ".planning/phases/04-event-semantics-and-aggregates/04-VALIDATION.md"
      to: "cargo test --workspace"
      via: "final full-suite gate"
      pattern: "Full suite command"
---

<objective>
Finalize Phase 4 by refreshing schema/examples, proving deterministic non-empty event/aggregate output, updating README, and running full workspace quality gates.

Purpose: leave Phase 4 ready for verification and Phase 5 CLI/parity planning.
Output: fresh schema/example artifacts, README handoff, deterministic tests, and full gate evidence.
</objective>

<execution_context>
@/home/alexandr/.codex/get-shit-done/workflows/execute-plan.md
@/home/alexandr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-event-semantics-and-aggregates/04-CONTEXT.md
@.planning/phases/04-event-semantics-and-aggregates/04-RESEARCH.md
@.planning/phases/04-event-semantics-and-aggregates/04-PATTERNS.md
@.planning/phases/04-event-semantics-and-aggregates/04-VALIDATION.md
@crates/parser-contract/examples/parse_artifact_success.v1.json
@crates/parser-contract/tests/schema_contract.rs
@crates/parser-core/tests/deterministic_output.rs
@README.md
@gsd-briefs/replay-parser-2.md
@gsd-briefs/server-2.md
@gsd-briefs/web.md
@/home/alexandr/Projects/SolidGames/replays-fetcher/.planning/PROJECT.md
@AGENTS.md
</context>

<decision_coverage>
- D-01 through D-16: final schema, examples, deterministic tests, README, and cross-app boundary check confirm all decisions remain represented.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Refresh committed schema and success example after all Phase 4 fields are populated</name>
  <files>crates/parser-contract/examples/parse_artifact_success.v1.json, schemas/parse-artifact-v1.schema.json, crates/parser-contract/tests/schema_contract.rs</files>
  <read_first>
    - `crates/parser-contract/examples/parse_artifact_success.v1.json`
    - `crates/parser-contract/examples/export_schema.rs`
    - `crates/parser-contract/tests/schema_contract.rs`
    - `schemas/parse-artifact-v1.schema.json`
  </read_first>
  <action>
    Update `parse_artifact_success.v1.json` to reflect final Phase 4 populated shapes:

    - at least one combat event with source refs, `combat.semantic`, vehicle context, bounty eligibility, and legacy effects
    - aggregate contributions for legacy counter, relationship, bounty input, and vehicle score input
    - projections for `legacy.player_game_results`, `legacy.relationships`, `legacy.game_type_compatibility`, `legacy.squad_inputs`, `legacy.rotation_inputs`, `bounty.inputs`, `vehicle_score.inputs`, and `vehicle_score.denominator_inputs`
    - populated `side_facts` with `commanders` array and `outcome`

    Regenerate schema:

    ```bash
    cargo run -p parser-contract --example export_schema > schemas/parse-artifact-v1.schema.json
    ```

    Extend `schema_contract.rs` if needed so schema text checks include all final projection and type names:

    - `CombatEventAttributes`
    - `VehicleScoreInputValue`
    - `ReplaySideFacts`
    - `side_facts`
    - `legacy.player_game_results`
    - `vehicle_score.denominator_inputs`
  </action>
  <verify>
    <automated>cargo run -p parser-contract --example export_schema > /tmp/parse-artifact-v1.schema.json && cmp /tmp/parse-artifact-v1.schema.json schemas/parse-artifact-v1.schema.json && cargo test -p parser-contract schema_contract && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `parse_artifact_success.v1.json` contains `"legacy.player_game_results"`.
    - `parse_artifact_success.v1.json` contains `"vehicle_score.denominator_inputs"`.
    - `parse_artifact_success.v1.json` contains `"side_facts"`.
    - `schemas/parse-artifact-v1.schema.json` matches freshly generated schema.
    - `cargo test -p parser-contract schema_contract` exits 0.
  </acceptance_criteria>
  <done>Committed contract artifacts match final Phase 4 Rust types.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen deterministic output tests for populated Phase 4 artifacts</name>
  <files>crates/parser-core/tests/deterministic_output.rs</files>
  <read_first>
    - `crates/parser-core/tests/deterministic_output.rs`
    - `crates/parser-core/tests/fixtures/aggregate-combat.ocap.json`
    - `crates/parser-core/src/artifact.rs`
    - `crates/parser-core/src/aggregates.rs`
  </read_first>
  <action>
    Add tests to `deterministic_output.rs` named:

    - `deterministic_output_should_serialize_populated_events_and_aggregates_identically`
    - `deterministic_output_should_keep_aggregate_contributions_sorted_by_id`
    - `deterministic_output_should_keep_projection_keys_sorted`

    Use the Phase 4 aggregate fixture. Assert:

    - parsing the same bytes twice serializes to exactly the same JSON string
    - events are non-empty
    - aggregate contributions are non-empty
    - contribution IDs are sorted ascending
    - projection keys are sorted as a `BTreeMap`
    - `produced_at` remains `None`
  </action>
  <verify>
    <automated>cargo test -p parser-core deterministic_output && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `deterministic_output.rs` contains `deterministic_output_should_serialize_populated_events_and_aggregates_identically`.
    - The new test asserts events are non-empty.
    - The new test asserts aggregate contributions are non-empty.
    - The new test asserts contribution IDs are sorted.
    - `cargo test -p parser-core deterministic_output` exits 0.
  </acceptance_criteria>
  <done>Determinism is proven for non-empty Phase 4 artifacts.</done>
</task>

<task type="auto">
  <name>Task 3: Update README with Phase 4 completion handoff and boundaries</name>
  <files>README.md</files>
  <read_first>
    - `README.md`
    - `.planning/PROJECT.md`
    - `.planning/ROADMAP.md`
    - `.planning/STATE.md`
    - `.planning/phases/04-event-semantics-and-aggregates/04-CONTEXT.md`
    - `.planning/phases/04-event-semantics-and-aggregates/04-RESEARCH.md`
  </read_first>
  <action>
    After all Phase 4 implementation tests pass, update `README.md`:

    - Change current status to `Phase 4 Event Semantics and Aggregates work is complete and verified`.
    - Mention parser-core now emits normalized combat events, aggregate contributions/projections, bounty inputs, vehicle score inputs, and typed side facts.
    - Keep CLI, worker, full-corpus parity, benchmark, 100% coverage gate enforcement, RabbitMQ/S3, PostgreSQL persistence, public APIs, canonical identity, public UI, and annual/yearly nomination product support listed as not implemented.
    - Add/keep validation commands:
      - `cargo test -p parser-contract`
      - `cargo test -p parser-core`
      - `cargo test --workspace`
      - full quality gate command
    - Update `Last updated:` to `2026-04-27 after completing Phase 4 event semantics and aggregates`.
  </action>
  <verify>
    <automated>rg -n "Phase 4 Event Semantics and Aggregates work is complete|normalized combat events|vehicle score inputs|typed side facts|not implemented|Last updated: 2026-04-27 after completing Phase 4" README.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `Phase 4 Event Semantics and Aggregates work is complete`.
    - `README.md` contains `normalized combat events`.
    - `README.md` contains `vehicle score inputs`.
    - `README.md` contains `typed side facts`.
    - `README.md` still states project development is performed only by AI agents using GSD.
    - `git diff --check` exits 0.
  </acceptance_criteria>
  <done>README reflects Phase 4 completion without claiming later-phase commands exist.</done>
</task>

<task type="auto">
  <name>Task 4: Run final Phase 4 quality and boundary gates</name>
  <files>README.md, crates/parser-contract/tests/schema_contract.rs, crates/parser-core/tests/deterministic_output.rs</files>
  <read_first>
    - `.planning/phases/04-event-semantics-and-aggregates/04-VALIDATION.md`
    - `gsd-briefs/replay-parser-2.md`
    - `gsd-briefs/server-2.md`
    - `gsd-briefs/web.md`
    - `/home/alexandr/Projects/SolidGames/replays-fetcher/.planning/PROJECT.md`
  </read_first>
  <action>
    Run final gates:

    ```bash
    cargo fmt --all -- --check
    cargo clippy --workspace --all-targets -- -D warnings
    cargo test --workspace
    cargo doc --workspace --no-deps
    git diff --check
    ```

    Confirm no Phase 4 source files add:

    - PostgreSQL/database writes
    - RabbitMQ/lapin worker behavior
    - S3/aws-sdk behavior
    - canonical player IDs
    - public API/OpenAPI or web UI code
    - replay discovery/fetcher behavior

    Use `rg` checks:

    ```bash
    rg -n "postgres|sqlx|diesel|lapin|RabbitMQ|aws_sdk_s3|canonical_player|openapi|TanStack|fetch replay|crawl" crates/parser-contract crates/parser-core README.md
    ```

    If matches are documentation-only boundary statements, record that in the plan summary; if matches are implementation imports or behavior, remove or re-scope before completing the phase.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace && cargo doc --workspace --no-deps && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0.
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0.
    - `cargo test --workspace` exits 0.
    - `cargo doc --workspace --no-deps` exits 0.
    - `git diff --check` exits 0.
    - Boundary `rg` check finds no implementation imports or behavior for database, queue, S3, canonical identity, APIs, UI, or replay discovery.
  </acceptance_criteria>
  <done>Phase 4 implementation is quality-gated and remains within parser ownership boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| final artifact schema -> server integration | Schema and examples must match Rust types before downstream consumers rely on them. |
| parser-core output -> parity harness | Deterministic non-empty event/aggregate output is required for Phase 5 comparison. |
| parser scope -> adjacent apps | Parser must not absorb backend, worker, ingest, API, or UI responsibilities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | generated schema | mitigate | Compare freshly generated schema to committed schema. |
| T-04-06-02 | Repudiation | deterministic output | mitigate | Add repeated serialization tests for populated artifacts. |
| T-04-06-03 | Elevation of Privilege | scope boundaries | mitigate | Run boundary grep checks for DB/queue/S3/API/UI/canonical identity code. |
</threat_model>

<verification>
Run:

```bash
cargo test -p parser-contract schema_contract
cargo test -p parser-core deterministic_output
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cargo doc --workspace --no-deps
git diff --check
```
</verification>

<success_criteria>
- Schema and examples are current.
- Populated Phase 4 output is deterministic.
- README is current and does not overclaim later phases.
- Full workspace gates pass.
- Parser remains inside its application boundary.
</success_criteria>
