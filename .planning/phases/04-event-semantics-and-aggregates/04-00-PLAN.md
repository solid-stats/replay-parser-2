---
phase: 04-event-semantics-and-aggregates
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/parser-contract/src/events.rs
  - crates/parser-contract/src/aggregates.rs
  - crates/parser-contract/src/side_facts.rs
  - crates/parser-contract/src/artifact.rs
  - crates/parser-contract/src/lib.rs
  - crates/parser-contract/tests/combat_event_contract.rs
  - crates/parser-contract/tests/aggregate_contract.rs
  - crates/parser-contract/tests/replay_side_facts_contract.rs
  - crates/parser-contract/tests/schema_contract.rs
  - crates/parser-contract/examples/parse_artifact_success.v1.json
  - schemas/parse-artifact-v1.schema.json
autonomous: true
requirements:
  - PARS-08
  - PARS-09
  - PARS-10
  - PARS-11
  - AGG-02
  - AGG-06
  - AGG-07
  - AGG-08
  - AGG-09
  - AGG-10
  - AGG-11
requirements_addressed: [PARS-08, PARS-09, PARS-10, PARS-11, AGG-02, AGG-06, AGG-07, AGG-08, AGG-09, AGG-10, AGG-11]
must_haves:
  truths:
    - "D-01: each source killed tuple can be represented as one source-backed normalized event with one dominant combat semantic."
    - "D-04: bounty eligibility and exclusion reasons are schema-visible and can exclude teamkills, suicides, null-killer deaths, vehicle victims, and unknown actor cases."
    - "D-09 through D-12: vehicle score category, raw evidence, matrix weight, applied weight, sign, denominator eligibility, and clamp evidence are schema-visible."
    - "D-15: commander/outcome facts live in a typed parser-contract section, not only in extensions."
    - "D-16: missing commander/winner can be explicit unknown without forcing partial status."
  artifacts:
    - path: "crates/parser-contract/src/events.rs"
      provides: "Typed combat payloads for normalized events"
      contains: "CombatEventAttributes"
    - path: "crates/parser-contract/src/side_facts.rs"
      provides: "Typed commander and outcome facts"
      contains: "ReplaySideFacts"
    - path: "crates/parser-contract/src/aggregates.rs"
      provides: "Typed contribution payload helpers for legacy, bounty, and vehicle score values"
      contains: "VehicleScoreInputValue"
  key_links:
    - from: ".planning/phases/04-event-semantics-and-aggregates/04-CONTEXT.md"
      to: "crates/parser-contract/src/events.rs"
      via: "D-01, D-04, D-09 through D-12"
      pattern: "CombatEventAttributes"
---

<objective>
Extend the parser contract so Phase 4 parser-core work has schema-visible fields for combat events, aggregate contribution payloads, vehicle score evidence, and replay-side commander/outcome facts.

Purpose: lock the artifact shape before parser-core starts populating it.
Output: Rust contract types, schema/example updates, and contract tests.
</objective>

<execution_context>
@/home/alexandr/.codex/get-shit-done/workflows/execute-plan.md
@/home/alexandr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-event-semantics-and-aggregates/04-CONTEXT.md
@.planning/phases/04-event-semantics-and-aggregates/04-RESEARCH.md
@.planning/phases/04-event-semantics-and-aggregates/04-PATTERNS.md
@.planning/phases/04-event-semantics-and-aggregates/04-VALIDATION.md
@crates/parser-contract/src/events.rs
@crates/parser-contract/src/aggregates.rs
@crates/parser-contract/src/artifact.rs
@crates/parser-contract/src/presence.rs
@crates/parser-contract/src/source_ref.rs
@AGENTS.md
</context>

<decision_coverage>
- D-01: typed combat attributes with one dominant semantic.
- D-04: bounty eligibility and exclusion reasons.
- D-09 through D-12: vehicle-score categories, weights, denominator inputs, and teamkill clamp evidence.
- D-13 through D-16: typed commander/outcome facts with confidence, unknown state, and non-partial absence policy.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Add typed combat event payload contract</name>
  <files>crates/parser-contract/src/events.rs, crates/parser-contract/tests/combat_event_contract.rs</files>
  <read_first>
    - `crates/parser-contract/src/events.rs`
    - `crates/parser-contract/src/identity.rs`
    - `crates/parser-contract/src/presence.rs`
    - `crates/parser-contract/src/source_ref.rs`
    - `.planning/phases/04-event-semantics-and-aggregates/04-CONTEXT.md`
    - `.planning/phases/04-event-semantics-and-aggregates/04-RESEARCH.md`
  </read_first>
  <action>
    In `events.rs`, add these `Serialize`, `Deserialize`, `JsonSchema`, `Debug`, `Clone`, `PartialEq` contract types using `snake_case` enum serialization:

    - `CombatSemantic` enum values: `EnemyKill`, `Teamkill`, `Suicide`, `NullKillerDeath`, `VehicleDestroyed`, `Unknown`.
    - `CombatVictimKind` enum values: `Player`, `Vehicle`, `StaticWeapon`, `Unknown`.
    - `BountyEligibilityState` enum values: `Eligible`, `Excluded`.
    - `BountyExclusionReason` enum values: `Teamkill`, `Suicide`, `NullKiller`, `VehicleVictim`, `UnknownActor`, `UnknownSide`, `NotEnemyKill`.
    - `VehicleScoreCategory` enum values: `StaticWeapon`, `Car`, `Truck`, `Apc`, `Tank`, `Heli`, `Plane`, `Player`, `Unknown`.
    - `VehicleContext` struct with fields:
      - `is_kill_from_vehicle: bool`
      - `raw_weapon: FieldPresence<String>`
      - `attacker_vehicle_entity_id: FieldPresence<i64>`
      - `attacker_vehicle_name: FieldPresence<String>`
      - `attacker_vehicle_class: FieldPresence<String>`
      - `attacker_vehicle_category: FieldPresence<VehicleScoreCategory>`
      - `target_category: FieldPresence<VehicleScoreCategory>`
    - `BountyEligibility` struct with fields:
      - `state: BountyEligibilityState`
      - `exclusion_reasons: Vec<BountyExclusionReason>`
    - `LegacyCounterEffect` struct with fields:
      - `player_entity_id: i64`
      - `field: String`
      - `delta: i64`
      - `relationship_target_entity_id: Option<i64>`
    - `CombatEventAttributes` struct with fields:
      - `semantic: CombatSemantic`
      - `killer: FieldPresence<EventActorRef>`
      - `victim: FieldPresence<EventActorRef>`
      - `victim_kind: CombatVictimKind`
      - `weapon: FieldPresence<String>`
      - `distance_meters: FieldPresence<f64>`
      - `vehicle_context: VehicleContext`
      - `bounty: BountyEligibility`
      - `legacy_counter_effects: Vec<LegacyCounterEffect>`

    Add `pub combat: Option<CombatEventAttributes>` to `NormalizedEvent`, keeping the existing `attributes: BTreeMap<String, Value>` field for extension-only details.

    Add `combat_event_contract.rs` tests that:
    - create an enemy-kill `NormalizedEvent` with `combat.semantic == CombatSemantic::EnemyKill`
    - assert serialized JSON contains `"combat"`, `"enemy_kill"`, `"eligible"`, `"is_kill_from_vehicle"`, `"attacker_vehicle_category"`, and `"legacy_counter_effects"`
    - assert `NormalizedEventKind::Kill` still serializes as `"kill"`
  </action>
  <verify>
    <automated>cargo test -p parser-contract combat_event_contract && cargo test -p parser-contract source_ref_contract && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `events.rs` contains `pub struct CombatEventAttributes`.
    - `events.rs` contains `pub enum CombatSemantic`.
    - `events.rs` contains `pub enum VehicleScoreCategory`.
    - `NormalizedEvent` contains `pub combat: Option<CombatEventAttributes>`.
    - `combat_event_contract.rs` contains a test that serializes `"enemy_kill"`.
    - `cargo test -p parser-contract combat_event_contract` exits 0.
  </acceptance_criteria>
  <done>Combat event payload shape is available to parser-core and JSON Schema generation.</done>
</task>

<task type="auto">
  <name>Task 2: Add typed aggregate contribution payload helpers</name>
  <files>crates/parser-contract/src/aggregates.rs, crates/parser-contract/tests/aggregate_contract.rs</files>
  <read_first>
    - `crates/parser-contract/src/aggregates.rs`
    - `crates/parser-contract/src/events.rs`
    - `crates/parser-contract/src/source_ref.rs`
    - `.planning/phases/04-event-semantics-and-aggregates/04-CONTEXT.md`
  </read_first>
  <action>
    In `aggregates.rs`, keep `AggregateContributionRef.value: Value` for stable forward compatibility, and add typed helper structs that parser-core can serialize into that value:

    - `LegacyCounterContributionValue` with fields `projection_key: String`, `player_entity_id: i64`, `compatibility_key: String`, `field: String`, `delta: i64`, `event_id: String`.
    - `RelationshipContributionValue` with fields `projection_key: String`, `source_player_entity_id: i64`, `target_player_entity_id: i64`, `relationship: String`, `compatibility_source_key: String`, `compatibility_target_key: String`, `count_delta: i64`.
    - `BountyInputContributionValue` with fields `killer_entity_id: i64`, `victim_entity_id: i64`, `killer_side: String`, `victim_side: String`, `frame: Option<u64>`, `event_id: String`, `eligible: bool`, `exclusion_reasons: Vec<String>`.
    - `VehicleScoreSign` enum values `Award` and `Penalty`.
    - `VehicleScoreInputValue` with fields `player_entity_id: i64`, `event_id: String`, `sign: VehicleScoreSign`, `attacker_category: crate::events::VehicleScoreCategory`, `target_category: crate::events::VehicleScoreCategory`, `raw_attacker_vehicle_name: Option<String>`, `raw_attacker_vehicle_class: Option<String>`, `raw_target_class: Option<String>`, `matrix_weight: f64`, `applied_weight: f64`, `teamkill_penalty_clamped: bool`, `denominator_eligible: bool`.

    Add `aggregate_contract.rs` tests that:
    - serialize `VehicleScoreInputValue` and assert JSON has `"penalty"`, `"tank"`, `"static_weapon"`, `"matrix_weight": 0.25`, `"applied_weight": 1.0`, and `"teamkill_penalty_clamped": true`
    - wrap a vehicle score value in `AggregateContributionRef` with kind `VehicleScoreInput`, non-empty source refs, and rule ID `aggregate.vehicle_score.input`
    - assert `AggregateContributionKind::BountyInput` serializes as `"bounty_input"`
  </action>
  <verify>
    <automated>cargo test -p parser-contract aggregate_contract && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `aggregates.rs` contains `pub struct VehicleScoreInputValue`.
    - `aggregates.rs` contains `pub enum VehicleScoreSign`.
    - `aggregates.rs` contains `pub struct BountyInputContributionValue`.
    - `aggregate_contract.rs` asserts `"teamkill_penalty_clamped"` is serialized.
    - `cargo test -p parser-contract aggregate_contract` exits 0.
  </acceptance_criteria>
  <done>Aggregate payload values are typed in Rust while preserving the existing contribution envelope.</done>
</task>

<task type="auto">
  <name>Task 3: Add typed replay-side commander and outcome facts</name>
  <files>crates/parser-contract/src/side_facts.rs, crates/parser-contract/src/artifact.rs, crates/parser-contract/src/lib.rs, crates/parser-contract/tests/replay_side_facts_contract.rs</files>
  <read_first>
    - `crates/parser-contract/src/artifact.rs`
    - `crates/parser-contract/src/metadata.rs`
    - `crates/parser-contract/src/identity.rs`
    - `crates/parser-contract/src/presence.rs`
    - `.planning/phases/04-event-semantics-and-aggregates/04-CONTEXT.md`
    - `gsd-briefs/server-2.md`
  </read_first>
  <action>
    Add `crates/parser-contract/src/side_facts.rs` and export it from `lib.rs`.

    The new module must define:

    - `ReplaySideFacts` with fields `commanders: Vec<CommanderSideFact>` and `outcome: OutcomeFact`; implement `Default` with empty commanders and unknown outcome.
    - `CommanderFactKind` enum values `Observed` and `Candidate`.
    - `CommanderSideFact` with fields:
      - `side: FieldPresence<EntitySide>`
      - `side_name: FieldPresence<String>`
      - `commander: FieldPresence<EventActorRef>`
      - `kind: CommanderFactKind`
      - `confidence: Option<Confidence>`
      - `rule_id: RuleId`
      - `source_refs: SourceRefs`
    - `OutcomeStatus` enum values `Known`, `Unknown`, `Inferred`.
    - `OutcomeFact` with fields:
      - `status: OutcomeStatus`
      - `winner_side: FieldPresence<EntitySide>`
      - `source_label: FieldPresence<String>`
      - `confidence: Option<Confidence>`
      - `rule_id: RuleId`
      - `source_refs: Option<SourceRefs>`

    Add `side_facts: ReplaySideFacts` to `ParseArtifact`.

    Add `replay_side_facts_contract.rs` tests that:
    - assert `ReplaySideFacts::default().outcome.status == OutcomeStatus::Unknown`
    - serialize a candidate commander fact and assert JSON contains `"candidate"`, `"confidence"`, `"commander"`, and `"source_refs"`
    - serialize a known outcome and assert JSON contains `"known"` and `"winner_side"`
  </action>
  <verify>
    <automated>cargo test -p parser-contract replay_side_facts_contract artifact_envelope && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `side_facts.rs` exists and contains `pub struct ReplaySideFacts`.
    - `artifact.rs` contains `pub side_facts: ReplaySideFacts`.
    - `lib.rs` exports `pub mod side_facts`.
    - `replay_side_facts_contract.rs` proves default outcome is unknown.
    - `cargo test -p parser-contract replay_side_facts_contract artifact_envelope` exits 0.
  </acceptance_criteria>
  <done>Commander/outcome facts have a typed contract section for server integration.</done>
</task>

<task type="auto">
  <name>Task 4: Regenerate schema and success example with new contract fields</name>
  <files>crates/parser-contract/tests/schema_contract.rs, crates/parser-contract/examples/parse_artifact_success.v1.json, schemas/parse-artifact-v1.schema.json</files>
  <read_first>
    - `crates/parser-contract/examples/parse_artifact_success.v1.json`
    - `crates/parser-contract/tests/schema_contract.rs`
    - `crates/parser-contract/examples/export_schema.rs`
    - `schemas/parse-artifact-v1.schema.json`
  </read_first>
  <action>
    Update the committed success example so it includes:

    - top-level `"side_facts"` object with `"commanders": []` and unknown `"outcome"`
    - at least one normalized combat event with `"combat"` and `semantic: "enemy_kill"`
    - at least one aggregate contribution with `kind: "bounty_input"`
    - at least one aggregate contribution with `kind: "vehicle_score_input"` and `"teamkill_penalty_clamped": false`
    - aggregate projections containing keys `"legacy.player_game_results"`, `"legacy.relationships"`, `"bounty.inputs"`, and `"vehicle_score.inputs"`

    Update `schema_contract.rs` to assert the committed schema text contains:

    - `CombatEventAttributes`
    - `VehicleScoreInputValue`
    - `ReplaySideFacts`
    - `side_facts`
    - `vehicle_score_input`

    Regenerate the committed schema with:

    ```bash
    cargo run -p parser-contract --example export_schema > schemas/parse-artifact-v1.schema.json
    ```
  </action>
  <verify>
    <automated>cargo run -p parser-contract --example export_schema > /tmp/parse-artifact-v1.schema.json && cmp /tmp/parse-artifact-v1.schema.json schemas/parse-artifact-v1.schema.json && cargo test -p parser-contract schema_contract combat_event_contract aggregate_contract replay_side_facts_contract && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `parse_artifact_success.v1.json` contains `"side_facts"`.
    - `parse_artifact_success.v1.json` contains `"combat"`.
    - `parse_artifact_success.v1.json` contains `"vehicle_score_input"`.
    - `schemas/parse-artifact-v1.schema.json` contains `"ReplaySideFacts"`.
    - Regenerated `/tmp/parse-artifact-v1.schema.json` matches committed schema.
    - `cargo test -p parser-contract schema_contract combat_event_contract aggregate_contract replay_side_facts_contract` exits 0.
  </acceptance_criteria>
  <done>Contract additions are schema-backed and represented in the committed example artifact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parser-contract -> server-2 | New artifact fields must be typed and schema-visible before backend persistence consumes them. |
| replay facts -> aggregate projections | Aggregate payloads must remain auditable and source-reference-backed. |
| unknown commander/outcome -> public stats | Missing facts must be distinguishable from known false values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00-01 | Tampering | contract schema | mitigate | Regenerate schema from Rust and assert committed schema contains new types. |
| T-04-00-02 | Repudiation | aggregate payloads | mitigate | Keep contribution refs with event IDs, source refs, and rule IDs. |
| T-04-00-03 | Information Disclosure | diagnostics/events | mitigate | Reuse path/source refs and do not embed raw replay snippets. |
| T-04-00-04 | Elevation of Privilege | identity boundary | mitigate | Event/aggregate payloads use observed entity IDs only, never canonical player IDs. |
</threat_model>

<verification>
Run:

```bash
cargo test -p parser-contract combat_event_contract
cargo test -p parser-contract aggregate_contract
cargo test -p parser-contract replay_side_facts_contract
cargo test -p parser-contract schema_contract
cargo fmt --all -- --check
git diff --check
```
</verification>

<success_criteria>
- Parser contract exposes typed combat payloads.
- Parser contract exposes typed replay-side commander/outcome facts.
- Aggregate payload helper types cover legacy counters, relationships, bounty inputs, and vehicle score inputs.
- Committed schema and example artifact include all new Phase 4 contract surfaces.
- Existing contract status/failure/source-ref invariants still pass.
</success_criteria>
