---
phase: 03-deterministic-parser-core
plan: 05
type: execute
wave: 6
depends_on:
  - 03-04
files_modified:
  - crates/parser-core/src/raw.rs
  - crates/parser-core/src/entities.rs
  - crates/parser-core/src/diagnostics.rs
  - crates/parser-core/tests/legacy_entity_compatibility.rs
  - crates/parser-core/tests/fixtures/connected-backfill.ocap.json
  - crates/parser-core/tests/fixtures/duplicate-slot-same-name.ocap.json
  - README.md
autonomous: true
requirements:
  - OUT-08
  - PARS-01
  - PARS-02
  - PARS-03
  - PARS-04
  - PARS-05
  - PARS-06
  - PARS-07
requirements_addressed: [OUT-08, PARS-01, PARS-02, PARS-03, PARS-04, PARS-05, PARS-06, PARS-07]
must_haves:
  truths:
    - "D-18 / PARS-06: connected-player backfill creates inferred observed facts when legacy conditions are met."
    - "D-19: connected-player backfilled fields carry rule ID `entity.connected_player_backfill` and source refs to connected event and entity evidence."
    - "D-20 / PARS-07: duplicate-slot same-name behavior is represented as compatibility hints and does not merge normalized entities."
    - "D-21: compatibility hooks alone do not force partial status; conflicts or data loss do."
    - "D-22: game-type filtering and annual/yearly nomination behavior remain outside parser-core."
    - "D-23: compatibility tests use small focused fixtures; full corpus and golden parity remain Phase 5."
    - "D-24: Phase 3 behavior tests cover normal metadata/entity extraction, schema drift, malformed input, explicit unknowns, deterministic ordering, source refs, connected-player backfill, and duplicate-slot hints."
    - "D-25: legacy hook tests reference `getEntities.ts` and `combineSamePlayersInfo.ts` while avoiding full real-corpus parity in Phase 3."
    - "D-26: tests assert observable public `ParseArtifact` behavior through `parse_replay` and avoid test-only production exports unless required."
    - "D-27: strict Rust format, clippy, test, and doc gates pass."
  artifacts:
    - path: "crates/parser-core/src/entities.rs"
      provides: "Legacy entity compatibility hooks"
      contains: "connected_player_backfill"
    - path: "crates/parser-core/tests/legacy_entity_compatibility.rs"
      provides: "Backfill and same-name compatibility behavior tests"
      contains: "legacy_entity_compatibility"
    - path: "README.md"
      provides: "Human-facing Phase 3 parser-core handoff"
      contains: "parser-core"
  key_links:
    - from: "/home/afgan0r/Projects/SolidGames/replays-parser/src/2 - parseReplayInfo/getEntities.ts"
      to: "crates/parser-core/src/entities.rs"
      via: "connected event backfill behavior"
      pattern: "connected"
    - from: "/home/afgan0r/Projects/SolidGames/replays-parser/src/2 - parseReplayInfo/combineSamePlayersInfo.ts"
      to: "crates/parser-core/src/entities.rs"
      via: "duplicate same-name compatibility hint"
      pattern: "duplicate_slot_same_name"
---

<objective>
Implement Phase 3 legacy entity compatibility hooks, update the README handoff, and run final workspace quality gates.

Purpose: preserve old connected-player and same-name slot behavior as auditable observed facts/hints without implementing aggregate formulas or canonical identity.
Output: compatibility hook implementation, focused legacy-hook fixtures/tests, README status update, and full quality verification.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deterministic-parser-core/03-CONTEXT.md
@.planning/phases/03-deterministic-parser-core/03-RESEARCH.md
@.planning/phases/03-deterministic-parser-core/03-PATTERNS.md
@.planning/phases/03-deterministic-parser-core/03-VALIDATION.md
@.planning/phases/01-legacy-baseline-and-corpus/legacy-rules-output-surfaces.md
@/home/afgan0r/Projects/SolidGames/replays-parser/src/2 - parseReplayInfo/getEntities.ts
@/home/afgan0r/Projects/SolidGames/replays-parser/src/2 - parseReplayInfo/combineSamePlayersInfo.ts
@README.md
@AGENTS.md
</context>

<decision_coverage>
- D-18: Preserve old connected-player backfill behavior as inferred observed entity facts.
- D-19: Backfilled facts carry explicit provenance and rule IDs.
- D-20: Preserve duplicate-slot same-name behavior as typed compatibility hints; do not collapse entities.
- D-21: Compatibility hooks do not automatically make the artifact partial.
- D-22: Keep game-type filtering and yearly nomination behavior out of parser-core.
- D-23 through D-27: Use focused fixtures and strict Rust gates.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Add raw connected-event helpers and connected-player backfill</name>
  <files>crates/parser-core/src/raw.rs, crates/parser-core/src/entities.rs, crates/parser-core/src/diagnostics.rs</files>
  <read_first>
    - `crates/parser-core/src/raw.rs`
    - `crates/parser-core/src/entities.rs`
    - `crates/parser-contract/src/identity.rs`
    - `crates/parser-contract/src/presence.rs`
    - `crates/parser-contract/src/source_ref.rs`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/src/2 - parseReplayInfo/getEntities.ts`
  </read_first>
  <action>
    Extend `raw.rs` with connected event helpers for event tuples:

    - event must be an array
    - `event[0]` is frame number
    - `event[1]` is event type string
    - connected event shape is `[frame, "connected", name, entity_id]`
    - return connected event observations with original event index, frame, name, entity ID, and JSON path `$.events[<index>]`

    Update `entities.rs` after primary entity normalization:

    - Build an index from source entity ID to normalized entity.
    - For each connected event:
      - skip if no entity with the same ID exists
      - skip if the entity kind is `EntityKind::Vehicle` or `EntityKind::StaticWeapon`
      - skip if connected name is empty
      - if the entity already has an observed present name, keep the observed name and add an `EntityCompatibilityHintKind::ConnectedPlayerBackfill` hint with rule ID `entity.connected_player_backfill`
      - if the entity name is unknown, set `observed_name` and `identity.nickname` to `FieldPresence::Inferred` with value from the connected event, reason `"legacy connected event player backfill"`, confidence `Some(Confidence::new(1.0)?)`, event source ref, and rule ID `entity.connected_player_backfill`
      - include source refs to both the connected event and original entity in the hint

    Do not emit a normalized event in `artifact.events`; Phase 4 owns event semantics.
  </action>
  <verify>
    <automated>rg -n "connected_player_backfill|entity\\.connected_player_backfill|legacy connected event player backfill" crates/parser-core/src && cargo check -p parser-core --all-targets && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `raw.rs` recognizes connected event tuple shape `[frame, "connected", name, entity_id]`.
    - `entities.rs` contains rule ID string `"entity.connected_player_backfill"`.
    - Backfill skips vehicle/static weapon entities.
    - Backfill skips empty connected names.
    - Backfill uses `FieldPresence::Inferred` when source entity name is unknown.
    - Backfill does not push entries into `artifact.events`.
    - `cargo check -p parser-core --all-targets` exits 0.
  </acceptance_criteria>
  <done>Connected-player compatibility is implemented as inferred observed facts and hints with provenance.</done>
</task>

<task type="auto">
  <name>Task 2: Add duplicate-slot same-name compatibility hints without merging entities</name>
  <files>crates/parser-core/src/entities.rs, crates/parser-core/src/diagnostics.rs</files>
  <read_first>
    - `crates/parser-core/src/entities.rs`
    - `crates/parser-contract/src/identity.rs`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/src/2 - parseReplayInfo/combineSamePlayersInfo.ts`
    - `.planning/phases/03-deterministic-parser-core/03-CONTEXT.md`
  </read_first>
  <action>
    In `entities.rs`, after connected-player backfill and before final sorting returns:

    - Group unit entities by non-empty present or inferred `observed_name` value.
    - For every group with two or more source entity IDs, create an `EntityCompatibilityHintKind::DuplicateSlotSameName` hint.
    - Add the hint to every entity in the group.
    - `related_entity_ids` must be sorted ascending and include every source entity ID in the group.
    - `observed_name` in the hint must be `FieldPresence::Present` when the shared name was observed and `FieldPresence::Inferred` when the shared name came from connected backfill.
    - `rule_id` must be `RuleId::new("entity.duplicate_slot_same_name")`.
    - `source_refs` must include source refs from every related entity, preserving original JSON paths.
    - Do not remove or merge any `ObservedEntity`.
    - If grouped entities have conflicting present sides, emit warning diagnostic code `"compat.entity_duplicate_side_conflict"` with parser action `"kept_entities_unmerged"` and classify it as `DiagnosticImpact::DataLoss` so the artifact becomes partial.
    - If there is no conflict, do not change status by the presence of the hint alone.
  </action>
  <verify>
    <automated>rg -n "duplicate_slot_same_name|compat\\.entity_duplicate_side_conflict|kept_entities_unmerged" crates/parser-core/src && cargo check -p parser-core --all-targets && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `entities.rs` contains rule ID string `"entity.duplicate_slot_same_name"`.
    - Duplicate same-name groups add hints to each related entity.
    - Duplicate same-name groups keep all `ObservedEntity` records.
    - Same-name side conflicts emit diagnostic code `"compat.entity_duplicate_side_conflict"`.
    - Non-conflicting same-name hints do not force partial status.
    - `cargo check -p parser-core --all-targets` exits 0.
  </acceptance_criteria>
  <done>Same-name slot compatibility is preserved as auditable hints without collapsing observations.</done>
</task>

<task type="auto">
  <name>Task 3: Add legacy compatibility fixtures and behavior tests</name>
  <files>crates/parser-core/tests/legacy_entity_compatibility.rs, crates/parser-core/tests/fixtures/connected-backfill.ocap.json, crates/parser-core/tests/fixtures/duplicate-slot-same-name.ocap.json</files>
  <read_first>
    - `crates/parser-core/src/entities.rs`
    - `crates/parser-core/tests/entity_normalization.rs`
    - `.planning/phases/01-legacy-baseline-and-corpus/legacy-rules-output-surfaces.md`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/src/2 - parseReplayInfo/getEntities.ts`
    - `/home/afgan0r/Projects/SolidGames/replays-parser/src/2 - parseReplayInfo/combineSamePlayersInfo.ts`
  </read_first>
  <action>
    Add `connected-backfill.ocap.json` with:

    - valid metadata
    - one unit entity id `11`, type `"unit"`, empty `name`, side `"WEST"`, group `"Alpha 1-1"`, description `"Rifleman"`, isPlayer `1`
    - one vehicle entity id `99`
    - connected events:
      - `[4, "connected", "BackfilledName", 11]`
      - `[5, "connected", "VehicleShouldSkip", 99]`
      - `[6, "connected", "", 11]`

    Add `duplicate-slot-same-name.ocap.json` with:

    - valid metadata
    - two unit entities id `21` and `22` with name `"SameName"`, side `"WEST"`
    - one unit entity id `23` with name `"OtherName"`
    - no combat events

    Add `legacy_entity_compatibility.rs` tests named:

    - `legacy_entity_compatibility_should_infer_player_name_from_connected_event_when_entity_name_is_missing`
    - `legacy_entity_compatibility_should_skip_connected_backfill_for_vehicle_entities`
    - `legacy_entity_compatibility_should_attach_connected_backfill_hint_with_event_and_entity_source_refs`
    - `legacy_entity_compatibility_should_add_duplicate_slot_hint_without_merging_same_name_entities`
    - `legacy_entity_compatibility_should_keep_success_status_when_duplicate_slot_hint_has_no_conflict`

    Tests must assert:

    - backfilled entity id `11` has inferred observed name `"BackfilledName"`
    - inferred field has rule ID `"entity.connected_player_backfill"`
    - vehicle id `99` is not renamed to `"VehicleShouldSkip"`
    - duplicate fixture still has three entities
    - entities id `21` and `22` each have one `duplicate_slot_same_name` hint with related IDs `[21, 22]`
    - duplicate fixture status remains `ParseStatus::Success`
  </action>
  <verify>
    <automated>cargo test -p parser-core legacy_entity_compatibility && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p parser-core legacy_entity_compatibility` exits 0.
    - Connected backfill test proves inferred name `"BackfilledName"`.
    - Connected backfill test proves rule ID `"entity.connected_player_backfill"`.
    - Vehicle skip test proves vehicle id `99` keeps original observed name.
    - Duplicate-slot test proves entity count remains `3`.
    - Duplicate-slot test proves related IDs `[21, 22]`.
    - Duplicate-slot no-conflict test proves artifact status is `ParseStatus::Success`.
  </acceptance_criteria>
  <done>Legacy identity compatibility hooks are locked by focused behavior tests.</done>
</task>

<task type="auto">
  <name>Task 4: Update README and run final Phase 3 quality gates</name>
  <files>README.md</files>
  <read_first>
    - `README.md`
    - `.planning/PROJECT.md`
    - `.planning/ROADMAP.md`
    - `.planning/STATE.md`
    - `.planning/phases/03-deterministic-parser-core/03-CONTEXT.md`
    - `.planning/phases/03-deterministic-parser-core/03-RESEARCH.md`
  </read_first>
  <action>
    Update `README.md` after parser-core exists:

    - Change current status from "Phase 3 ready to plan" to "Phase 3 parser-core work complete" only after all previous tasks in this plan pass.
    - Mention the new crate path `crates/parser-core`.
    - Add the parser-core quick validation command:

    ```bash
    cargo test -p parser-core
    ```

    - Keep the broader workspace gate exactly:

    ```bash
    cargo fmt --all -- --check
    cargo clippy --workspace --all-targets -- -D warnings
    cargo test --workspace
    cargo doc --workspace --no-deps
    ```

    - Do not claim CLI, worker, golden parity, full corpus, or benchmark commands exist.

    Run final verification:

    ```bash
    cargo fmt --all -- --check
    cargo clippy --workspace --all-targets -- -D warnings
    cargo test --workspace
    cargo doc --workspace --no-deps
    git diff --check
    ```
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace && cargo doc --workspace --no-deps && rg -n "crates/parser-core|cargo test -p parser-core" README.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `crates/parser-core`.
    - `README.md` contains `cargo test -p parser-core`.
    - `README.md` does not claim CLI parse command is implemented.
    - `cargo fmt --all -- --check` exits 0.
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0.
    - `cargo test --workspace` exits 0.
    - `cargo doc --workspace --no-deps` exits 0.
    - `git diff --check` exits 0.
  </acceptance_criteria>
  <done>Phase 3 implementation is documented and verified by strict workspace gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy identity compatibility -> normalized artifact | Old behavior must be preserved as evidence/hints, not canonical identity. |
| README -> developer workflow | Documentation must not advertise commands or surfaces that are not implemented. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Spoofing | connected-player identity | mitigate | Mark backfilled names as inferred with rule ID and event/entity source refs. |
| T-03-05-02 | Tampering | duplicate-slot compatibility | mitigate | Emit hints but do not merge or delete observed entities. |
| T-03-05-03 | Repudiation | compatibility provenance | mitigate | Require source refs and stable rule IDs for every compatibility hint. |
| T-03-05-04 | Information Disclosure | README claims | mitigate | Document only implemented parser-core commands and keep adjacent app ownership boundaries. |
</threat_model>

<verification>
Run:

```bash
cargo test -p parser-core legacy_entity_compatibility
cargo test -p parser-core
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cargo doc --workspace --no-deps
git diff --check
```
</verification>

<success_criteria>
Plan 03-05 is complete when connected-player backfill and duplicate-slot same-name compatibility are implemented as auditable inferred facts/hints, raw observations remain unmerged, README reflects implemented parser-core status, and full workspace quality gates pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-deterministic-parser-core/03-05-SUMMARY.md`.
</output>
