---
phase: 05-cli-golden-parity-benchmarks-and-coverage-gates
plan: 05
type: execute
wave: 6
depends_on:
  - 05-00
  - 05-01
  - 05-02
  - 05-03
  - 05-04
files_modified:
  - crates/parser-harness/benches/parser_pipeline.rs
  - crates/parser-harness/src/benchmark_report.rs
  - crates/parser-harness/tests/benchmark_report.rs
  - scripts/benchmark-phase5.sh
  - README.md
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
  - TEST-06
  - TEST-07
  - TEST-08
  - TEST-09
  - TEST-10
  - TEST-11
  - TEST-12
requirements_addressed: [CLI-01, CLI-02, CLI-03, CLI-04, TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-06, TEST-07, TEST-08, TEST-09, TEST-10, TEST-11, TEST-12]
must_haves:
  truths:
    - "D-01: README and command docs use `replay-parser-2 parse`, `replay-parser-2 schema`, and `replay-parser-2 compare`."
    - "D-14: benchmark evidence is two-tier: Rust parser-core stages and command-level old-vs-new workloads."
    - "D-15: workloads are tiered into small CI sample, curated representative sample, and optional/manual full-corpus run."
    - "D-16: reports include files/sec, MB/sec or events/sec, wall time, memory/RSS where practical, old baseline profile, parity status, and 10x status."
    - "D-17: less than roughly 10x performance blocks Phase 5 until triage records bottleneck, parity, fix, or accepted gap."
    - "Final Phase 5 gates do not add RabbitMQ/S3 worker, PostgreSQL, public API, canonical identity, replay discovery, UI, or yearly nomination support."
  artifacts:
    - path: "crates/parser-harness/src/benchmark_report.rs"
      provides: "Benchmark report schema and validator"
      contains: "ten_x_status"
    - path: "scripts/benchmark-phase5.sh"
      provides: "Command-level benchmark runner"
      contains: "WORKER_COUNT=1"
    - path: "README.md"
      provides: "Phase 5 user-facing command handoff"
      contains: "replay-parser-2 parse"
  key_links:
    - from: ".planning/phases/05-cli-golden-parity-benchmarks-and-coverage-gates/05-CONTEXT.md"
      to: "crates/parser-harness/src/benchmark_report.rs"
      via: "D-14, D-15, D-16, D-17"
      pattern: "ten_x_status"
---

<objective>
Add benchmark/report validation, update README for implemented Phase 5 commands,
and run final quality, coverage, fault, parity, benchmark, and boundary gates.

Purpose: complete Phase 5 with reproducible local CLI, parity reports, coverage
and fault gates, and measured 10x evidence or explicit triage.
Output: benchmark tooling, report validators, README handoff, and final full
workspace gate evidence.
</objective>

<execution_context>
@/home/alexandr/.codex/get-shit-done/workflows/execute-plan.md
@/home/alexandr/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cli-golden-parity-benchmarks-and-coverage-gates/05-CONTEXT.md
@.planning/phases/05-cli-golden-parity-benchmarks-and-coverage-gates/05-RESEARCH.md
@.planning/phases/05-cli-golden-parity-benchmarks-and-coverage-gates/05-VALIDATION.md
@.planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md
@.planning/phases/01-legacy-baseline-and-corpus/fixture-index.json
@crates/parser-core/src/lib.rs
@crates/parser-core/src/raw.rs
@crates/parser-core/src/entities.rs
@crates/parser-core/src/events.rs
@crates/parser-core/src/aggregates.rs
@crates/parser-harness/src/report.rs
@README.md
@AGENTS.md
</context>

<decision_coverage>
- D-01: README command names.
- D-14: two-tier benchmark evidence.
- D-15: workload tiers and fixture identity.
- D-16: required benchmark report fields.
- D-17: below-10x triage gate.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Add benchmark report schema and validator</name>
  <files>crates/parser-harness/src/benchmark_report.rs, crates/parser-harness/src/lib.rs, crates/parser-harness/tests/benchmark_report.rs</files>
  <read_first>
    - `.planning/phases/05-cli-golden-parity-benchmarks-and-coverage-gates/05-CONTEXT.md`
    - `.planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md`
    - `crates/parser-harness/src/report.rs`
  </read_first>
  <action>
    Add `benchmark_report.rs` with serializable types:

    - `BenchmarkTier` values `SmallCi`, `CuratedRepresentative`, `ManualFullCorpus`;
    - `TenXStatus` values `Pass`, `Fail`, `Unknown`;
    - `ParityStatus` values `Passed`, `Failed`, `HumanReview`, `NotRun`;
    - `BenchmarkMetric` with `wall_time_ms`, `files_per_sec`, `mb_per_sec`, `events_per_sec`, and `rss_mb`;
    - `BenchmarkWorkload` with `tier`, `fixtures`, `corpus_selector`, and `total_bytes`;
    - `BenchmarkReport` with `report_version`, `old_baseline_profile`, `old_command`, `new_command`, `workload`, `parse_only`, `aggregate_only`, `end_to_end`, `parity_status`, `ten_x_status`, and `triage`.

    Validator rules:

    - `old_baseline_profile` must contain `WORKER_COUNT=1` unless `ten_x_status` is `Unknown` and triage explains why;
    - report must include workload identity through fixture list or corpus selector;
    - report must include parity status;
    - if `ten_x_status` is `Fail`, `triage` must be non-empty and mention `bottleneck` and `parity`;
    - if `rss_mb` is absent, report must include a note explaining why memory/RSS was not practical.

    Add tests for valid pass, fail without triage rejected, missing parity rejected, and missing workload identity rejected.
  </action>
  <verify>
    <automated>cargo test -p parser-harness benchmark_report && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `benchmark_report.rs` contains `TenXStatus`.
    - `benchmark_report.rs` contains `WORKER_COUNT=1`.
    - `benchmark_report.rs` contains `parity_status`.
    - `benchmark_report.rs` contains `rss_mb`.
    - `cargo test -p parser-harness benchmark_report` exits 0.
  </acceptance_criteria>
  <done>Benchmark reports are structured and enforce 10x evidence rules.</done>
</task>

<task type="auto">
  <name>Task 2: Add parser-stage and command-level benchmark runners</name>
  <files>crates/parser-harness/Cargo.toml, crates/parser-harness/benches/parser_pipeline.rs, scripts/benchmark-phase5.sh</files>
  <read_first>
    - `crates/parser-core/src/lib.rs`
    - `crates/parser-core/src/raw.rs`
    - `crates/parser-core/src/entities.rs`
    - `crates/parser-core/src/events.rs`
    - `crates/parser-core/src/aggregates.rs`
    - `crates/parser-core/tests/fixtures/golden/manifest.json`
    - `.planning/phases/01-legacy-baseline-and-corpus/baseline-command-runtime.md`
  </read_first>
  <action>
    Add `criterion = "0.5"` as a parser-harness dev dependency and create a benchmark target for parser stages.

    `parser_pipeline.rs` should benchmark:

    - parse-only raw JSON decode into `serde_json::Value` or `RawReplay` construction;
    - aggregate-only projection from a prepared normalized artifact fixture using existing public module functions where available;
    - end-to-end `parser_core::parse_replay`.

    Avoid adding production exports solely for benchmarks. If a stage cannot be measured without breaking API boundaries, use the nearest existing public function and record the limitation in the benchmark report.

    Add `scripts/benchmark-phase5.sh` that:

    - creates `.planning/generated/phase-05/benchmarks/`;
    - runs `cargo bench -p parser-harness --bench parser_pipeline` for Rust-stage evidence;
    - optionally runs command-level old/new benchmark only when `RUN_PHASE5_OLD_BASELINE=1` is set;
    - old command uses the deterministic baseline pattern with `WORKER_COUNT=1 pnpm run parse` from `/home/afgan0r/Projects/SolidGames/replays-parser`;
    - new command uses `cargo run --release -p parser-cli --bin replay-parser-2 -- parse ... --output ...` or a harness loop over the same selected workload;
    - writes a JSON report validated by `BenchmarkReport`.
  </action>
  <verify>
    <automated>cargo bench -p parser-harness --bench parser_pipeline -- --sample-size 10 && scripts/benchmark-phase5.sh --ci && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `parser_pipeline.rs` contains `parse_replay`.
    - `parser_pipeline.rs` contains `parse_only`.
    - `parser_pipeline.rs` contains `aggregate_only`.
    - `scripts/benchmark-phase5.sh` contains `WORKER_COUNT=1`.
    - `scripts/benchmark-phase5.sh --ci` writes a report under `.planning/generated/phase-05/benchmarks/`.
  </acceptance_criteria>
  <done>Phase 5 has parser-stage and command-level benchmark entrypoints.</done>
</task>

<task type="auto">
  <name>Task 3: Update README with implemented Phase 5 commands and gates</name>
  <files>README.md</files>
  <read_first>
    - `README.md`
    - `.planning/PROJECT.md`
    - `.planning/ROADMAP.md`
    - `.planning/REQUIREMENTS.md`
    - `.planning/phases/05-cli-golden-parity-benchmarks-and-coverage-gates/05-CONTEXT.md`
  </read_first>
  <action>
    After implementation and tests pass, update README:

    - current status states Phase 5 CLI, golden parity, benchmark, and coverage gate work is implemented or ready for verification, matching actual execution state;
    - planned/user commands use `replay-parser-2 parse`, `replay-parser-2 schema`, and `replay-parser-2 compare`, not `sg-replay-parser`;
    - document `scripts/coverage-gate.sh`, `scripts/fault-report-gate.sh`, and `scripts/benchmark-phase5.sh --ci`;
    - keep RabbitMQ/S3 worker, PostgreSQL persistence, public APIs, canonical identity, public UI, replay discovery, and annual/yearly nomination product support listed as not implemented until later phases;
    - keep the AI agents plus GSD-only development workflow statement;
    - update `Last updated:` to the actual completion date.
  </action>
  <verify>
    <automated>rg -n "replay-parser-2 parse|replay-parser-2 schema|replay-parser-2 compare|coverage-gate|fault-report-gate|benchmark-phase5|AI agents using the GSD workflow|not implemented" README.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `replay-parser-2 parse`.
    - `README.md` contains `replay-parser-2 schema`.
    - `README.md` contains `replay-parser-2 compare`.
    - `README.md` contains `scripts/coverage-gate.sh`.
    - `README.md` still states development is performed only by AI agents using GSD.
  </acceptance_criteria>
  <done>README reflects Phase 5 command names and gates without claiming later-phase ownership.</done>
</task>

<task type="auto">
  <name>Task 4: Run final Phase 5 quality, coverage, fault, benchmark, and boundary gates</name>
  <files>README.md, scripts/coverage-gate.sh, scripts/fault-report-gate.sh, scripts/benchmark-phase5.sh, crates/parser-harness/src/benchmark_report.rs</files>
  <read_first>
    - `.planning/phases/05-cli-golden-parity-benchmarks-and-coverage-gates/05-VALIDATION.md`
    - `gsd-briefs/replay-parser-2.md`
    - `gsd-briefs/server-2.md`
    - `gsd-briefs/replays-fetcher.md`
    - `gsd-briefs/web.md`
  </read_first>
  <action>
    Run the full final gate set:

    ```bash
    cargo fmt --all -- --check
    cargo clippy --workspace --all-targets -- -D warnings
    cargo test --workspace
    cargo doc --workspace --no-deps
    scripts/coverage-gate.sh
    scripts/fault-report-gate.sh
    scripts/benchmark-phase5.sh --ci
    git diff --check
    ```

    Run boundary grep:

    ```bash
    rg -n "postgres|sqlx|diesel|lapin|RabbitMQ|aws_sdk_s3|S3|canonical_player|openapi|TanStack|fetch replay|crawl|year_results|!yearStatistics" crates README.md scripts
    ```

    Acceptable matches are README/planning text that explicitly says these are out of scope or future phases, or benchmark scripts that reference the legacy old-parser baseline without implementing production worker behavior.

    If `scripts/benchmark-phase5.sh --ci` reports `ten_x_status = "fail"`, add a triage section to the generated report and do not mark Phase 5 complete until the triage identifies bottleneck, parity status, and either a fix or accepted gap.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace && cargo doc --workspace --no-deps && scripts/coverage-gate.sh && scripts/fault-report-gate.sh && scripts/benchmark-phase5.sh --ci && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0.
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0.
    - `cargo test --workspace` exits 0.
    - `cargo doc --workspace --no-deps` exits 0.
    - `scripts/coverage-gate.sh` exits 0.
    - `scripts/fault-report-gate.sh` exits 0.
    - `scripts/benchmark-phase5.sh --ci` exits 0 and validates its report.
    - Boundary grep finds no new implementation of worker, database, replay discovery, canonical identity, UI, or yearly nomination support.
  </acceptance_criteria>
  <done>Phase 5 is ready for verification with all gates green or explicitly triaged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark evidence -> performance claim | Speed claims must be tied to workload identity, parity, and baseline profile. |
| old baseline command -> local generated data | Benchmark scripts must isolate old parser output and avoid mutating real `~/sg_stats/results`. |
| final docs -> user expectations | README must distinguish implemented Phase 5 commands from future worker/server/UI work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Repudiation | 10x claim | mitigate | Benchmark report requires old profile, workload, parity status, metrics, and 10x status. |
| T-05-05-02 | Tampering | historical results | mitigate | Old-parser benchmark uses fake HOME/generated paths and never writes real `~/sg_stats/results`. |
| T-05-05-03 | Information Disclosure | reports/docs | mitigate | Generated benchmark details stay under `.planning/generated/phase-05/`; README exposes only reproducible commands and scope. |
</threat_model>

<verification>
Run:

```bash
cargo test -p parser-harness benchmark_report
cargo bench -p parser-harness --bench parser_pipeline -- --sample-size 10
scripts/benchmark-phase5.sh --ci
scripts/coverage-gate.sh
scripts/fault-report-gate.sh
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
cargo doc --workspace --no-deps
git diff --check
```
</verification>

<success_criteria>
- Benchmark reports include parser-stage and command-level evidence.
- Reports include workload identity, parity status, old baseline profile,
  throughput, memory/RSS note, and 10x status.
- README uses `replay-parser-2` command names and documents Phase 5 gates.
- Full workspace, coverage, fault, benchmark, and boundary gates pass before
  Phase 5 verification.
</success_criteria>
