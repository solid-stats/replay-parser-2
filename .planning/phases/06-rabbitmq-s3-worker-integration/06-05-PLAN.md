---
phase: 06-rabbitmq-s3-worker-integration
plan: 05
type: execute
wave: 5
depends_on:
  - 06-04
files_modified:
  - README.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
  - crates/parser-contract/examples/parse_job.v1.json
  - crates/parser-contract/examples/parse_completed.v1.json
  - crates/parser-contract/examples/parse_failed.v1.json
  - schemas/parse-job-v1.schema.json
  - schemas/parse-result-v1.schema.json
autonomous: true
requirements:
  - WORK-01
  - WORK-02
  - WORK-03
  - WORK-04
  - WORK-05
  - WORK-06
  - WORK-07
requirements_addressed: [WORK-01, WORK-02, WORK-03, WORK-04, WORK-05, WORK-06, WORK-07]
must_haves:
  truths:
    - "D-01 through D-17: final documentation and gates reflect every locked Phase 6 decision."
    - "Phase 6 delivers single-worker RabbitMQ/S3 integration only; Phase 7 owns multi-worker safety, HTTP health/readiness, and container hardening."
    - "README.md remains current and states project development uses only AI agents plus GSD workflow."
---

<objective>
Run final worker integration gates, validate schemas and boundary constraints, and update README/ROADMAP/STATE handoff so Phase 7 starts from accurate Phase 6 evidence.
</objective>

<execution_context>
@/home/afgan0r/.codex/get-shit-done/workflows/execute-plan.md
@/home/afgan0r/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-rabbitmq-s3-worker-integration/06-CONTEXT.md
@.planning/phases/06-rabbitmq-s3-worker-integration/06-RESEARCH.md
@.planning/phases/06-rabbitmq-s3-worker-integration/06-VALIDATION.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@README.md
@AGENTS.md
</context>

<decision_coverage>
- D-01 through D-17.
</decision_coverage>

<tasks>

<task type="auto">
  <name>Task 1: Validate worker contract, runtime, and boundary gates</name>
  <files>schemas/parse-job-v1.schema.json, schemas/parse-result-v1.schema.json, crates/parser-contract/examples/parse_job.v1.json, crates/parser-contract/examples/parse_completed.v1.json, crates/parser-contract/examples/parse_failed.v1.json</files>
  <read_first>
    - `Cargo.toml`
    - `crates/parser-contract/src/schema.rs`
    - `crates/parser-contract/src/worker.rs`
    - `crates/parser-worker/src/lib.rs`
    - `crates/parser-cli/src/main.rs`
    - `scripts/coverage-gate.sh`
    - `scripts/fault-report-gate.sh`
    - `scripts/benchmark-phase5.sh`
  </read_first>
  <action>
    Run and fix all Phase 6 quality gates:

    - `cargo fmt --all -- --check`
    - `cargo clippy --workspace --all-targets -- -D warnings`
    - `cargo test --workspace`
    - `cargo doc --workspace --no-deps`
    - `scripts/coverage-gate.sh --check`
    - `scripts/fault-report-gate.sh`
    - `scripts/benchmark-phase5.sh --ci`
    - `cargo run -p parser-contract --example export_worker_schemas -- --output-dir schemas`
    - `cargo test -p parser-contract schema_contract worker_message_contract`
    - `cargo test -p parser-worker`
    - `cargo test -p parser-cli worker_command parse_command`
    - `git diff --check`

    Add boundary grep checks:

    - `crates/parser-core` and `crates/parser-contract` contain no `lapin`, `aws_sdk_s3`, or `tokio::signal`;
    - `crates/parser-worker` contains no `parse_replay_debug`;
    - `crates/parser-worker` and `crates/parser-cli` contain no `health`, `readiness`, `HEALTHCHECK`, or `/health`;
    - active code/docs do not print `AWS_SECRET_ACCESS_KEY`;
    - README and examples do not contain real credentials or password-bearing AMQP URLs.
  </action>
  <verify>
    <automated>cargo fmt --all -- --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace && cargo doc --workspace --no-deps && scripts/coverage-gate.sh --check && scripts/fault-report-gate.sh && scripts/benchmark-phase5.sh --ci && cargo run -p parser-contract --example export_worker_schemas -- --output-dir schemas && cargo test -p parser-contract schema_contract worker_message_contract && cargo test -p parser-worker && cargo test -p parser-cli worker_command parse_command && ! rg -n "lapin|aws_sdk_s3|tokio::signal" crates/parser-core crates/parser-contract && ! rg -n "parse_replay_debug" crates/parser-worker && ! rg -n "health|readiness|HEALTHCHECK|/health" crates/parser-worker crates/parser-cli README.md && ! rg -n "AWS_SECRET_ACCESS_KEY=.*[^*]|amqp://[^\\s]*:[^*@\\s]+@" README.md crates/parser-worker crates/parser-cli .planning/phases/06-rabbitmq-s3-worker-integration && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` exits 0.
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0.
    - `cargo test --workspace` exits 0.
    - `scripts/coverage-gate.sh --check` exits 0.
    - `scripts/fault-report-gate.sh` exits 0.
    - `cargo test -p parser-worker` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update README and planning handoff</name>
  <files>README.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - `README.md`
    - `.planning/ROADMAP.md`
    - `.planning/REQUIREMENTS.md`
    - `.planning/STATE.md`
    - `.planning/phases/06-rabbitmq-s3-worker-integration/06-CONTEXT.md`
    - `.planning/phases/06-rabbitmq-s3-worker-integration/06-VALIDATION.md`
  </read_first>
  <action>
    Update docs only after final gates pass.

    README must document:

    - `replay-parser-2 worker` is implemented;
    - worker consumes jobs with fields `job_id`, `replay_id`, `object_key`, `checksum`, and `parser_contract_version`;
    - worker reads raw objects from S3-compatible storage, computes local SHA-256, and fails structurally on mismatch;
    - worker writes minimal v3 artifacts under deterministic `artifacts/v3/{encoded_replay_id}/{source_sha256}.json` style keys;
    - worker publishes `parse.completed` with artifact bucket/key/checksum/size and parser info;
    - worker publishes `parse.failed` with structured failure data and retryability;
    - manual ack occurs only after confirmed completed/failed result publication;
    - default prefetch is `1`;
    - health/readiness endpoints and multi-worker hardening remain Phase 7;
    - project development uses only AI agents plus GSD workflow.

    Update ROADMAP/REQUIREMENTS/STATE:

    - mark Phase 6 plans complete after execution evidence exists;
    - mark `WORK-01` through `WORK-07` complete/validated where appropriate;
    - move current focus to Phase 7 ready for planning/execution;
    - preserve Phase 7 requirements `WORK-08` and `WORK-09` as pending;
    - record final command evidence and any accepted non-blocking caveats.
  </action>
  <verify>
    <automated>rg -n "replay-parser-2 worker|job_id|object_key|parser_contract_version|parse\\.completed|parse\\.failed|prefetch.*1|AI agents plus GSD|Phase 7|WORK-01|WORK-07|WORK-08|WORK-09" README.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md && git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `replay-parser-2 worker`.
    - `README.md` contains `parse.completed`.
    - `README.md` contains `parse.failed`.
    - `README.md` contains `prefetch`.
    - `.planning/REQUIREMENTS.md` contains `WORK-07`.
    - `.planning/ROADMAP.md` contains `06-05-PLAN.md`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Quality gates to phase handoff | A green subset of tests is insufficient if worker schemas, ack behavior, or boundary greps fail. |
| Documentation to Phase 7 planning | Incorrect handoff could hide unimplemented Phase 7 health/parallel/container work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Repudiation | Phase completion evidence | mitigate | README/ROADMAP/STATE record final gates and exact worker deliverables. |
| T-06-05-02 | Information Disclosure | Documentation/examples | mitigate | Secret grep rejects committed AWS secrets and password-bearing AMQP URLs. |
| T-06-05-03 | Elevation of Privilege | Scope creep | mitigate | Boundary greps confirm parser-core/contract remain transport-free and Phase 7 health/readiness is not added. |
| T-06-05-04 | Denial of Service | Future operator expectations | mitigate | Docs state prefetch default `1` and defer multi-worker/higher-concurrency proof to Phase 7. |
</threat_model>

<verification>
- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace`
- `cargo doc --workspace --no-deps`
- `scripts/coverage-gate.sh --check`
- `scripts/fault-report-gate.sh`
- `scripts/benchmark-phase5.sh --ci`
- `cargo test -p parser-worker`
- `cargo test -p parser-cli worker_command parse_command`
- `git diff --check`
</verification>

<success_criteria>
- All Phase 6 worker contracts, runtime behavior, ack/publish, storage, and CLI tests pass.
- Worker schemas and examples are current.
- README and planning handoff reflect implemented worker mode and preserve Phase 7 boundary.
- No secrets, Phase 7 health endpoints, or transport dependencies leak into parser-core/contract.
</success_criteria>
