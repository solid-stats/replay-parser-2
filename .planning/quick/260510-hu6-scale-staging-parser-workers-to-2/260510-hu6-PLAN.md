---
quick_id: 260510-hu6
description: Scale staging parser workers to 2
status: complete
---

# Quick Task 260510-hu6: Scale Staging Parser Workers To 2

## Goal

Run two `replay-parser-2` worker pods in staging and persist that replica count
in the Kubernetes deployment manifest so future CD runs keep the same capacity.

## Tasks

1. Set `deploy/k8s/staging/deployment.yaml` replicas to `2`.
2. Scale the live staging deployment to two replicas.
3. Verify rollout status and both parser worker pods are Ready.

