# Quick Task 260502-i8w Plan

## Steps

1. Clean generated Phase 5 output.
   - Remove ignored generated files under `.planning/generated/phase-05`.
   - Remove useless tracked generated placeholders from the repo.
   - Add benchmark-script cleanup so future runs start from a clean generated directory.

2. Update default artifact contract.
   - Add `players[].eids`, `players[].tag`, `players[].rn`, and nested `players[].kills`.
   - Remove top-level `ParseArtifact.kills`.
   - Regenerate `schemas/parse-artifact-v3.schema.json`.

3. Update parser-core aggregation.
   - Merge same-name legacy player slots into one `MinimalPlayerRow`.
   - Split legacy bracket tag from nickname.
   - Emit enemy/team kill rows under the killer player only.
   - Keep suicides, null-killer deaths, and unknown deaths as player counters.

4. Update comparison and benchmark harnesses.
   - Derive legacy kill rows from nested `players[].kills`.
   - Add relationship tag/name fields.
   - Replace old all-raw baseline with direct `parseReplayInfo` over every raw file.
   - Count old errors without skipping or aborting the whole baseline.

5. Verify.
   - `cargo fmt --all -- --check`
   - `cargo clippy --workspace --all-targets -- -D warnings`
   - `cargo test --workspace`
   - full benchmark with `RUN_PHASE5_FULL_CORPUS=1 RUN_PHASE5_FULL_OLD_BASELINE=1 scripts/benchmark-phase5.sh --ci`
   - structural benchmark report validation.

6. Document results and commit.
